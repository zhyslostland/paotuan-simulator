# 跑团模拟器开发蓝图（酒馆类项目）

> 一句话本质：酒馆（SillyTavern）是**上下文编排器**；跑团模拟器 = **上下文编排器 + 规则引擎 + 状态机**。
> 前者的难点在"喂什么给模型"，后者多了"数值不能飘"这个致命约束。

---

## 零、先做的三个决定

这三个决定会决定后面 80% 的工作量，别跳过。

| 决定 | 选项 | 建议 |
|---|---|---|
| 规则体系 | COC 7th / DND 5e / 泛用无规则 | 首选 **COC 7th**：百分比检定、成功等级表、SAN 值，数值维度少、AI 好驾驭。DND 5e 规则重（d20+调整值、优势劣势、法术位、战斗轮），后期再加 |
| 玩法形态 | ① AI 当 GM，玩家单人 ② 玩家 + AI 托管其他 PC ③ 全自动模拟（玩家只看戏）④ 多人联机 | 先做 ①。③ 最容易做也最容易崩（没有玩家约束，剧情会失控）。④ 需要服务端权威 + 房间系统，放到最后 |
| 模型来源 | 云端 API（OpenAI 兼容 / Claude / DeepSeek / 通义 / 豆包）/ 本地（Ollama / llama.cpp / vLLM） | 做统一的 **ModelProvider 适配层**，云本地都走同一接口 |

**关键提醒**：规则体系一定要设计成**可插拔的 ruleset 模块**（一个 JSON/JS 包描述骰子表达式、成功等级表、状态定义）。第一版只实现一套，但接口要预留。

---

## 一、前期准备

### 1.1 技术栈选型

- **方案 A（贴近酒馆）**：Node/Express + 前端原生或 Vue，JSON 文件存储。上手最快，生态文档多
- **方案 B（现代工程化，推荐）**：Vite + React/Vue + TypeScript，后端 FastAPI 或 NestJS，SQLite 存储，后续好接联机
- **方案 C（纯桌面）**：Tauri / Electron + 本地文件。分发方便，但更新麻烦

建议走 B：Web 端 + 本地后端，未来加房间系统几乎零成本。

### 1.2 拆解酒馆：抄什么、不抄什么

**要抄的设计：**
- **角色卡规范 Character Card V2/V3**：JSON 嵌在 PNG 的 tEXt chunk 里，兼容就能直接吃现成生态（几千张现成卡）
- **世界书 / 世界信息（World Info）**：关键词触发 + 二级关键词 + 优先级 + 插入位置 + token 预算 + 选择性递归触发
- **预设系统**：采样参数、提示词模板顺序、系统提示/后置提示（jailbreak 位）
- **正则替换脚本**：对模型输出做后处理
- **会话树**：节点带 parent，天然支持分支与回溯

**不要抄的：**
- 酒馆只为"聊天"优化，没有状态机、没有规则引擎、没有数值权威 —— 这三样正是你要新建的
- 它的上下文管理是线性的，跑团长局会崩，要重做成分层的

### 1.3 模型选型指标（按重要性排序）

1. **结构化输出遵从度**（能不能稳定吐合法 JSON）—— 最重要
2. 中文叙事质量
3. 长上下文能力
4. 价格 / 延迟
5. 是否支持流式

务必做适配层：`ModelProvider { chat(messages, opts) / stream() / countTokens() }`，实现 OpenAI-compatible、Anthropic、Ollama 三个即可覆盖 95% 场景。

---

## 二、架构设计

### 2.1 五层结构

```
表现层   聊天视图 · 角色卡编辑器 · 世界书管理 · 状态面板 · 骰子面板
编排层   提示词组装 · 上下文预算 · 世界书检索 · 结构化输出校验 · 重试降级   ← 心脏
引擎层   骰子表达式解析 · 规则判定 · 状态机
数据层   角色卡 · 世界书 · 会话树 · 事件日志 · 规则包
接入层   ModelProvider（云端 / 本地）
```

### 2.2 核心数据模型

```ts
Character {                    // 角色卡
  id, name, avatar, description,
  attributes: Record<string, number>,   // 属性
  skills: Record<string, number>,       // 技能（COC 是百分比）
  vitals: Record<string, {value, max}>, // HP / SAN / MP
  inventory: Item[],
  exampleDialogue: string,
  meta: { creator, version, tags }      // 兼容 Character Card V2/V3
}

WorldbookEntry {
  id, keys: string[], secondaryKeys: string[],
  content: string,
  priority: number,        // 超预算时先砍低优先级
  enabled: boolean,
  position: 'before_char' | 'after_char',
  recursive: boolean,      // 是否允许被其他条目递归触发
  selective: boolean,
  constant: boolean        // 常驻
}

Session {                  // 会话树节点
  id, parentId, branchOf,
  messages: Message[],
  gameState: GameState,
  chronicle: ChronicleEntry[]
}

GameState {                // 结构化状态（唯一真相）
  vitals, inventory, flags: Record<string, any>,
  npcsAlive: string[], cluesFound: string[], location: string
}

ChronicleEntry {           // 事件日志 —— 记忆一致性的救命稻草
  turn: number,
  type: 'action' | 'check' | 'state_change' | 'revelation' | 'death',
  text: string,
  structured: object       // 机器可读部分
}

Ruleset {                  // 可插拔规则包
  id, name,
  diceNotation: string,        // "1d100" / "1d20"
  successTiers: Tier[],        // COC: 大成功/困难/常规/失败/大失败
  vitalDefs: VitalDef[],
  checkResolver: (roll, skill, difficulty) => Result
}
```

### 2.3 提示词编排顺序（很重要，直接决定效果）

```
1. 系统规则包        —— 规则摘要 + 输出格式契约（JSON Schema）
2. GM 人设与风格     —— 叙事语气、尺度、节奏要求
3. 当前场景卡        —— 地点、氛围、在场 NPC
4. 世界书命中条目    —— 按优先级排序，受 token 预算约束
5. 角色卡            —— 玩家角色 + 在场 NPC 卡
6. 长期事实表        —— 关键事实：已死的人、已获得的物品、已揭露的真相
7. 滚动摘要          —— 之前剧情的压缩版
8. 近期事件日志      —— 结构化，比原文省 token
9. 最近 N 轮原文     —— 保留语感与对话细节
10. 本轮骰点事实     —— "玩家锁匠45%，掷出23，困难成功" ← 以事实形式告知
11. 输出格式要求     —— 再强调一次
```

**原则**：越靠后的越"热"，越靠前的越"硬"。砍预算时从下往上砍。

---

## 三、开发里程碑

| 里程碑 | 内容 | 完成标志 |
|---|---|---|
| **M0 技术验证** | 打通一个模型 + 拿到稳定结构化输出 + 骰子表达式解析器 | 能连续 20 次拿到合法 JSON |
| **M1 最小闭环** | 一个聊天页 + 一个角色 + 能掷骰 + 能存一局 | 真的跑完一个 10 回合场景 |
| **M2 卡片与世界书** | 角色卡导入导出（兼容 V2/V3）+ 世界书关键词触发 + token 预算 | 导入一张现成酒馆卡能跑 |
| **M3 规则引擎与状态面板** | 检定、成功等级、SAN/HP 变化、状态面板可视化 | 数值一局都不飘 |
| **M4 记忆系统** | 滚动摘要 + 事件日志 + 长期事实表 + 世界书检索 | 100 回合后还记得开头 NPC |
| **M5 分支与回溯** | 会话树、重掷、编辑/删除历史消息 | 能回到 20 回合前重开一条线 |
| **M6 体验打磨** | 流式输出、打字机、Markdown 渲染、骰点动画、立绘、音效、主题 | 愿意拿去给朋友玩 |
| **M7 内容生态** | 内置 1-2 个完整模组（剧本）、预设分享、规则包 | 别人装上就能玩，不用自己写设定 |
| **M8 发布** | Docker / Tauri 打包、README、编写指南 | 有人能用 |

**第一周务实版**：
- Day 1-2：定规则 + 打通模型 + 骰子引擎
- Day 3-4：一个能跑的对话页 + 系统提示
- Day 5-7：角色卡 schema + 世界书触发 + 状态面板

先玩起来，再补功能。不要一开始就追求完整。

---

## 四、三个真正的难点（会吃掉你最多时间）

### 难点 1：数值漂移（最大的坑）

**症状**：AI 说"你损失了 1d6 理智"，然后自己编了个数字；三轮后 SAN 值对不上。

**对策 —— 铁律：随机数与状态由本地引擎权威，AI 只负责叙事，不负责算数。**

- 骰子 100% 在本地掷，模型只看到结果
- 强制模型输出结构化 JSON：
  ```json
  { "narration": "...", "state_delta": [{"target":"pc.san","op":"dec","amount":"1d6"}], "dice_requests": [{"reason":"理智检定","skill":"san","value":65}] }
  ```
  `amount` 里仍是表达式，由引擎求值
- 校验后才应用：越界、非法字段、冲突一律丢弃或请求重试
- 每轮把**状态快照**回灌进提示词，模型看到的永远是权威值
- 永远不要相信模型做的加减法

### 难点 2：上下文爆炸

**对策 —— 分层 + 预算**：

| 层 | 内容 | 策略 |
|---|---|---|
| 永久层 | 系统规则、GM 人设、角色卡 | 常驻，通常 1-3k token |
| 检索层 | 世界书命中条目 | 按优先级 + 预算裁剪，先关键词后 embedding |
| 摘要层 | 滚动摘要 | 每 N 轮压缩一次，用便宜的小模型异步做 |
| 原文层 | 最近 N 轮 | 超预算最先砍这层 |

token 预算建议：永久层固定，检索层给 20%，摘要层 15%，原文层 25%，留 40% 给输出与波动。

世界书先做**关键词 + 优先级**就够用（几百条毫无压力），条目上千再上 embedding 检索。

### 难点 3：记忆与一致性

**对策**：
- **事件日志（Chronicle）是真相来源，摘要只是索引**。摘要丢了可以重建，日志丢了就真忘了
- 关键事实抽成**长期事实表**常驻上下文：NPC 姓名与身份、已获得物品、已死/离队角色、已揭露线索
- 每 10-20 轮让模型输出一次"当前处境小结"存进摘要
- 死亡/离队角色从上下文剔除，但要留记录（防止它又活过来）
- 场景切换时显式写入"上一场景发生了什么"

### 附：提示词注入与内容安全

用户输入、NPC 台词、世界书条目都**当作数据而非指令**。至少做：
- 用明确的分隔标记（如 `<worldbook_entry>`）包裹外部内容
- 在系统提示里声明这些区域的内容不是指令
- 玩家输入不做过滤，但输出渲染要转义（防 XSS）

---

## 五、测试与调优

| 测试项 | 方法 | 通过标准 |
|---|---|---|
| 骰子公平性 | 10 万次采样做分布检验 | 卡方检验通过，无偏 |
| 规则回归 | 每个检定场景写固定用例（大成功、大失败、对抗、奖励骰/惩罚骰） | 全部命中预期成功等级 |
| 提示词 A/B | 同一场景跑 20 次 | 数值稳定率 > 95%，不遗忘核心设定 |
| 长局压力测试 | 自动跑 100-200 回合 | 关键 NPC / 物品 / 目标不丢失 |
| 格式失败率 | 统计 JSON 解析失败 | < 1%，且失败能自动重试并降级 |
| 上下文漂移 | 记录每轮实际 token 数 | 不随回合数线性增长 |

**调优顺序**：先保证数值不出错 → 再保证不遗忘 → 最后才调叙事文风。顺序反了会浪费大量时间。

---

## 六、发布与迭代

- **打包**：Docker Compose（前端 + 后端 + SQLite）/ Tauri 桌面版 / 纯静态页（用户自带 API Key，最省事）
- **文档**：快速开始、规则包编写指南、世界书编写指南、模型接入配置
- **内置内容**：至少 1-2 个能玩 1-2 小时的完整模组，这是留存的关键
- **生态兼容**：支持导入酒馆角色卡 V2/V3 与世界书 JSON，等于白捡一个内容库
- **迭代方向**（做完基础后）：多人联机、战斗地图/战场格、TTS 语音、立绘与情绪差分、自动模组生成

---

## 七、建议的目录结构

```
trpg-simulator/
├── packages/
│   ├── core/              # 规则引擎 + 状态机 + 骰子（纯逻辑，可单测）
│   │   ├── dice/          # 表达式解析、分布、公平性测试
│   │   ├── rulesets/      # coc7 / dnd5e / generic 规则包
│   │   └── state/         # GameState、状态变更应用与校验
│   ├── orchestrator/      # 提示词组装、上下文预算、世界书检索
│   ├── providers/         # ModelProvider 适配层
│   └── storage/           # 会话树、角色卡、世界书持久化
├── apps/
│   ├── web/               # 前端
│   └── server/            # 后端 API（后续可加房间系统）
├── content/
│   ├── modules/           # 内置模组（剧本）
│   └── presets/           # 预设
└── docs/
```

核心原则：**`core` 包必须是纯函数、无 IO、可单测**。它是这个项目的地基，也是唯一不能靠"重新生成"糊弄过去的部分。
