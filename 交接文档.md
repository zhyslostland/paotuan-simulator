# 跑团模拟器 · 项目交接文档

> 这份文档的作用：**让一个新的对话（或新的协作者）读完就能接着干**。
> 新对话开场只需说：「读 `交接文档.md` 和 `.workbuddy/memory/`，我们继续做跑团模拟器。」

---

## 1. 这是什么

一个**单人自用的 AI 跑团模拟器**（类酒馆 SillyTavern，但多了规则引擎 + 状态机）。

**用户需求（长期有效）**：

1. **自己一个人玩**，不公开发布。细节要打磨，**外观要好看**。
2. **PC + 手机双端**，手机要能**独立运行**（不依赖 PC 开机）。
3. **泛用规则接口**打底，先做 COC 7th，将来能挂 DnD / 各种小规则。
4. **AI 单机 + 单人玩家**；模型接 API（用户用**硅基流动 + DeepSeek V3.2**）；生图留接口。
5. **重视沉浸感**：反感"为了迁就系统而增加的操作负担"。交互优先"自然语言 + 系统自己消歧"。
6. 推进策略（用户定）：**按顺序一个个做，"先完成再完美"，每做完一块主动提醒用户测试并给反馈。**

---

## 2. 当前状态

| 项 | 值 |
|---|---|
| 版本 | 可玩，已完成 P0 全部 + 大部分 P1 |
| 测试 | **68 个全通过**（dice 12 / coc7 13 / state 19 / store 14 / prompt 10） |
| 部署 | **https://aada7b06d66143b68180d328e85609ac.app.workbuddy.host**（CloudStudio 沙箱，sandboxId 稳定不变） |
| 规则包 | 1 套（COC 7th） |
| 数据 | 全部在浏览器 localStorage（无后端） |

**当前部署链接是稳定的**：多次重新部署 sandboxId 都是 `aada7b06d66143b68180d328e85609ac`，用户手机上的主屏快捷方式不会失效。

---

## 3. 怎么跑

```bash
cd <项目根>
npm install            # 首次
npm run dev            # 开发服务器（--host，手机同 WiFi 可访问）
npm run build          # 构建到 dist/
npm run test           # 跑全部测试
npm run typecheck      # tsc --noEmit
npm run demo           # 无模型的完整回合演示脚本
```

**部署**：构建后用 `workbuddy_cloudstudio_deploy` 工具，参数
`directory=<项目根>/dist`、`entry=index.html`、`port=3000`。

**技术栈**：Vite 8 + React 19 + TS + **Tailwind v4** + zustand + react-markdown + vite-plugin-pwa。

> ⚠️ 依赖版本坑：vitest 5 才支持 vite 8；升级时要 `npm uninstall vite vitest` 再装，否则版本冲突。

---

## 4. 架构与文件地图

**五层架构**：表现层 / 编排层 / 引擎层 / 数据层 / 接入层。纯前端，无后端。

```
src/
  core/                        ← 引擎层：纯函数、无 IO、可单测
    dice/{parse,roll,index}.ts     骰子表达式解析 + 可注入 RNG + 分布枚举
    rulesets/
      types.ts                     Ruleset 接口（可插拔规则包的契约）
      coc7.ts                      COC 7th 实现（成功等级/奖励骰/属性/技能表）
      index.ts                     getRuleset(id)
    state/gameState.ts             GameState + applyDeltas()（防数值漂移的核心）
  orchestrator/                ← 编排层：系统的心脏
    prompt.ts                      系统提示词组装 + stripMeta/dedupeNpcLines/isRefusal
    generate.ts                    各类 AI 生成（角色/队友/世界书/模组）+ 宽容 JSON 解析
  providers/
    model.ts                       OpenAI 兼容流式接入（SSE）+ generateImage 占位
  ui/
    store.ts                       zustand 全局状态 + localStorage 持久化（唯一数据源）
    App.tsx                       主界面 + sendToGm（回合流程）+ 开团/回溯/重掷
    Chat.tsx                      对话区（流式/骰点卡/检定卡/NPC 台词卡/回溯重掷按钮）
    CharacterSheet.tsx            角色卡 + CheckDialog（检定面板）
    WorldPanel.tsx                世界面板（同行者/在地人物/线索/剧情标记/编年史）
    Preparation.tsx               前期准备（模组/角色卡/同行者/世界书，均可 AI 生成）
    Settings.tsx                  设置（主题/服务商预设/API/参数/存档导入导出）
    theme.css                     3 套主题（CSS 变量 + @theme inline）
  demo.ts                       ← 无模型的完整回合演示
tests/                           ← dice / coc7 / state / store / prompt
```

---

## 5. 关键设计决策（**务必遵守**）

### 5.1 铁律：骰子与状态由本地引擎权威，AI 只写叙事

- 模型只输出**变更意图**：`state_delta`（amount 可以是 `"1d6"`，由本地掷）、`dice_requests`、`location`、`summary_delta`、`npc_lines`。
- 绝不信任模型算的数；路径白名单；规则包未定义的数值条一律拒绝；只 reject 不抛异常。
- 输出契约是回复**最后**的 ```json 块，`extractContract()` 提取。

### 5.2 规则包可插拔（"泛用规则"的落地方式）

`Ruleset` 提供：`mainDice` / `vitalDefs` / **`characteristicDefs`（属性集）** / **`skillCatalog`（标准技能表 + 基础值）** / `resolveCheck` / `tierLabel`。
**换规则只换这一层**，角色卡界面与提示词会自动跟着变。

### 5.3 三层内容与版权

- **机制**（判定公式）→ 内嵌代码，合法。
- **数据**（技能表/物品表）→ 放 ruleset / 外挂 JSON。
- **表达**（规则书原文/模组设定文本）→ **绝不进系统**。

### 5.4 角色卡 = 叙事层（对齐 SillyTavern Card V2）+ 数值层（规则驱动）

- 叙事层：`name` / `gender` / `description` / `personality` / `mes_example`（+`address` 称呼）
- 数值层：`characteristics`（COC 八属性）+ `skills`
- 旧字段 `occupation/age/portrait/background` 已废弃，`migrateCharacter()` 会合进 `description`。

### 5.5 四层记忆

永久层（规则/角色/状态快照）→ 摘要层（远期压缩）→ **事件日志（Chronicle，每轮一句客观事实，从头保留）** → 原文层（最近 20 轮）。
**事件日志是真相来源，摘要只是索引。**

### 5.6 模组 = 故事骨架，不是剧本

对 AI 守密人来说不需要完整剧本，只需够硬的骨架：`title` / `premise` 前言 / `opening` 开场白 / `truth` 真相 / `npcs[]`（动机+秘密）/ `locations` / `clueChain` 线索链 / `acts` 幕结构 / `endings` / `notes`。
**世界书是"血肉"（关键词触发），模组是"骨架"。**
**故事的四个来源**：① 报名字就开团 ② AI 一键生成 ③ 自己写 ④ 导入外部模组。

### 5.7 剧情保密

- 提示词里「真相」标注**绝不可直接告知玩家**，只能靠事件与线索逐步揭示；线索链要求"逐步给出，不要一次抖完"。
- 界面上 **GM 内部资料默认遮罩**（`ModuleTab` 的 `revealed` 状态），**AI 生成后保持隐藏**——免得用户（也是玩家）提前看到剧透。

### 5.8 提示词红线体系 + 客户端兜底（**双保险模式**）

提示词负责引导，客户端兜底负责保证下限。已建立的成对规则：

| 提示词红线 | 客户端兜底 |
|---|---|
| 不替玩家说话 / 不写玩家念头 | （靠提示词） |
| 危险举动必须走检定 | （靠提示词） |
| NPC 台词只走 npc_lines，正文不重复 | **`dedupeNpcLines()`** 删掉正文里重复的引号对白 |
| 只输出故事，不输出分析/指令解析 | **`stripMeta()`** 按行清除过程性文字 |
| **永不拒绝玩家、不跳出戏外** | **`isRefusal()` 检测 → 追加 `RETRY_NOTE` 自动重生成一次** |
| 性格只是参考、不是枷锁 | （靠提示词） |
| 动作 vs 台词由模型自行判断（不加标记） | （靠提示词） |

### 5.9 交互偏好

不引入格式约定（如"用「」包台词"）、不加额外控件，除非无解。玩家输入默认按**动作**理解，拿不准一律当动作。

---

## 6. 数据与存储

全部在 localStorage（无后端、无账号）：

| key | 内容 |
|---|---|
| `trpg.config` | API 配置（地址/key/模型/温度/maxTokens） |
| `trpg.character` | 角色卡 |
| `trpg.module` | 模组（故事骨架） |
| `trpg.theme` | 主题 |
| `trpg.worldbook` | 世界书条目 |
| `trpg.chronicle` | 事件日志 |
| `trpg.summary` | 远期摘要 |
| `trpg.messages` | 对话记录 |
| `trpg.gameState` | 游戏状态（数值/线索/物品/队友/地点） |
| `trpg.snapshots` | 回合快照（回溯/重掷用） |

设置页可导出/导入存档 JSON（含上述内容，不含 API Key）。

---

## 7. 已完成

**引擎与地基**
- 骰子引擎：`2d6+3`/`d%`/括号/单目负；可注入 RNG + `seededRng`；分布枚举；10 万次卡方检验
- COC 7th 规则包：六档成功等级、奖励/惩罚骰、100 必大失败、技能<50 时 96-99 大失败
- 状态机 `applyDeltas`：路径白名单、表达式本地求值、min/max 裁剪、非法只拒不出错
- 规则驱动：属性集（STR/CON/…）+ 标准技能表（46 项含基础值）

**应用**
- 纯前端 PWA 双端 + 3 套主题（午夜暗金/冷灰/羊皮纸）；
- 编排层：8+ 段提示词组装、流式输出、针对 DeepSeek V3.2 优化（关思考模式、忽略 reasoning_content、maxTokens 3072、结尾禁总结）
- 接入层：OpenAI 兼容协议（DeepSeek/硅基流动/智谱/通义/豆包/Ollama 预设）+ 测试连接
- 防漂移闭环：模型请求检定 → 本地掷骰 → 结果作为既成事实回灌 → 叙事 → 契约 → 状态机校验
- NPC 队友：主动性三档（被动/均衡/主动）、防抢戏硬边界、独立台词卡
- 前期准备：模组 / 角色卡 / 同行者 / 世界书，**四者都能 AI 生成**
- 分层记忆：事件日志 + 滚动摘要 + 原文层，自动折叠
- 持久化 + 存档导入导出 + 复制对话记录（便于排查）
- 检定面板：三档难度说明 + 对象分组（环境/在场人物/同行者）
- **分支回溯 / 重掷 / 编辑重发**（回合快照机制）
- 编年史与线索面板
- **模组卡升级为故事骨架 + AI 一键生成 + 剧透遮罩**
- **「开团」入口**（`startNewGame`：清剧情、满状态、按模组开场）
- 技能标准表挑选器（按基础值加入）
- 整套提示词红线 + 客户端兜底
- **「模组包」**：生成模组时**连带产出**世界书词条（只含公开事实，不剧透）+ **队友候选 3-4 个**（时代/地点贴合，用户挑谁入队）；给 AI 产出的条目/队友打 `fromModule` 标记，重新生成只替换机器产出，手写内容不动
- **「报个名字就开团」**：模组生成加 `canonical` 模式（`moduleUserPrompt()`），用户给已出版模组名时按原版设定填卡

---

## 8. 待办路线图

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P1** | **第二套规则包（DnD 风格）+ 规则切换** | 用户明确要求**傻瓜式**：每个规则包带 `beginnerGuide`（大白话解释什么是技能/判定/难度/DC），显示在角色卡与检定面板、并喂给 GM。DnD-lite 建议：`mainDice='1d20'`、技能存**加值**、`resolveCheck` = d20+加值 ≥ DC（常规12/困难15/极难18）；需把检定 UI 的 `%` 与 `≤` 文案改为规则包驱动 |
| **P1** | **模组智能导入** | **不是格式兼容**（市面上模组是 PDF/文本，没有通用交换格式），而是"贴任意文本 → AI 转成模组卡"。需新增 `IMPORT_SYSTEM` + 文本导入 UI |
| P1 | 模组导出 | JSON（本应用复用）+ Markdown/文本（人或别的 AI 都能读） |
| P2 | 生图接入（场景/立绘） | `generateImage()` 接口已留；用户说"接入并实测速度" |
| P2 | 多存档槽 | 目前单进度，多局并存要手动导出 |
| P2 | 音效 / BGM | 氛围 |
| P3 | 契约失败重试与降级 | JSON 不合规时自动重试一次 |
| P3 | 网络中断重试 | 弱网/切后台体验 |
| P3 | 模型参数面板 | top_p / 思考模式开关做成可调 |
| P3 | 移动端性能打磨 | 长对话列表虚拟化等 |

---

## 9. 踩坑与教训（**避免重复踩**）

1. **`{...默认值, ...存档}` 的陷阱**：默认值里**不能放"会随用户输入变化"的字段**（姓名/称呼/性别等）。老存档缺该键时会被默认值填上、盖掉用户的设置。→ 默认值只放真正的常量；用 `mergeCharacter` / `mergeModule` 这类纯函数显式迁移。
2. **不要给元素同时加 `w-full` 和 `w-XX`**：Tailwind 的 CSS 顺序里 `w-full` 往往排在 `w-XX` 之后 → **`w-full` 胜出**；再叠加 `shrink-0`，它会霸占整行把旁边的元素挤没（技能名就是这么消失的）。flex 行内占满用 `flex-1 min-w-0`，定宽用专用类。
3. **可编辑列表不要用数组下标当 React key**，改值不要重建对象（会重排+串位）。改键名要保顺序，且把输入提交时机从"每次按键"改为"失焦/回车"。
4. **"管道铺好没接线"**：多次出现"定义了数据结构/契约字段却没接 UI 或持久化"（世界书传空数组、summary_delta 没接、messages 没持久化）。**加新状态时必须同步检查持久化与接线。**
5. **纯前端应用，用户辛苦积累的数据必须持久化**；建议用户"刷新"之前先确认已落盘。
6. **部署工具会 429**（有调用频率上限）——直接重试即可，不必换沙箱。
7. **诊断 UI 问题要看原图**：预览图会被压缩丢细节，用 Pillow 裁剪放大（已装在 `.workbuddy/binaries/python/envs/default`）。
8. **测试环境**：store 在模块加载时就读 localStorage，node 测试里必须先 `vi.stubGlobal('localStorage', ...)` 再用**动态 import** 引入 store。

---

## 10. 在新对话里怎么接着做

**方法（推荐）**：**在同一个工作区（`跑团模拟器`）里新开一个对话**，开场说一句：

> 读 `交接文档.md` 和 `.workbuddy/memory/`（尤其 `2026-09-12.md` 与 `MEMORY.md`），继续做跑团模拟器。

新对话会自动拿到：同一份磁盘文件、同一个 git 仓库、同一份项目记忆。
**无需**打包搬运、无需重新安装依赖（`node_modules/` 已在原地）。

**如果确实要换到别的工作区**：把整个项目目录（含 `.workbuddy/`，但可排除 `node_modules/` 与 `dist/`）复制/打包过去，在新位置 `npm install` 后即可。

**注意**：`present_files` 只能投递工作区内的文件；`.workbuddy/` 是项目数据目录（含记忆与交接文档），**不要删**。
