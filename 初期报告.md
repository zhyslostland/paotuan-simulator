# 跑团模拟器 · 初期报告

> **面向用户的项目体检 + 规划。** 写于 2026-09-15 晚，代码基线 `65e7ba5`。
> 用途：① 回答"每 5 分钟拉一次会不会消耗性能"；② 系统过一遍性能与架构风险；
> ③ 核对"项目立意 → 实际落实"有没有跑偏；④ 把接下来该做什么排好队；
> ⑤ **主动盘点你这几天提过、可能已忘记的需求**。
>
> 阅读顺序建议：**第 1 节（先回答你最关心的）→ 第 4 节（需求盘点，最可能有遗漏）→ 第 3 节（立意偏差）→ 第 2 节（性能）→ 第 5 节（规划）**。

| 体检项 | 结果 |
|---|---|
| `tsc --noEmit` | ✅ 通过（strict + `noUncheckedIndexedAccess`） |
| `vitest run` | ✅ **9 文件 / 193 条全过**（1.15s） |
| `vite build` | ✅ 通过 |
| 代码规模 | `src` **14,093 行** / 35 个源文件 |
| 产物规模 | `dist` 787K，主 JS **717,829 字节（≈700KB）**，CSS 44KB，`version.json` 49 字节 |
| git | 工作区干净，HEAD `65e7ba5` |

**一句话结论**：项目没有跑偏，两条北极星（沉浸不中断 / 主角存在感）在实现里都能逐条对上；
性能上没有发现会"卡住玩家"的问题，**你问的 5 分钟轮询开销完全可以忽略**；
但有几个"长局会慢慢变重"的地方值得在接下来的批次里顺手修掉（都不紧急，见第 2 节）。

---

## 1. 先回答你的问题：每 5 分钟拉一次会不会消耗性能？

**结论：不会。可以放着不管，不用改。**

### 1.1 算一笔账

`src/update.ts` 每 5 分钟做的事，就是对一个 49 字节的静态文件发一次 GET：

| 项 | 实测值 |
|---|---|
| `version.json` 体积 | **49 字节** |
| 请求频率 | 每 5 分钟 1 次 = **12 次/小时 = 288 次/天** |
| 一天的总流量 | 49 × 288 ≈ **14 KB** |
| 对你的 API 账单 | **0 元**（这是静态文件，不经过模型服务商） |
| 对模型 token 的消耗 | **0**（跟 AI 无关，纯静态探测） |

对比一下：你每发一条消息，模型要吃掉几万 token。这个轮询的"成本"大约是**一条消息的百万分之一**。

### 1.2 你可能真正担心的副作用，我逐条查了

| 担心 | 实际情况 | 判定 |
|---|---|---|
| 会不会反复重绘、卡界面？ | 探测结果只有"一样"或"不一样"两种，只有在**真的出新版本**时才 `setUpdateReady(true)` 弹一次横幅。除此之外**一个 React 状态都不动** | ✅ 无影响 |
| 会不会把服务器打爆？ | 已经有 **30 秒节流**（`if (!force && now - lastCheck < 30_000) return`），而且只在"回到前台 / 定时到点"才触发 | ✅ 已兜住 |
| 回到前台会不会连着触发好几次？ | 会（`visibilitychange` 可能连发），但被上面那道 30 秒闸门挡住 —— 这个守卫是**专门为此写的** | ✅ 已兜住 |
| 手机流量/耗电？ | 一天 14KB，比一张缩略图还小。且浏览器后台标签页的定时器会被节流，实际次数**低于** 288 次 | ✅ 可忽略 |
| 离线时会怎样？ | `fetch` 失败 → `catch` 掉 → 当作"没新版本"，静默返回。**不会弹错误、不会重试堆积** | ✅ 安全 |

### 1.3 唯一一个可选的微调（不做也行）

现在是"定时器 + 回前台"两路都在跑。可以再省一点：**页面在前台时用 5 分钟定时器；切到后台时直接 `clearInterval`，回前台时立即补一次探测。**

现在的写法已经很轻，做这个优化的收益是"从 14KB/天降到约 3KB/天"——**不值得为它冒改坏更新检测的风险**。我的建议是**不动**。（真要动的话，30 秒节流那道闸门必须保留。）

---

## 2. 性能体检：其他发现（按优先级排）

我把源码里所有"会随游戏时长增长"或"每帧/每次输入都跑"的地方都过了一遍。**没有一个是会当场卡住的**，以下是"长局会慢慢变重"的清单。

### 🟡 P1-1 `saveJson` 全量序列化：45 处写入点，其中一半没有节流

`src/ui/store.ts` 里有 **45 处** `saveJson('trpg.*')`。每次都把整个对象 `JSON.stringify` 一遍再塞进 localStorage（**同步阻塞主线程**）。

已经做对的：
- `trpg.messages`（最易变的）有 **800ms 节流**（`scheduleSaveMessages`），流式输出期间不会疯狂落盘；
- `trpg.*` 都是小对象，正常单次写入在**毫秒级**。

问题在两处：
1. **`trpg.snapshots` 没有节流**，而且它是**最重的那个**：每个快照都完整存了一份 `gameState` + 编年史 + 摘要，最多保留 **60 份**（`SNAPSHOT_LIMIT`）。长局到 60 回合时，这一个键的序列化量可能是其他所有键加起来的几倍。而它每回合至少被写 **2 次**（`snapshotTurn` + `markSnapshotKey`/`setSnapshotPendingChecks`）。
2. **`applyModelDeltas` 每次都写 `trpg.gameState`**（`store.ts:2489`）。一轮里模型可能分几次 delta（状态 + 地点），就是几次全量写。

**建议（下一批顺手做）**：
- 给 `snapshots` 也套 800ms 节流（它只是回溯用，晚 800ms 落盘完全无感）；
- `applyModelDeltas` 里那次写改走节流。
- **预期收益**：一回合的同步写入次数从 3-4 次降到 1 次；长局（50+ 回合）每回合省下的主线程时间在**几十毫秒**量级 —— 对手机端尤其明显。

### 🟡 P1-2 `WorldPanel` 每次 render 都全量重算（无 `useMemo`）

`src/ui/WorldPanel.tsx`（912 行）里，这几个量**每次 render 都重新计算**，一个 `useMemo` 都没有：

| 计算 | 代价 | 增长性 |
|---|---|---|
| `knownText` | 把**整部编年史 + 全部线索 + 摘要** join 成一个长字符串 | ⚠️ **随局长线性增长** |
| `mapNodes` / `hasLinks` | 遍历模组地点 + `visited` + 相邻推导出迷雾 | 随地点数增长（最长篇 12 个，可控） |
| `anchors` | filter + map + reverse 快照表 | 随快照数增长（最多 60） |
| `visitedNames` / `revealed` | Set 构造 | 小 |

`knownText` 是最值得修的一个：它拼的是**全量**文本，100 回合的局可能拼出上万字，而每敲一个字、每次流式输出都会触发一轮 render。

**建议**：给 `knownText` 加 `useMemo`（依赖 `[chronicle, clues, summary]`），另外三个顺手包上。**这是一个 20 分钟的改动，收益确定。**

### 🟡 P1-3 IndexedDB 连接没有复用

`src/ui/idb.ts` 的 `openDb()` **每次调用都 `indexedDB.open()` 开一个新连接**；`audio.ts` 的 `getAudio()` 每次 `playSfx` 也会开一次。

单次开销不大（几毫秒），但：
- 战斗中连续播音效（进入战斗 + 受伤 + 大成功）会连着开好几次；
- 保存消息图片、读地图图片也各开一次。

**建议**：把 `openDb()` 的 Promise 缓存成一个模块级单例（一次连接反复用）。**改动很小、收益稳定。**

### 🟢 P2-1 主包 700KB，没有做代码分割

`dist/assets/index-*.js` 是 **717,829 字节**，**全部模块打成一个包**：`Preparation.tsx`（88KB 源码）、`Settings.tsx`（66KB）、`TestSandbox.tsx`（22KB）这些**只有特定页面才用得到**的东西，玩家开局加载时**全都要下载并解析**。

- 首屏在 WiFi 下大概 1 秒内，**可接受**；
- 但手机 4G 下、以及"只是想打开看一眼"的场景，这 700KB 是纯浪费。

**建议**（不急，属于体验优化）：给 `Preparation` / `Settings` / `TestSandbox` / `Changelog` 上 `React.lazy` + `Suspense` 拆成独立 chunk；再给 `react-markdown`（75KB）单独分块。**理论上首屏包能砍掉一半左右**。因为你们是 PWA + 离线优先，第二次打开本来就有缓存，所以这件事的优先级**低于前面的 P1**。

### 🟢 P2-2 `Chat.tsx` 已有 120 条切片 + `memo`，但不是真虚拟滚动

`MessageRow` 已经 `memo` 住（props 全是原始值），并且只渲染**最后 120 条**（`renderCount = 120`）。这在移动端是**当前性价比最高的做法**，我认可这个取舍（协作清单 2.3 也把"真虚拟滚动"列为刻意不做）。

需要留意的只是：**120 条 Markdown 仍然要解析**。如果将来玩家抱怨"翻很久的聊天记录会顿"，再考虑降到 60 条或上真虚拟滚动。**现在不用动。**

### 🟢 P2-3 编年史增长有闸门，但闸门是"事后"的

`foldChronicleIfNeeded`（`App.tsx:261`）在编年史超过 **50 条**时，把早期条目折进摘要、只留最近 **20 条**（`CHRONICLE_FOLD_AT = 50` / `CHRONICLE_KEEP = 20`）。这个设计是对的。

唯一的边角：**折叠依赖一次模型调用**（`FOLD_SYSTEM`）。如果这次调用失败（网络/额度），代码是 `console.warn` + 保留完整日志、**下次重试** —— 处理得很稳。所以最坏情况是"日志涨到 60-70 条"，不会失控。**保持现状。**

### ✅ 已经做对、值得记下来的性能设计

- `MessageRow` 的 `memo` + props 全原始值（`Chat.tsx:228`）
- `scheduleSaveMessages` 的 800ms 节流
- 图片/音频**一律走 IndexedDB**（不塞 localStorage）—— 这是根治"立绘消失"的那一刀
- 更新探测的 30 秒节流闸门
- `dist/version.json` 刻意排除在 SW precache 之外（否则探测永远探到旧的）
- 团队只装了 5 个运行时依赖（react / react-dom / react-markdown / remark-gfm / zustand），**没有引入 lodash / moment 这类重型库** —— 包体控制得很克制

---

## 3. 立意核对：项目要什么 vs 实际做成了什么

拿 `项目台账.md` 第 1 节（你定下的方向）逐条比对当前实现。

### 3.1 两条北极星

| 北极星 | 落实情况 | 判定 |
|---|---|---|
| **沉浸不中断**（长玩不出硬错、不出戏） | 契约解析有重试兜底（`isRefusal` / `stripMeta` / `dedupeNpcLines`）；重试路径**不清屏**（新内容长过旧内容才替换）；更新检测双路保证"你打开的就是最新版"；结档有**唯一出口**（任何来源置 ending 都会去要一段结局正文，不会再出现"屏幕上什么都不发生"）；求死由引擎结算（不发给模型、不会被安全对齐劝回来） | ✅ **达成**。这几个都是"不出戏"的硬保障 |
| **主角存在感**（每轮都有"选择是我的、成长可见"） | 检定面板的**难度选项已被移除**（不让你自己做元游戏决策）；描述加权**改的是目标值、并且显示给你看**（"含描述加分 +15"），你在掷骰前就知道自己的描述起了作用；状态变化会弹「状态变化」卡片（数值不静默更新）；"关键抉择"列出你走过的岔路口，随时可退回 | ✅ **达成**，而且是"后来特意改对的"（见 3.3） |

### 3.2 明确拒绝项 —— 核对有没有偷偷加回来

| 你拒绝的 | 检查结果 |
|---|---|
| **免死 / 改命按钮** | ✅ 没有。回溯是"退回某个关键抉择重来"，代价是这段剧情作废，不是"复活继续演"。符合"用可靠读档替代免死"的定调 |
| **fail-forward**（"少死但永久失去"） | ✅ 没有实现 |
| 多人 / 社区 / 商业化 / 战棋网格 / 重度刷数值 | ✅ 全都没有。依赖表极其干净，没有任何后端/账号/支付相关代码 |
| 猎奇致郁、背叛剧情、强队友群像、女频套路 | ✅ 提示词层与内置题材都没有往这些方向走。队友是 `reactive`（被动，不抢戏）默认口吻，`proactive` 也要"先征询玩家"；内置 5 个题材里的"情感线（粉红团）"是**柔和路线**，不是让你看剧情被背叛 |
| **不做选项菜单引导** | ✅ 卡住时只给**剧情内的抓手**（提示词层），没有出现"请选择：A/B/C"这种菜单 |

### 3.3 已拍板的玩法 —— 逐条对账

| 定论 | 实现情况 | 状态 |
|---|---|---|
| **死亡 = 结档**，结局正文由守密人写 | ✅ `applyDeltas` 写空正文 ending → App 的 `useEffect` 唯一出口去要结局。**濒死给一轮缓冲**（第一次归零只是标记 `dying`，下一个结算点仍为 0 才真结档） | 完成 |
| **主动求死 = 引擎直接结算** | ✅ `store.forceEnding()`，`SUICIDE_RE` 命中后二次确认，**不发给模型** | 完成 |
| **回溯永远保留**，锚点在结档页与常规 UI 两处 | ✅ `AnchorList` 在 `EndingScreen` 与世界面板复用；快照保留 60 回合 | 完成 |
| **短/长模组**（世界不延续 / 跨局延续）+ 角色独立成**档案库** | ⚠️ **只做了一半**：`Module.scale`（短/中/长篇）已落地，时间尺度与分章骨架都写死了；但"世界跨局延续"的数据模型（`Campaign`）**还没做** | **待做（Phase 2 地基）** |
| **卡住时只做剧情内抓手**，不做选项菜单 | ✅ 提示词层已写死（`ctx.module.goal` 会持续推回玩家面前） | 完成 |
| **带图战报**（关键节点自动生图 + 总开关 + 手动按钮） | ⚠️ 手动生图已有（`ImageField` / `Chat` 里的生图按钮 + `ImageLightbox` 可点开放大）；**"关键节点自动生图 + 总开关"没做** | **待做** |
| **以玩家为中心，规则退到后台** | ⚠️ **曾经跑偏、已纠正**（见下） | 已纠正 |

### 3.4 曾经跑偏、后来拉回来的（值得记下来，防止再犯）

| 跑偏 | 症状 | 怎么拉回来的 |
|---|---|---|
| **规则跳出来抢戏** | 检定面板一度让你**自己选"普通/困难/极难"**，还一度把"描述加权"做成**调难度档位** | ① 移除难度选项（难度该由行动本身决定，不该由你宣布）；② 加权改成**调目标值**（因为难度档需要先选难度才有东西可改，而你本来就不该选难度 —— 加权形同虚设）。现在改的是引擎算出的目标值，**掷骰前**加，判定/成功率/标签三者一致 |
| **描述加权方向错了** | "写两句 +1、写一堆 +0" —— 惩罚了认真写的人 | 重写 `weighDescription()`：**以长度为主**（15 字 +1、45 字再 +1），提到场上真实存在的东西再 +1，提到手里没有的武器 −2。空描述不罚 |
| **主题绑死在克苏鲁** | 生成提示词里写死"1920 年代 / 克苏鲁"，换题材就矛盾 | 抽出 `src/core/genres.ts`，**题材 × 规则正交**；所有生成提示词改成 `(genre, ruleset)` 双驱动；兜底开场白也按题材分 |
| **开局剧透** | 内置模组里直接写了"青铜小像""圣烛照相馆正在清理痕迹"—— 等于开局把真相摊开 | 立下硬规矩：`goal/stakes/urgency` **只写玩家开局就能知道的信息**，真相与关键物品的真实性质一律留在 `truth` 里 |
| **开局不知道该干嘛** | 早期模组没有 `goal`，玩家进来一片茫然 | "模组必须给目标"写进提示词与台账：`goal` + `stakes` + `urgency` 三件套，开场文本与 UI 都摆出来 |
| **世界观空转** | 世界书里都是"没有啊"这类无信息条目 | 世界书条目要求"只写客观事实（是什么、长什么样、有什么规矩）"，不要写剧情走向 |

### 3.5 立意核对结论

**整体判定：项目立意没有跑偏。** 两条北极星都在实现里能逐条找到对应的机制，拒绝项一条都没偷偷加回来。

真正"没做到"的不是立意问题，而是**Phase 2 的地基还没打**：短/长模组里的"长模组 = 世界跨局延续"，需要一个新的数据层（`Campaign`），这件事做之前，其余东西都在正确的路上。

---

## 4. 你的需求盘点（重点：找出你可能忘了的）

我把你从 09-14 到现在的所有需求都过了一遍。**下面这张表就是"你忘了也没关系"的兜底。**

### 4.1 ✅ 已完成（23 项）

| # | 你提的 | 状态 |
|---|---|---|
| 1 | 骰子 `NdM(N≥3)` 分布翻倍（3d6 被算成 8d6） | ✅ |
| 2 | 检定请求要能一次来多个（改队列） | ✅ |
| 3 | 目标值文案规则包驱动（d20 显示加值、d100 显示 %） | ✅ |
| 4 | 背包物品按数量消耗 | ✅ |
| 5 | 属性上下限与 NaN 防护 | ✅ |
| 6 | 检定结论不泄漏进正文 | ✅ |
| 7 | `combat.*` 加入 delta 白名单 | ✅ |
| 8 | 编年史 turn 号 | ✅ |
| 9 | 存档版本迁移（`SAVE_VERSION` + `migrateSave`） | ✅ |
| 10 | 槽位补快照 | ✅ |
| 11 | PWA 安装引导 | ✅ |
| 12 | 消息行 `memo`（性能） | ✅ |
| 13 | 地图迷雾失效（AI 模组丢 `map_nodes`） | ✅ |
| 14 | 地名不全 / 点图即瞬移改"意图" / 状态变化无提示 | ✅ |
| 15 | 地图粘鼠标、缩放失效、移动意图漏进正文、世界书与同行者消失、重试清屏 | ✅ |
| 16 | 结档结算 + 关键抉择回溯 | ✅ |
| 17 | 测试沙盒（能测所有操作的环境） | ✅ |
| 18 | 篇幅/时间尺度 + 长模组生成方式 | ✅ |
| 19 | 物品作用 / 怪物数值分开生成 | ✅ |
| 20 | 描述加权扩大到每一次检定 | ✅ |
| 21 | 更新日志（设置入口 + 新版本自动弹 + 未读打点） | ✅ |
| 22 | R42 帮助常驻入口 | ✅ |
| 23 | R34 音频全槽位 + 存储管理 + 顶栏控制坞 | ✅ |
| 24 | 结档只有一个出口（修测试沙盒死亡不结算） | ✅ |
| 25 | 更新检测双路 + 手动检查更新 + 显示当前版本号 | ✅ |

### 4.2 ⬜ **还没做**（这些是真正的"待办"，你可能已经忘了自己在等它们）

| # | 事项 | 来源 | 我的判断 |
|---|---|---|---|
| ~~**A**~~ | ~~你的需求 2 是空的~~ —— **2026-09-16 结案** | 你自己 | ✅ 用户确认：**没有这条需求，已从所有待办里删除**。这条不再是悬案 |
| **B** | **怪物引擎剩余部分**：① 敌对者表从准备页抽成**独立设定页**；② **结档后解锁的怪物图鉴**（通关后才看得到"它到底是什么"） | "怪物引擎还在排队吗"那一轮 | 已做一半（单独生成 + 提示词注入 + 一键投入战斗），剩这两个 |
| **C** | **生图抽成 store 级后台任务 + 全局进度角标** | 之前的待办 | 现在生图必须停在那个页面等结果，切走就看不见。你之前提过"关闭设置后仍能看到结果" |
| **D** | **GM 口吻预设**（老派说书人 / 隐形旁白 / 搭档 / 命运之声）+ `state_delta` 的 `announce` 标记 + 契约版本号 | 你第 5 轮 + 协作方 | 建议合并做（一次改契约，别改两遍） |
| **E** | **长模组接力生成（R14）**："进章时按需展开" | 你的长篇需求 | 已完成"分章骨架"，"进章时再展开"还没做 |
| **F** | **短/长模组的世界延续** + 角色**档案库**（Phase 2 地基） | 你拍板的玩法表 | 需要新数据层 `Campaign`，是后面很多东西的前置 |
| **G** | **带图战报**：关键节点自动生图 + 总开关 + 手动按钮 | 你拍板的玩法表 | 手动生图已有，自动那个没有 |
| **H** | **R30 生涯概览 / 成就 / 高光时刻** | Backlog | "给成长点"的那一块 |
| **I** | **R39 要不要升 `SAVE_VERSION`** | 协作方待裁定 | `checks` 是 messages 内的可选字段，旧版读到会忽略、不会损坏；升版本却要写一条无意义迁移。**我倾向不升** |
| **J** | **锚点判据是不是太宽**（"移步"也算锚点 → 长局几十个） | 协作方待裁定 | 我倾向收紧为"跨章节 / 首次到访"才算。**这条需要你拍一下** |
| **K** | **求死识别面**（`SUICIDE_RE` 11 个词的覆盖面） | 协作方待裁定 | 宁可漏判（让模型去演）也不要误判（把悲壮抉择直接判死）。**保持现状我也认可** |
| **L** | **濒死那一轮要不要强制走"同伴施救 / 自救"引导** | 协作方待裁定 | 我倾向**保持开放**（强制引导会削弱"主角存在感"） |
| **M** | **长期托管要不要迁到 Cloudflare Pages / Netlify** | 协作方建议迁 | 现在挂在 WorkBuddy 云，依赖套餐。**这个得你定** |

### 4.3 关于"需求太多会忘记"这件事

**你不需要记。** 从今天起，**所有"未做"的东西都只以第 4.2 节这张表为准**——它同时进了 `项目台账.md`（第 3.2 节）和 `.workbuddy/memory/MEMORY.md`，接手的人（或下一个我）第一眼就会看到。

**2026-09-16 更新**：原本唯一的短板（A 条，你自己的需求 2 是空的）**已经结案**——用户确认没有这条需求。
现在这张表**完全由我兜住**，你不需要记住任何一条。

**同日用户拍板的三件事**（已写进 `项目台账.md` 3.6）：角色面板 → 拆三个页签（已实现）·
终幕导出 → Markdown/文本（已实现）· 负重 → 完整版（待做）。

---

## 5. 项目整体规划

按"**先完成再完美**"、一次一块、每块做完提醒你测试的节奏排。

### 5.1 我建议的推进顺序

| 批次 | 内容 | 为什么排这里 |
|---|---|---|
| **批 1**（下一步） | **B 怪物引擎收尾**：敌对者独立设定页 + 结档解锁图鉴 | 它是"半成品"，做完这一块怪物这条线就闭环了。用户之前明确问过"还在排队吗"，闭环优先 |
| **批 2** | **性能三连**（P1-1 节流 / P1-2 `useMemo` / P1-3 IDB 单例） | 改动都很小、风险低、收益确定。趁"改得动"的时候做掉，别等长局真卡了再回头治 |
| **批 3** | **C 生图后台任务 + 全局进度角标** | 独立功能，做完体验明显提升（生图不用守着页面） |
| **批 4** | **D GM 口吻预设 + `announce` + 契约版本号**（合并一次改契约） | 契约改动最好一次改完，别拆两批 |
| **批 5** | **E 长模组进章按需展开** | 承接批 1 的长篇骨架，让长篇真正"跑得下去" |
| **批 6** | **F Phase 2 地基**：`Campaign` 世界层 + 角色档案库 | 这是最大的一个，前五批都是它的铺垫。**动手前我会先跟你确认数据模型的取舍** |
| **批 7** | **G 带图战报 + H 生涯概览/成就** | Backlog，锦上添花 |

### 5.2 持续做的事（不占批次）

- **每次改动都跑三门禁**（tsc → test → build），通过后**直接上线**（不问你）；
- 改完**更新台账 + 当日记忆**；
- **每有大改动更新 `协作清单.md`**（项目根 + 桌面副本），因为你说过之后可能还要找协作方。

### 5.3 需要你拍板的三件事（就这三件）

1. **需求 2 是什么？**（4.2 的 A 条）
2. **锚点判据要不要收紧？**（J 条）——我倾向收紧成"跨章节 / 首次到访"才算锚点。
3. **长期托管迁不迁？**（M 条）——现在依赖 WorkBuddy 套餐。

其余的我都能自己判断，不会拿这些来烦你。

---

## 6. 本次报告的变更记录

| 项 | 说明 |
|---|---|
| 新增 | 本文件 `初期报告.md` |
| 更新 | `项目台账.md`（第 3.2 待办重排 + 新增第 3.4 性能小节 + 变更记录 v1.4） |
| 更新 | `.workbuddy/memory/MEMORY.md`（新增性能约定 + 待办指针） |
| 更新 | `.workbuddy/memory/2026-09-15.md`（追加本轮：审计 + 报告 + 规划） |
| 代码 | **本轮无代码改动**（纯审计）。门禁复跑：tsc 通过 / 193 测试全过 / build 通过 |
