# 跑团模拟器 · 项目总览

> 最近更新：2026-09-14（本轮按《交接清单》修完 **P0 全部 8 项** + **P1 四类** + **玩家实测的 4 个 bug**）
> 一句话：**一个以玩家为中心的单人 AI 跑团模拟器**，规则可插拔（COC 7th / DnD 5e / 自建），题材可切换。

---

## 1. 它是什么

单人自用、纯前端、无后端的 PWA。AI 当守密人（GM），你当唯一的玩家。
数据在浏览器本地（localStorage + IndexedDB），不上传任何地方。手机装了就能独立跑，不依赖电脑。

**在线地址**：https://paotuan-sim-79663.app.workbuddy.host/（WorkBuddy 云，依赖平台套餐）
**本机 / 局域网**：`npm run build && npm run preview` → 手机连同一 WiFi 访问 `http://<电脑IP>:4173`
**说明**：项目已取消对外开源与 GitHub 发布计划（用户网络访问不了 GitHub）；`dist/` 仍是通用静态产物，
`base: './'` 相对路径保留，想换任何静态托管都能直接拖上去。

> 详细部署方式见 `README.md` 的「跑起来 / 部署」一节。

---

## 2. 最重要的两条：以玩家为中心（t0）、题材与规则正交

**t0 —— 以玩家为中心**，写在 `MEMORY.md` 的技术约定里：

> **规则退到后台**——只在剧情需要时被唤起，绝不主动冒出来要求玩家操作。
> 前台永远优先回答三件事：**我是谁 / 我要什么 / 我接下来干嘛**。

判断标准很简单：**如果某个改动是让规则更精确、却让玩家多一道操作，那它多半是错的。**

由此派生的硬约定：
- **模组必须给目标**：`goal` / `stakes` / `urgency` 三件套，缺一不可。玩家不知道该干嘛＝守密人失职。
- **战斗轮的回合是给世界用的**：敌人每轮推进一次，玩家在自己窗口内自由描述、不限动作数。

**第二条 —— 题材与规则正交**（2026-09-13 新增的一层）：

- **规则包（Ruleset）** 只管"怎么算"：COC 的 1d100 百分比、DnD 的 1d20+加值、你的自建房规。
- **题材（Genre）** 管"什么味道"：世界观、守密人的写法、生图画风、队友倾向。
- 两者**各自独立**：可以用 COC 的百分比跑东京怪谈，也可以用 DnD 跑校园日常。

内置 6 个题材：经典克苏鲁 / 东京怪谈 / 二次元日常 / 情感线（粉红团）/ 剑与魔法 / 都市异能。
题材可自建，也可由「导入文本 → 整套预设」自动生成。

---

## 3. 架构（五层）

```
表现层   src/ui/*.tsx        React 组件（App / Chat / WorldPanel / CharacterSheet / Preparation / Settings）
编排层   src/orchestrator/   prompt.ts（系统提示词、契约提取、兜底检测）· generate.ts（AI 生成各类卡）
引擎层   src/core/           dice/（骰子）· rulesets/（规则包）· state/（状态机）
数据层   src/ui/store.ts     zustand 单一数据源 + localStorage 持久化
接入层   src/providers/      model.ts（OpenAI 兼容 SSE 流式 + 生图）
```

**核心不变式**：模型只输出"意图"（`state_delta` / `dice_requests`），
真正的数值计算、边界裁剪、状态合并**全部在本地完成**。模型算的数一律不作数。

---

## 4. 文件地图

| 文件 | 职责 |
|---|---|
| `src/core/dice/*` | 骰子表达式解析与掷骰，RNG 可注入（便于单测） |
| `src/core/rulesets/types.ts` | `Ruleset` 接口——换规则只换这个实现 |
| `src/core/rulesets/coc7.ts` | COC 7th：8 属性 + 51 技能 + 数值派生 + `resolveCheck` + `starterSkills` |
| `src/core/rulesets/dnd5e.ts` | DnD 5e 轻量：6 属性 + 15 技能（加值制）+ d20 vs DC |
| `src/core/rulesets/custom.ts` | 自建规则工厂（由「导入文本」或高级编辑器生成） |
| `src/core/genres.ts` | **题材预设**：写法 / 世界观 / 画风 / 队友倾向 / 建议规则包 |
| `src/core/state/gameState.ts` | `GameState` / `applyDeltas`（路径白名单 + 裁剪）/ 战斗轮状态 |
| `src/orchestrator/prompt.ts` | 系统提示词组装、红线、输出契约、泄漏检测与重试话术 |
| `src/orchestrator/generate.ts` | **题材 + 规则双驱动**的生成提示词（角色/队友/世界书/模组/模组包/整套预设）+ 生图提示词 |
| `src/ui/store.ts` | 唯一数据源；角色卡、模组、世界书、编年史、快照、待掷检定队列、状态变化提示、题材、音频设置的持久化；**存档版本迁移 `migrateSave`** |
| `src/ui/preset.ts` | **游玩预设**：应用一份预设（题材+规则+模组+世界书+队友）与导入导出 |
| `src/ui/App.tsx` | 发送流程、流式、回溯 / 重掷、一键掷骰（队列）、音频唤醒、图片水合、状态变化提示、安装引导 |
| `src/ui/Chat.tsx` | 消息渲染（`MessageRow` 已 memo）、NPC 台词卡、骰点卡、战斗轮提示条、待掷检定队列 |
| `src/ui/WorldPanel.tsx` | 当前目标、**地图节点关系图**（迷雾 + 缩放拖拽 + 名字折行 + 地点芯片）、场景立绘、同行者、在地人物、线索、编年史 |
| `src/ui/CharacterSheet.tsx` | 属性 / 技能（降序）/ 数值条 / 背包（弹窗详情）/ 检定抽屉（**不模糊遮挡正文**） |
| `src/ui/Preparation.tsx` | 开团准备：模组 / 角色 / 同行者 / 世界书 四个页签 + 题材与规则选择条 |
| `src/ui/Settings.tsx` | API、生图、排版、音频、存档槽、**安装到设备**、**题材/规则预设、导入文本生成预设** |
| `src/ui/audio.ts` | Web Audio 程序化氛围音 + 判定音效 + IndexedDB 音频文件 |
| `src/pwa.ts` | Service Worker 更新提示 + **「安装到设备」事件捕获**（避免手机卡在旧版） |
| `src/ui/charCard.ts` | 酒馆角色卡 PNG 解析（V2/V3 tEXt chunk） |
| `src/ui/idb.ts` | 通用 IndexedDB 键值存储（图片与音频这类大件走这里） |

---

## 5. 数据存在哪

- **localStorage**：角色卡、模组、世界书、消息、编年史、快照、设置（都是小文本）
- **IndexedDB**：场景立绘、消息小图、地图、用户上传的音频（base64 图片动辄几百 KB，
  塞 localStorage 会超 5MB 配额并静默失败——这就是"场景立绘偶尔消失"的根因，已修）

---

## 6. 已完成清单

**基础**：骰子引擎 · 三套规则（COC 7th / DnD 5e 轻量 / 自建，可切换）· **5 个内置题材（可自建）** ·
状态机与防漂移 · PWA（离线可用 + 更新提示）· 三套主题 · 排版预设

**准备阶段**：角色卡（叙事层 + 数值层 + 示例角色一键套用 + 酒馆卡 PNG 导入）· 模组（含目标三件套、开局地点、地图关系）·
世界书（关键词触发）· 同行者（模组级候选 + 未登场不可入队）· AI 生成以上全部 ·
贴任意文本导入成模组 · **贴剧本/小说/安科 → 自动生成整套游玩预设** · 模组单卡导出（JSON/Markdown）

**游玩**：流式叙事 · NPC 台词分流 · 一键掷骰 · 回溯 / 重掷 · 分层记忆（事件日志 + 摘要 + 近 16-20 轮原文）·
自动摘要压缩 · 战斗轮（轻量回合 + 玩家自由窗口 + 敌人血条）·
**地图节点关系图（带迷雾 + 缩放拖拽，点节点转场）** ·
背包（点击弹窗看详情，武器带伤害）· SAN 疯狂 / 濒死死亡 · 检定记录 ·
**支线表（把网状叙事收敛成有界几条线）**

**表现**：场景立绘 · 动作小图 · 手绘区域图 · BGM（程序化氛围音 + 自配音乐）· 大成功/大失败音效 · 骰点动画

**操作**：全局快捷键（Enter 发送 / ↑ 取回上条 / Esc 关弹层 / Cmd+1·2·3 切面板 / Cmd+, 设置）·
触摸设备自动放大按钮热区

**可靠性**：契约缺失 / 拒绝话术 / 检定泄漏 三类自动重写 · 网络抖动与流式中断自动重试 · 图片走 IndexedDB 防丢图 ·
数据久未备份提醒 · 新手引导

**数据**：多存档槽（含快照）· 存档导入导出（带版本迁移）· 模型参数面板（top_p / 思考模式）

**质量**：**155 个测试** · 提示词红线 + 客户端兜底（stripMeta / dedupeNpcLines / isRefusal / isCheckLeak / 契约重试 / 占位符清洗）

**2026-09-14 修复（P0 / P1 / 实测 bug，全部带回归测试）**

| 症状 | 修复 |
|---|---|
| `NdM(N≥3)` 概率分布算错（3d6 均值 14 而非 10.5） | `enumerate` 改为逐个卷积单骰（原来 `convolve(dist,dist)` 把骰数翻倍） |
| 一轮里要求多个检定只跑第一个 | `pendingCheck` 改成队列 `pendingChecks`，逐个呈现、可逐个掷、可整体忽略 |
| 非 COC 规则包的目标值显示成 `%` | 统一走 `checkTargetText()`：d100 显示"目标值 65%"，d20 显示"加值 +2" |
| 背包不能按数量消耗（子弹永远用不完） | `inventory` 支持 `dec`/`inc`，qty 递减、归零移除；兼容 `inventory.<物品名>` 写法 |
| 属性可填 9999 / 空输入变 NaN | 输入框加 min/max，store 端对非有限数回落默认值 |
| 检定结果泄漏进正文 | 提示词新增【红线二之三】禁止复述结论；扩大 `isCheckLeak`；引擎回灌文本自带约束 |
| `state_delta` 白名单漏 `combat.*` | 与战斗规则自相矛盾的契约补全 |
| 编年史 turn 号折叠后错乱 | `addChronicle` 改用 `max(turn)+1`；读档时顺带修正旧档编号 |
| 存档结构变更后旧档打不开 | 新增 `SAVE_VERSION` + `migrateSave()`（只补不删） |
| 长对话列表卡顿 | `Chat` 的消息行抽成 `memo` 的 `MessageRow`，回调用 ref 稳定身份 |
| 检定面板 backdrop-blur 糊掉正文 | 改为不透明底、宽屏收窄，不再"边看边瞎" |
| 地图名字显示不全 | 节点名折行（最多两行）+ 图下加一排完整名字的地点芯片 |
| **地图迷雾失效、开局就能去所有地点** | 根因：`Preparation` 两处生成没把 `map_nodes` 写进模组 → 永远没有可达关系 → 迷雾被整块关掉。已接住；并且兜底时**按地点顺序串成一条线**，不再"无 links 就取消迷雾" |
| 点地图即传送、想去哪就去哪 | 改成发出**意图**（"我打算前往 X"），守密人可用剧情理由拦下；提示词新增【红线二之四】 |
| 状态变化主页面无提示 | 每轮把数值/背包/线索/人物/支线/地点的变更翻译成人话，弹「状态变化」卡片（9 秒自动收起） |
| 手机端不知道怎么装到主屏 | 新增「安装到设备」：能弹系统安装提示就弹，不能（iOS）就给分平台文字指引 |

---

## 7. 剩余工作

**功能性的剩余工作已经全部做完。** 下面两项是**刻意的设计取舍**，不是"没做完"：

| 事项 | 说明 |
|---|---|
| 移动端真虚拟滚动 | 现在是"只渲染最近 120 条 + 消息行 memo + 加载更早"，几百条以内够用；真虚拟滚动工程量与回归风险都大，暂缓 |
| 战斗轮更细战术层（掩体/距离/状态） | 刻意保持轻量——往细做容易又变回"规则驱动"，违背 t0 原则 |

> ⚠️ 关于「GMD 口吻预设 / 界面随题材切换 / 带图战报 / 时间系统」等清单里的 Phase 4-5 项：
> 这轮**没有做**——用户这轮给的是《交接清单》里的 P0/P1 与实测 bug，改动范围刻意收敛在这份清单内。

## 8. 新对话怎么接手

1. 先读本文件 + `.workbuddy/memory/MEMORY.md`（长期约定）+ 当天的 `.workbuddy/memory/YYYY-MM-DD.md`
2. **改代码前** `npx tsc --noEmit`，**改完** `npm test`（当前 155 个）
3. **大改动完成后**：更新项目根的 **`协作清单.md`**（它随桌面代码包一起交付，不再单独复制一份到桌面），供用户转给另一个协作 AI
4. **提交**：本地 git 提交即可（**没有远程仓库了**，GitHub 计划已取消，`origin` 已移除）
5. **部署（可选）**：
   - 本机/局域网：`npm run build && npm run preview`
   - WorkBuddy 云：`workbuddy_sites_deploy` → `directory=dist/`、`language=static`、
     `entryHtml=index.html`、`appId=wbapp_hyMfuN9HRchWs5vje27m3S`
   - **上线属于「对外动作」，需要用户在当轮明确说要发布**（工具层面会拦一道）
6. 上线后提醒用户测试并等反馈
