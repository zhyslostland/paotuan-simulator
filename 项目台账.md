# 跑团模拟器 · 项目台账

> **给"接手的人"看的第一份文件。** 从零接手时按这个顺序读：
> **① 第 0 节（现在在哪）→ ② 第 1 节（这项目要什么）→ ③ 第 3 节（当前待办）→ ④ 第 2 节（历史）。**
> 需要了解某条具体约定时再翻第 4、5 节。
>
> 协作方（另一个 AI）于 2026-09-15 退出，**2026-09-16 复活**，恢复双 AI 协作
> （协作方不改代码、只出建议；技术细节实现方定；方向性分歧由用户裁决）。
> **云端沟通计划已取消**（用户 2026-09-16 定：**上云失败，协作方打不开**）。
> 沟通回到**桌面文件 + 代码包**：`协作清单.md` 仍是双方工作台，协作方直接在里面追加回复，
> 我按**桌面 `积压需求实现清单.md`（第 2 版）** 的执行序推进。
> 协作记录见 **`协作清单.md`**（已续到 v10），本文件是接手第一文件。

| 项 | 值 |
|---|---|
| 台账版本 | 2026-09-16 · v1.9 |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，**代码基线 = 09-16 第五轮提交**（更新缓存策略根治；`git log --oneline` 第一条即它。前一轮＝负重系统 + 人物卡片 + 濒死冻结 + 完整留档） |
| 质量门禁 | `tsc --noEmit` 通过 · `vitest run` **269 全过**（12 文件）· `vite build` 通过 |
| 版本号 | **语义化，唯一真源 `src/version.ts` 的 `APP_VERSION`**；当前 **v0.2.1**。发版＝改它 + 在 `src/ui/Changelog.tsx` 最前面加同版本条目 |
| 初期报告 | **`初期报告.md`**（2026-09-15 体检报告：性能 / 立意核对 / 需求盘点 / 规划） |
| 在线 | https://paotuan-sim-79663.app.workbuddy.host/ |
| 环境 | **bash 工具可用**（2026-09-16 晚实测 `sed`/`grep`/`tail`/管道都正常）。若再遇 exit 127 则改 PowerShell 工具，命令输出用 `\| Out-File -Encoding utf8 文件` 再读（直接 `>` 会写成 UTF-16）。跑 node 用托管路径 `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\node.exe` 指向 `node_modules` 里的脚本，**不要用 `npx`**；`git commit` 用 `-F 消息文件`（here-string 传中文会静默失败） |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端，数据全在浏览器 localStorage + IndexedDB |

---

## 0. 现在在哪（30 秒版）

一个"以玩家为中心"的单人 AI 跑团应用。**核心不变量只有一条：骰子与状态由本地引擎权威，AI 只写叙事。**
模型只输出结构化 JSON 契约（`state_delta` / `dice_requests` / `location` / `summary_delta` / `npc_lines`），
引擎据此改状态，再回灌一句话告诉模型结果；玩家看到的结果由界面卡片呈现，正文里**不许出现**检定结论。

已经做完的：P0/P1 缺陷全部修完、结档系统、关键抉择回溯、测试沙盒、图片灯箱、背包使用、
连续检定一次全掷、未受训技能按基础值、求死引擎结算、地图软同步、题材预设、地图关系图、
角色卡三页签、终幕导出、更新弹窗死循环热修 + 语义化版本号体系、
**负重系统（v0.2.0）· 点击人物看档案 · 濒死当轮冻结扣血 · 结档「完整留档 · 含真相」**……

正在做/待做：**第 3 节**列的待办（批 1-8 执行序 + 协作方建议的排队项，见 3.2 / 3.7）。

---

## 1. 这项目要什么（用户定下的，不要改方向）

### 1.1 两条北极星
- **沉浸不中断**：长玩不出硬错、不出戏。
- **主角存在感**：每轮都有"选择是我的、成长可见"。

### 1.2 明确不喜欢 / 明确拒绝
- 不喜欢：猎奇致郁、背叛剧情、强队友群像（这是单人体验）、女频套路。
- 拒绝：**免死 / 改命按钮**（用可靠读档替代）；fail-forward（"少死但永久失去"）。
- 拒绝：多人 / 社区 / 商业化 / 战棋网格 / 重度刷数值养成。

### 1.3 已拍板的玩法
| 事项 | 定论 |
|---|---|
| **死亡** | = **结档**（这段故事收束）。不是弹"你死了"，不是读档当没发生。结局正文由守密人写 |
| **主动求死** | **引擎直接结算，不发给模型**（模型有安全对齐，会劝诫或让旁人"刚好"救人） |
| **回溯** | 系统层永远保留；关键决策点标为**锚点**，入口在结档页与常规 UI 两处 |
| **模组分两类**（待做） | 短模组（世界不延续）/ 长模组（世界跨局延续）；角色独立成**档案库** |
| **卡住时的引导** | 只做**剧情内的抓手**（提示词层），不做选项菜单 |
| **带图战报**（待做） | 关键节点自动生图 + 一个总开关 + 手动「生成」按钮 |

### 1.4 与我协作的方式（用户明确要求）
- **不要为了确认而提问**。"你多问一句纯浪费我钱"。能自己判断的就直接做完再汇报。
- **改完直接上线，不要每次问**（部署是既定动作；上线后提醒测试即可）。
- 干完活给**结果 + 结论**，不要只说"已完成"。
- 中文回答。

---

## 2. 历史（做过什么，接手时不必细读）

| 时间 | 内容 |
|---|---|
| 09-13 | 上游《交接清单》定稿：P0 缺陷 8 项、P1 四类、路线图 |
| 09-14 上午 | **P0 8/8**：骰子 `NdM(N≥3)` 分布翻倍（3d6 被算成 8d6）、检定请求改队列、目标值文案规则包驱动、背包按数量消耗、属性上下限与 NaN、检定结论不泄漏正文、`combat.*` 白名单、编年史 turn 号。**P1 6/6**：存档迁移、槽位补快照、PWA 安装引导、消息行 memo、检定面板去模糊 |
| 09-14 上午 | 玩家实测 4 个 bug：地图迷雾失效（AI 模组丢 `map_nodes`）、地名不全、点图即瞬移改"意图"、状态变化无提示 |
| 09-14 | **取消开源/GitHub**（用户网络访问不了）：删 workflow 与 LICENSE，移除 remote；`base: './'` 保留 |
| 09-14 下午 | 协作方 B1–B4：地图粘鼠标/缩放失效、移动意图漏进正文、世界书/同行者消失、重试清屏 |
| 09-14 晚 | **结档结算 + 关键抉择回溯**；协作方 N1–N4/N6 |
| 09-15 上午 | **批三第 1–3 节**：热修 G/F/I/H/B/N5、求死引擎结算、地点软同步、背包核对与「使用」、连续检定一次全掷、图片灯箱、设置页点外关闭+保持挂载 |
| 09-15 下午 | 测试沙盒、篇幅/时间尺度、道具表单独生成、描述加权 |
| 09-15 晚 | 修 6 个 bug、R42 帮助常驻、R34 音频全槽位、结档单一出口、更新检测双路；**体检轮**（`初期报告.md`） |
| **09-16** | **用户实测《货舱里的东西》报 17 条 → 修掉 12 条**（武器误扣 / 无武器检定 / 状态不可见 / 换模组文案 / 敌对生物命名 / 属性齐平 / 技能排序；**新增结局收束机制**；补空间连贯与"不编造玩家"提示词）。协作方复活 |

---

## 3. 当前状态与待办

### 3.1 本轮（2026-09-15 下午）做完的

| # | 用户的需求 | 落实现状 |
|---|---|---|
| **4** | 「测试模组先做，现在测试功能要从头开始太麻烦，给个能测所有操作的环境」 | ✅ 新增 `src/ui/TestSandbox.tsx`。**设置 → 开发者**打开「开发者模式」后，顶栏出现 🧪 入口（设置里也能开）。提供：一键灌入完整测试局（角色+模组+背包+队友候选+世界书+支线+快照锚点+待掷队列+开场）、三套脚本化模组（解谜短篇 / 战斗向 / 长篇多日）、背包塞满各类物品、数值回满/置 1/归零、**直接触发结档**、塞 2 个待掷检定、回到出厂状态。**只写本地、不发请求、不需要 API Key** |
| **5** | 「时间限制权重太高，随便生成就是 15 分钟；要有长模组的生成方式」 | ✅ `Module.scale`（短篇/中篇/长篇）+ 篇幅选择器（准备页模组名下方）。`SCALE_GUIDE` 把时间尺度写死：短篇以**小时**计、中篇以**天**计（3-7 天）、长篇以**周/月**计（如"下次满月还有二十三天"），并明确禁止长篇写"只剩 15 分钟"；长篇还会按**分章**写幕结构、说明章与章之间世界会怎么变，生成 token 从 3072 提到 6144 |
| **1** | 「物品作用能不能跟怪物一样分开生成，减轻模型压力、提高准确性」 | ✅ 新增 `Module.items`（道具表）与 `itemTableSystemPrompt`，准备页模组页签有「道具表」区 + 「生成道具表」按钮（只生成这一件事）。提示词里作为"作用已定，照此演出"注入；**玩家拾取时自动把作用带进背包说明**（`applyModelDeltas` 后处理） |
| **3** | 「描述加权要不要扩大到每一次检定？给个方案」 | ✅ 方案见 3.3，已按方案实现：`src/core/description.ts` 的 `weighDescription()`，**每一次检定都算**，只调难度档位不动数值 |
| **2** | —— | ⬜ **用户这条是空的，等他补内容** |

### 3.1.1 追加一轮（同日晚）：修 bug + 更新日志 + 怪物引擎

| # | 用户报的 | 处理 |
|---|---|---|
| 1 | 背包里普通物品点「使用」没反应 | 根因：`CharacterSheet` 的 `onUseItem` 回调**从来没从 App 传进去**（我上一轮只加了 prop 没接）。已接上 |
| 2 | 数量减了但没效果描述 | 同一根因。现在使用物品会把「（使用：X）」作为玩家意图发给守密人，由它演出结果 |
| 3 | 检定面板怎么又有三个难度？ | **已移除**。难度由这次行动本身（或守密人的要求）决定，不该由玩家宣布"这次是极难" |
| 4 | 描述加权：写两句 +1、写一堆 +0 | 打分规则重写：**以长度为主**（15 字 +1、45 字再 +1），提到场上真实存在的东西再 +1，提到手里没有的武器 −2。并且**改成改目标值**而不是难度档位（见下） |
| 5 | 更新日志没看到在哪（优先） | ✅ 新增 `src/ui/Changelog.tsx`：设置顶部「更新日志」按钮 + 新版本自动弹一次 + 未读时按钮上打点 |
| 6 | 怪物引擎还在排队吗？ | **没排队，已做掉一半**：`Module.monsters` + 单独生成 + 提示词注入 + 「投入战斗」按数值初始化 `combat.foes`。剩下的（独立页 + 结档解锁的图鉴）见 3.2 |

**描述加权为什么从"调难度档位"改成"调目标值"**：难度档需要玩家先选一个难度才有东西可改，
而难度本来就不该由玩家选（等于让玩家决定事情有多难）。玩家自己发起的检定默认"普通"，再降没处降 —— 加权形同虚设。
现在在引擎算出的目标值上加一个封顶的修正量（百分比 +10/+15，d20 +1/+2），**在掷骰之前**加，
所以判定、成功率、结果标签三者一致；面板与结果卡片都会标出"含描述加分 +15"。

### 3.1.2 再追加一轮（同日晚 · 继续推进 R42 + R34）

| 项 | 处理 |
|---|---|
| **R42 帮助常驻入口** | ✅ 把原来只在"首次进入"弹一次的欢迎页抽成 `src/ui/HelpGuide.tsx`（`HelpDialog`，`firstTime` 控制口吻/按钮文案）。内容是重写的，补全了检定怎么算、**描述为什么让检定更容易**、地图与移动、背包、结档与回溯。入口：顶栏 `?` + 设置顶部「帮助」 |
| **R34 音频全槽位** | ✅ `SfxSlot` 扩到 7 个（大成功/大失败/进入战斗/受伤/理智受创/结档/开团），每个都写了程序化兜底音色（战斗是急鼓、受伤是闷击+高频刺、理智受创是失谐下滑+耳鸣、结档是缓慢下行长音、开团是向上两音）。`stopSfx()` + 单例句柄（播新的先掐旧的；上传整首歌时能停）。触发点在 App 的回合结算处（受伤≥2 / 理智≥2 / 战斗开始 / 开团 / 结档）——放这一层才分得清"这一轮真发生了什么"，读档与测试沙盒不会乱响 |
| R34 存储管理 | ✅ 声效槽位补齐后，设置里加了**音频占用表**（总占用 + 单文件大小 + 超 15MB 提醒）。以前超限只写进 console，玩家看到的是"点了上传没反应" |
| R34 音频控制坞 | ✅ 顶栏 🔊（静音/恢复，含 BGM 与氛围音）与 ⏹（掐掉正在播的音效） |

### 3.1.3 再追加一轮（同日晚 · 两个用户报的问题）

| 用户报的 | 根因 | 处理 |
|---|---|---|
| **测试沙盒里死亡没有触发结档结算** | 引擎只负责写下 `ending = { kind, text: '' }`（正文永远是空），**"去要一段结局正文"这件事只写在两条路上**：守密人回合结束、主动求死。测试沙盒（以及任何别的来源）把生命清零时，只会静默写下一个空 ending —— 屏幕上什么都不发生 | 在 App 里建了**结档的唯一出口**：`useEffect` 盯 `ending.at`，只要出现"没有正文的 ending"就去要一段（用 at 去重）。`sendToGm` 里原来那段重复逻辑删掉了。顺带补 4 条测试把"两次归零才结档、正文留空"这个契约钉死 |
| **打开链接发现没有更新** | `index.html` 被 Service Worker precache 住，而 SW 什么时候去取新的 `sw.js` 是浏览器说了算——内嵌浏览器（微信里打开）与部分手机上经常不触发 | **两条独立的路**：① 原有 SW 提示；② **版本号探测**（`src/update.ts` + 构建时产出的 `dist/version.json`）：启动 / 回到前台 / 每 5 分钟各 `no-store` 拉一次，id 与运行的 `__BUILD_ID__` 不一致就弹更新横幅。横幅多了「稍后」。设置里加了**「检查更新」按钮 + 当前版本号**（以后"你打开的是哪个版本"一眼可查）。`version.json` 刻意不在 precache 的 globPatterns 里，否则探到的永远是旧 id |

### 3.1.4 再追加一轮（09-16 第四轮 · 积压清单「第一优先」四项全做完）

来源：**桌面 `积压需求实现清单.md`（第 2 版）** 的「第一优先」四项。用户 09-16 定：云端沟通取消，按这个清单推进。

| # | 需求 | 落地做法 | 关键文件 |
|---|---|---|---|
| 1 | **负重系统（完整版）** | 物品加 `weight`（缺省 1）；`Ruleset` 新增可选 `carryCapacity(characteristics)`（COC `(力量+体型)/2`、DnD `20+力量×3`）；超重两档：**d100 目标值 −10 / −20，d20 加值 −1 / −2**，**在掷骰前**生效。概况页与背包页都显示当前负重 / 上限并画进度条，检定面板预览同步带上惩罚。`SAVE_VERSION` **3 → 4**，迁移给缺 `weight` 的旧物品补 1；规则包没给上限则整套不启用（不误伤自定义规则） | `src/core/encumbrance.ts`（新）、`rulesets/{types,coc7,dnd5e}.ts`、`ui/store.ts`、`ui/CharacterSheet.tsx` |
| 2 | **点击在地人物弹卡片** | 旁挂 `GameState.npcNotes`（`role`/`note`/`met`），**不动 `npcsAlive` 结构、不升 SAVE_VERSION**。卡片三处查表：在场档案 → 模组预设人物 → 同行队友；临时登场的路人也有档案（提示词要求模型顺手写进 `npcNotes`）。名字宽松匹配（`nameKey()` 剥「老/小/阿」前缀与「先生/女士」后缀）。**不显示动机与秘密**（防剧透） | `src/ui/NpcCard.tsx`（新）、`ui/WorldPanel.tsx`、`orchestrator/prompt.ts` |
| 3 | **濒死当轮冻结扣血** | `VitalDef.isLife` + `isLifeVital()` 标出主生命条；`applyDeltas` 里生命见底或已 `dying` 时**拒绝继续下降的 delta**（`DYING_FREEZE_REASON`），玩家侧看到「还有一口气」（绿色）。同时一次性注入 `DYING_NOTE`：要求守密人**在叙境内**给施救/自救路径，不列菜单、不替玩家定成败。下一轮恢复结算 | `core/state/gameState.ts`、`ui/store.ts`、`orchestrator/prompt.ts` |
| 4 | **导出加「含真相」的完整留档** | `buildJourneyMarkdown(input, { withTruth })` + `buildArchiveMarkdown()`；真相放在**末尾 `<details>` 折叠块**（`守密人真相（剧透：展开前请确认这一局已经结束）`）。结档页三张卡：分享版 / 完整旅程 / **完整留档 · 含真相**。前两份仍不含真相（有断言钉死） | `src/ui/exportGame.ts`、`ui/EndingScreen.tsx` |

测试 232 → **269**（新增 `tests/encumbrance.test.ts`、`tests/npcCard.test.ts`，`state/prompt/export` 各补用例）。

### 3.1.5 再追加一轮（09-16 第五轮 · 更新问题根治）

用户实测「更新问题还没有修好」。**先诊断，结论是服务端没问题**：线上 `version.json` 已是新值、
`sw.js` 与**根路径**都返回新包。问题在客户端加载层。

| 层 | 原因 | 改法 |
|---|---|---|
| ① 打包 | `index.html` 在 precache 里（CacheFirst）→ 旧 SW 永远用旧 HTML 响应导航，它引的 JS 也是旧 hash | `globPatterns` **去掉 `html`**；导航改 `NetworkFirst`（`cacheName: 'html'`，超时 4 秒，离线退缓存） |
| ② 顺带的坑 | 修 ① 时若设了 `navigateFallback`，workbox 会注册一条**优先级更高**的 `NavigationRoute` 去 precache 找 index.html —— 而它已不在 precache，那条路由**必然失败**（比用旧版更糟） | **不设 `navigateFallback`**；`version.json` 单列一条 `NetworkOnly` |
| ③ 交互 | 横幅要玩家主动点，"没看到 / 点了没反应 / 不知道要点"都会让人卡在旧包 | 探测到新版本后**自动更新一次**（2.5 秒后执行，`localStorage` 记时间戳 + **10 分钟冷却**，不会死循环）；设置里常驻「**卡在旧版本？强制重载**」（清缓存 + 带戳重载，存档不受影响） |

`src/update.ts` 把 `purgeEverything()` / `navigateFresh()` 拆出来复用，新增 `forceReload()`。
**新增两条易重犯的教训，已写进 `MEMORY.md`**：html 不进 precache；设 `navigateFallback` 前必须先确认目标在 precache 里。

线上核对：`version.json` = `0.2.1` / `mu40kaia`；根路径引 `assets/index-C-bu-sn2.js`；
`sw.js` 含 `NetworkFirst`、**不含** `NavigationRoute`、precache 不含 `index.html`。

> 另：这轮中途为下一项（R38 怪物引擎）建过一个脚手架 `src/core/names.ts`，
> 用户指示"不做新功能、只收尾"，**已删除**，仓库回到干净状态。

### 3.2 待办（我排的顺序，**2026-09-15 晚按初期报告重排**）

> 完整盘点见 **`初期报告.md` 第 4.2 节**（含每条的来源与我的判断）。这里是执行序。

1. **批 1 · R38 怪物引擎收尾**：把敌对者表从准备页抽成**独立设定页**，并做**结档后解锁的怪物图鉴**（玩家通关后才看得到"它到底是什么"）。**这块是半成品，优先闭环。**（09-16 进展：测试沙盒三套模组已补 `monsters`、提示词已要求 `combat.foes.name` 必填，剩余的是独立页与图鉴）
2. **批 2 · 性能三连**（都很小、风险低、收益确定）：
   - `snapshots` 与 `applyModelDeltas` 的 `saveJson` 补 800ms 节流（快照最重、每回合写 2-3 次）；
   - `WorldPanel` 给 `knownText`（编年史+线索+摘要全量 join，**随局长线性增长**）/ `mapNodes` / `anchors` 加 `useMemo`；
   - `idb.ts` 的 `openDb()` 缓存成模块级单例（现在每次读写都重开连接，音效连播时尤其明显）。
3. **批 3 · R40 剩余**：生图抽成 store 级后台任务 + 全局进度角标（关闭页面后仍能看到结果）。
4. **批 4 · GM 口吻预设**（老派说书人 / 隐形旁白 / 搭档 / 命运之声）+ `state_delta` 的 `announce` 标记 + 契约版本号 —— 合并做，一次改契约。（09-16 已改过一次契约：加了 `ending`，可以一起收进"契约版本号"）
5. **批 5 · 长模组接力生成（R14）**：已做到"分章骨架"，"进章时按需展开"还没做。
6. **批 6 · Phase 2 地基**：短/长模组的世界延续（`Campaign` 世界层）+ 角色**档案库**。**动手前先跟用户确认数据模型取舍。**
7. **批 7 · Backlog**：R30 生涯概览/成就（给成长点）、带图战报（关键节点自动生图 + 总开关）。
8. **批 8 · 09-16 用户新提的 5 条**（用户 09-16 用弹窗拍板后的现状）：
   - ✅ **游戏内角色面板 → 拆三个页签**（概况 / 技能 / 背包）。**已实现**（`CharacterSheet.tsx` 的 `SHEET_TABS`）；
   - ✅ **终幕导出分享 → Markdown / 文本**，分「结算分享」与「整体流程分享」。**已实现**（`src/ui/exportGame.ts`）；
   - ✅ **负重系统 → 用户选了「完整版：超重影响检定与移动」**。**已实现**（见 3.1.4，含 `SAVE_VERSION` 3→4）；
   - ✅ **点击「在地人物」弹卡片**（记载他们的信息）→ **已实现**（`npcNotes` 旁挂 + `NpcCard.tsx`，不动 `npcsAlive`）；
   - ⬜ **完整伤口/流血系统**（引擎侧 DOT）—— 从"包扎成功还持续掉血"这条延伸出来。

**需要用户拍板**（2026-09-16 弹窗收口后）：
- ✅ **已拍板**：角色面板 → 拆三个页签（已实现）· 终幕导出 → Markdown/文本（已实现）· 负重 → 完整版（待做）·
  **「需求 2」→ 用户确认没有这条需求，已从所有待办中删除**。
- ⬜ **仍待定**：① 锚点判据要不要收紧（我倾向"跨章节/首次到访"才算）；② 长期托管迁不迁 Cloudflare/Netlify。

### 3.3 描述加权的方案（这是我对需求 3 的答复）

**结论：可以扩大到每一次检定，但必须满足三条限制。**

- **只调难度档位，不动数值。** 难度（普通/困难/极难）是规则包本身就有的概念，
  用它不会破坏规则权威；目标值仍由规则包算出，引擎仍是唯一权威，**模型看不到这个调整**。
- **幅度封顶一档。** 最多降一档、升一档。否则"写一段小说就能必过"，规则会失去意义。
- **判据只看两件事，写得长不等于有分：**
  ① 有没有提到**场上真实存在的东西**（在场人物 / 去过的地方 / 已获线索 / 随身物品）→ +1；
  ② 有没有写明**具体做法**（用、撬、贴着、慢慢、先…再…）→ +1。
  另：提到需要武器但手里没有 → −1（并升一档）。
- **空描述不罚。** 想直接点技能掷骰是玩家玩法的一部分，逼人写作文是错的。
- **结果必须对玩家可见**：检定面板实时显示"描述加分 +2 → 难度降到普通（撬棍；写明了怎么做）"。
  偷偷改数值是最坏的做法。
- **放在本地、不问模型**：模型打分要一次额外请求、几百毫秒延迟，还会随机波动
  （同一句描述两次结果不同，玩家会困惑）。这里是确定性规则，同一句永远同一结果。

代码在 `src/core/description.ts`（纯函数、可单测），测试 `tests/description.test.ts` 9 条。

---

### 3.4 性能体检结论（2026-09-15 晚）

**结论：没有会当场卡住的问题；`version.json` 每 5 分钟轮询开销可忽略（49 字节 × 288 次/天 ≈ 14KB，不动 token、不动 React 状态、已有 30 秒节流兜底），不用改。**

长局会慢慢变重的三处（即上面批 2）：`saveJson` 全量序列化无节流（快照最重，60 份上限 × 每回合 2-3 写）、`WorldPanel` 无 `useMemo` 全量重算（`knownText` 随局长线性增长）、IDB 连接未复用。

**已做对的（别改坏）**：`MessageRow` 的 `memo` + props 全原始值、`scheduleSaveMessages` 800ms 节流、图片音频一律走 IndexedDB、更新探测 30 秒闸门、`version.json` 排除出 precache、运行时依赖只有 5 个（无 lodash/moment 类重库）。

**未做但可控**：主包 700KB（717,829 字节）无代码分割。`Preparation`(88KB) / `Settings`(66KB) / `TestSandbox`(22KB) 可按需 lazy；PWA 有缓存，故优先级低于批 2。

### 3.5 2026-09-16 轮：用户实测 17 条 → 修掉 12 条

用户跑《货舱里的东西》（**测试沙盒的 `SCRIPTED_MODULES.combat`**，项目里没有内置模组常量）后一次性报了 17 条。逐条定位根因后分两类处理：

**修掉的（12 条）**

| 用户报的 | 根因 | 修法 |
|---|---|---|
| 开枪把手枪消耗掉 | 提示词只教了"用掉消耗品按数量扣"，**没禁止扣武器**，模型就近扣了枪 | 引擎拒绝 `inventory dec` 命中 `weapon`；`remove` 若 reason 含"使用语义"也拒（真丢枪放行） |
| 枪没了还触发手枪检定 | 技能按钮在"无可用武器"时**只给灰字提示、仍可点** | 按钮改禁用；`CheckDialog` 的掷骰按钮同样禁用 + 红框说明 |
| 流血状态界面不显示 | `CharacterSheet` 状态区是**四个写死的 flag 判断** | 改成动态渲染全部中文 `flags`，重状态用告警色 |
| 换模组后上轮文案没清 | `setModule` 只在 `patch.opening !== undefined` 时同步开场白 | 条件扩到 `opening/goal/stakes/urgency`（`openingText` 正好拼这四项） |
| 敌对生物没有命名 | `combat.foes` 的 `name` 必填（引擎会拒），但提示词没强调；测试模组**根本没有 `monsters`** | 提示词补"name 必填且要是能指认的叫法"；三套脚本化模组补 `monsters` |
| 属性太平均 | `defaultCharacteristics` 直接返回 `default` → **COC 八个 50** | 改确定性散列：COC 落 30-70、DnD 落 4-16 |
| 一堆 1% 技能 | `COC_SKILLS` 确有一批 `base:1`；「全部技能」按原顺序平铺 | 按 `base` 降序；`< 5%` 收进二级折叠 |
| 上救生艇没触发结局 | 模组 `endings` **只是给模型看的文本**，引擎里没有任何路径能让"达成目标"结档 | **新增结局收束**：契约加可选 `ending{kind,reason}`，模型可声明 success/failure/grey，引擎写空正文 ending 走既有唯一出口；**死亡与疯狂优先** |
| AI 空间概念混乱 | 提示词没有任何位置一致性要求 | 新增 `SPACE_RULES`：移动必须走 `location` |
| 给玩家加"右腿旧伤" | 提示词没有"不得替玩家编造设定" | 同上节：不得添加身体特征/病史/社会关系；需要时**让 NPC 去问** |
| 包扎后仍持续掉血 | 流血是 AI 自由发挥，引擎没有伤口系统 | 提示词明确：状态进 `flags`、**扣血要克制**、止血分三种依据、解除后必须清 flag |
| 简单包扎没触发检定 | ——**这条用户判断正确，不该触发** | 已核对其他路径（急救/医学技能的解析与 `CheckDialog`）均正常 |

**排队（5 条，见 3.2 批 8）**：游戏内角色面板、NPC 卡片、终幕导出分享、负重系统、完整伤口系统。

**关于问题 14 的明确答复**：现在的项目**不能**判断"有医疗物品 / 有医疗技能就能止住血"——流血完全由模型自由发挥，引擎没有伤口层。本轮只做到"看得见" + "提示词约束"，完整系统在批 8。

### 3.6 2026-09-16 第二轮：用户弹窗拍板 → 实现两条

用户用弹窗一次定了三件事，另一件是撤销：

| 问题 | 用户的选择 | 现状 |
|---|---|---|
| 角色面板怎么组织 | **拆成三个页签**（概况 / 技能 / 背包） | ✅ **已实现**。同一份角色卡换呈现方式，不新开一套 UI（避免两套各说各话、判定逻辑维护两遍） |
| 终幕导出分享 | **Markdown / 文本**（不做长图） | ✅ **已实现**。新增 `src/ui/exportGame.ts`：**结算分享**（结局 + 这一局的账，短）与**整体流程分享**（角色、目标三件套、编年史、关键抉择、线索、结局）；结档页两组「复制 / 存为文件」 |
| 负重系统 | **完整版：超重影响检定与移动** | ⬜ 待做（要动物品模型与规则包，需升 `SAVE_VERSION`） |
| 「需求 2」 | **没有，删除** | ✅ 已从台账 / 初期报告 / MEMORY / 协作清单**全部删除** |

**导出这一条的两个设计决定**：
1. **两份都不含 `module.truth`**。导出物是会被转发的，守密人的内部真相绝不能跟着出去（与"剧情保密"一致）。已写成断言钉住。
2. 技能按规则包量纲渲染（COC `60%` / DnD `+5`），属性写成中文名且**不带百分号**（属性不是成功率）。

### 3.7 2026-09-16 第三轮：协作方第 5 版回复 → 热修四连 + 版本号体系

用户贴了 `协作方回复-第5版-20260916.md`（协作方这次是**静态审查 + 对线上站点抓包实测**）。
四件事按协作方给的执行序做完了，外加一件用户当场提的。

**① P0 更新弹窗死循环**（用户当时被卡住，最优先）
- 协作方**抓到了线上证据**：`version.json` 是新值、线上 JS 包里也含新 id → **构建/注入/版本号三样都对**，问题在加载端。
- 根因：旧 SW 把 `index.html` 与 JS 钉在 precache；浏览器没去取新 `sw.js` 时**没有 waiting SW** →
  `updateSW(true)` 空操作 → `reload()` 落回旧缓存 → 新版永远进不来 → version.json 永远"更新" → **死循环**。
- 修法（`src/update.ts` 的 `applyUpdate`）：注销 SW + 清空 Cache Storage + `location.replace` 带 `_v` 时间戳；
  **删掉 `setTimeout(reload, 800)` —— `reload()` 正是被旧 SW 接管的那个动作，是死循环的一半**。
  SW 会在下次加载时由 `main.tsx → pwa` 自动重注册，离线能力自动恢复，代价为零。

**② 武器 `remove` 判据：黑名单 → 失去理由白名单**
- 旧判据（写了使用词才拦）有两个口子：**模型可以直接省掉 reason**（等于没拦，用户报的"枪没了"没堵死）；
  「开火时炸膛」含"开火"被**误伤**（其实是枪该坏的合法剧情）。
- 改成 `LOSS_RE` 白名单：命中＝删，**未命中（含空 reason）＝拒**；`dec` 命中武器**保持全拒**。
- 踩坑：白名单里只写 `送人` 会漏掉 `送给老霍华德防身`，改成单字 `送`。**改完用脚本穷举 13 个正反例确认零 mismatch 才落盘。**

**③ 被引擎拦下的变更要给玩家看见**
- `report.rejected` 以前只在引擎内部记账，玩家只觉得"东西怎么没扣"。
- 现在 `store.applyModelDeltas` 把 inventory 相关的 rejected 翻成人话，塞进同一条「状态变化」提示；
  `StateChangeLine.tone` 新增 `'warn'`（金色）。

**④ R32 描述加权：可行性为主 + 题材极性**（协作方发现的"做了但做偏"，它 grep 确认 `rationalityBias` 全库未实现）
- 用户拍板原话是"描述越**科学、合理、可行**，成功率越高"，但旧实现**以长度为主**：
  `>=15字 +1` / `>=45字 +1` → "我开门"硬凑 45 字拿满分，而"我用撬棍卡进门缝、肩膀顶住慢慢加力"短一点反而低分。
  **奖励了字数，没奖励可行性。**
- 新判据（各 +1，仍封顶 ±2）：用上背包里的真工具 / 针对环境的具体特征 / 说清步骤顺序 / 顾及风险 / 提到场上真实存在的人地线索。
  **长度降为次要**：只在 `score === 0 && len >= 40` 时补 1 分（不把负分抬起来）。
- **题材极性**：`RationalityBias = 'reward' | 'punish' | 'neutral'`（类型在 `description.ts`），
  `Genre` 新增 `rationalityBias`。克苏鲁/东京怪谈 = `punish`（科学解题 −1），
  剑与魔法/都市异能/二次元日常 = `reward`，情感线 = `neutral`。

**⑤ 锚点收敛**（协作方 3.2）：位置类**只在首次到访时记**，事件类（掷骰/战斗/新线索/新支线/受伤/理智受创）**全记**；
另加连续同 label 折叠。注意 `snapshots` 是 **Record 不是数组**。

**⑥ 版本号统一成语义化**（用户本轮当场要求："你的更新日志和版本号，前面用 v0.0.0 这种格式"）
- 以前**三套版本各说各话**：构建时间戳（`mu3m5cif`）、日志日期串（`2026-09-15d`）、设置里显示的乱码。
- 新建 **`src/version.ts` = 全项目唯一版本真源**：`APP_VERSION = '0.1.0'`、`versionLabel()`、`compareVersions()`。
- `version.json` 同时写 `version`（语义化，更新检测比对它）与 `id`（构建号，只用于排查）。
- **`checkForUpdate` 只认 `version`**；老部署只有 `id` 时**一律回 `unknown`**（构建号随机，比对它只会永远"有新版本"）。
- `Changelog` 的 `id` → `version`，`LATEST_CHANGELOG_VERSION = APP_VERSION`
  → **"设置说有新版本"与"日志有新条目"永远同步**；弹窗按 `v0.1.0` + 日期展示。
- 数字怎么走：修订＝修 bug/调文案；次版本＝加功能；主版本＝存档不兼容。

**协作方其余结论（已落进本节，不再是悬案）**：
- **3.1 不升 `SAVE_VERSION`**，判据一句话：**"可选新增字段不升；替换/重命名现有结构才升。"**
- **3.8 结局收束放契约顶层是对的**（别塞 `state_delta`）；补两条加固：`ending` 幂等、有 reason 无 kind 按 `other` 收但别丢 reason。
- **3.9 角色卡页签化**同意；唯一要求：三页签共用同一套 `weaponMissing` / `resolveCheckTarget`。
- **3.10 `npcNotes` 旁挂映射**（不升版本、`npcsAlive` 仍是唯一真源）· **3.12 导出两按钮 + 真相折叠** ·
  **3.4 濒死当轮冻结扣血** · **3.5 薄 `Campaign` 边界搬运**（动手前先确认数据模型）—— 均排队。
- **3.3 求死识别**只认第一人称自尽，**不加** `牺牲/赴死/同归于尽`（会把悲壮抉择误判成死）。现状已符合。
- **3.6 内容尺度**维持现有边界。

**部署踩坑（记下来）**：`language=node` 走 vite 会跑在默认 5173，而 sites 探的是 **3000** → 超时；
`startCmd` 里的 `$PORT` **不展开**（报 `option --port value is missing`）。
最终用 **`language=static` + `installCmd="npm install && npm run build"`** 走 Python 托管成功。
另外 `vite.config.ts` 的 **`server.allowedHosts` / `preview.allowedHosts: true` 必须保留**（云端反代域名会被 Vite Host 校验 403）。

## 4. 不可违背的技术约定（改动前先读，编号接 `MEMORY.md`）

1. **骰子与状态由本地引擎权威，AI 只写叙事**。模型只输出 `state_delta` / `dice_requests` / `location` / `summary_delta` / `npc_lines` 的 JSON 契约。
2. **规则包可插拔**：换规则只换 `Ruleset`。内置 `coc7`（1d100）+ `dnd5e`（1d20），另支持自定义规则包。检定 UI 必须规则包驱动。
3. **题材与规则正交**：题材管"味道"，规则管"怎么算"。生成提示词一律写成 `(genre, ruleset)` 双驱动，绝不写死"1920 年代/克苏鲁"。
4. **版权三层**：机制→内嵌代码；数据→外挂；规则书原文与模组设定文本→**绝不进系统**。
5. **剧情保密**：真相绝不直接告知玩家；界面 GM 内部资料默认遮罩。
6. **双保险**：提示词引导 + 客户端兜底（`stripMeta` / `dedupeNpcLines` / `isRefusal` + 重试）。
7. **`{...默认值, ...存档}` 的默认值里不能放"会随用户输入变化的字段"**（会盖掉用户设置）。
8. **不要同时用 `w-full` 和 `w-XX`**；flex 行内占满用 `flex-1 min-w-0`。
9. **可编辑列表不用数组下标当 key**；输入提交改在"失焦/回车"。
10. `core` 包必须纯函数、无 IO、可单测；`ui/store.ts` 是唯一数据源。
11. **以玩家为中心，规则退到后台**：规则只在剧情需要时被唤起，绝不主动冒出来要求玩家操作。
12. **模组必须给目标**：`goal` + `stakes` + `urgency` 三件套，且**不能剧透**（只能写玩家开局就知道的）。
13. **战斗轮＝回合给世界用**：敌人每轮推进一次，玩家在自己窗口内自由描述。绝不写"轮到你才能动"。
14. **音频不内置任何版权文件**：Web Audio 程序化生成，用户可上传自己的（存 IndexedDB）。
15. **图片/大件一律走 IndexedDB，不塞 localStorage**（会超 5MB 配额、保存静默失败）。
16. **`gameState.vitals` 必须覆盖当前规则包的全部 `vitalDefs`**：一律走 `reconcileVitals()`。
17. **换规则包只给 `Ruleset.starterSkills`**，绝不能把整张技能表倒进角色卡。
18. **存档结构一变就要 `SAVE_VERSION` +1 并补 `migrateSave()`**（只补不删）。
19. **状态变化必须给玩家可见反馈**（`describeChanges` → 「状态变化」卡片）。
20. **骰子概率分布枚举禁止 `convolve(dist, dist)`**，要逐个卷积单骰分布。
21. **检定请求是队列，不是单个**；**目标值显示一律走 `checkTargetText()`**。
22. **物品按数量消耗**（`dec` + `amount`），整件丢下才用 `remove`。
23. **所有重试/重写路径都不清屏**；只缺契约时改为单独补 JSON，不重写正文。
24. **所有可拖动组件统一用 `endDrag()` 收尾**；**"是否拖过"不能用 `dragRef` 判断**（要用独立的 `suppressClick`）。
25. **缩放类控件不能放在可拖动容器内部**；画布类组件的 wheel **不能无条件 `preventDefault`**（现在要 Ctrl/⌘）。
26. **战役数据要么全进存档、要么全不进**，统一走 `buildSave()`；**读档只补不删**（世界书取并集）。
27. **清理 `fromModule` 类数据只属于「换模组」**（`clearModuleDerived()`），开新团与读档都不清。
28. **求死由引擎说了算**（`forceEnding`），不发给模型。
29. **地点软同步**：地图上没有的地点补成孤立节点，不阻断叙事。
30. **物品要么有要么没有**：先核对背包（提示词红线二之五 + 按钮标「无可用武器」）；使用消耗品时**本地先扣 1** 再发意图。
31. **图片一律可点开放大**（`ImageLightbox`）。
32. **技能名必须规范化**（`canonicalSkillName`）；未受训走规则包基础值，**绝不能兜底成 50%**。
33. **结档机制**：死亡 = 结档；濒死给一轮缓冲；`ending.text` 留空 = 等守密人写结局的信号；回溯必须 `clearEnding()`。
34. **结档只有一个出口**（App 里盯 `ending.at` 的 `useEffect`）：引擎只写空正文的 ending，
    **任何来源**置上它都会去要一段结局。曾经把这段逻辑只写在"守密人回合结束"和"主动求死"两条路上，
    结果测试沙盒把生命清零时屏幕上什么都不发生。加新的结档来源时不要再自己去调 `requestEnding`。
35. **更新检测必须双路**：SW 的更新检测不可靠（内嵌浏览器/部分手机不触发），
    所以另有**版本号探测**（`src/update.ts` 拉 `version.json`，与构建注入的 `__BUILD_ID__` 比对）。
    `version.json` **绝不能进 precache**（`globPatterns` 里只有 js/css/html/svg/woff2，保持这样）。
    改完记得让用户点「有新版本 · 点击立即更新」——不然他看到的还是旧的。
36. **武器不按数量消耗**（09-16）。`inventory dec` 命中 `kind === 'weapon'` 一律拒绝；
    `remove` 只有 reason 写着"使用语义"时才拒（真丢枪/缴械要放行）。
    弹药是独立物品。**别再让模型因为"开了一枪"把枪扣掉**——用户实测过，枪没了之后
    系统还在给他触发手枪检定，整局都崩了。
37. **需要武器才能做的检定，没有武器就不给掷**（09-16）。技能值 ≠ 手里有东西。
    `CharacterSheet` 的技能按钮与 `CheckDialog` 的掷骰按钮都要在 `weaponMissing` 时禁用，
    不能只给个灰字提示还让人点。
38. **负面状态必须进 `flags`，界面必须显示**（09-16）。`CharacterSheet` 的状态区
    **动态渲染全部中文 flags**（曾经只有四个写死的判断，"流血"在界面上一片安静）。
    新增重状态记得加进 `SEVERE_FLAG_TONE` 拿告警色。
39. **结局收束是契约的一部分**（09-16）。模型可在 JSON 里写 `ending{kind,reason}`（success/failure/grey），
    引擎据此 `forceEnding(kind, reason)` 写**空正文** ending、走既有唯一出口要正文。
    **死亡与疯狂优先**：引擎已判 death/insanity 时，不接受模型降级成 success。
    提示词里必须保留"不确定就不要写"这条——误判会提前结束一局。
40. **`defaultCharacteristics` 必须是确定性的**（09-16）。它同时用于"新建角色"和"缺省回退"，
    每次返回不同值会让界面上的数字自己跳。现在按 `(rulesetId, key)` 散列生成偏移，
    让属性有起伏但不随机。
41. **改提示词要跟着补断言**（09-16）。`tests/prompt.test.ts` 里有一组"用户踩过的坑"断言
    （武器不是消耗品 / 敌人 name 必填 / 空间与位置 / 不编造玩家 / 负面状态进 flags / 宣告结局）。
    提示词删一行不会报错，但游戏会立刻变回原样——所以每条都要有断言钉住。

---

## 5. 文件地图（接手时最常翻的）

| 文件 | 干什么 |
|---|---|
| `src/core/state/gameState.ts` | 状态机：`applyDeltas` / `detectStatusEvents` / `createInitialState` |
| `src/core/rulesets/*.ts` | 规则包（coc7 / dnd5e / 自定义） |
| `src/core/description.ts` | **描述加权**（纯函数） |
| `src/core/dice.ts` | 骰子与概率分布枚举 |
| `src/orchestrator/prompt.ts` | 守密人系统提示词（红线一~二之五、状态规则、战斗规则、契约格式） |
| `src/orchestrator/generate.ts` | 模型生成：角色/队友/模组/道具表/世界书/模组包/预设/结局 |
| `src/ui/store.ts` | **唯一数据源**（zustand）。存档、快照、结档、背包、技能解析都在这 |
| `src/ui/App.tsx` | 主流程：发消息、流式、重试、契约解析、结档、求死 |
| `src/ui/WorldPanel.tsx` | 世界面板：目标、地图（迷雾+缩放+软同步）、编年史、关键抉择 |
| `src/ui/CharacterSheet.tsx` | 角色卡 + 检定面板（描述加权在这里显示）+ 背包 |
| `src/ui/EndingScreen.tsx` | 结档页 + `AnchorList`（关键抉择列表，世界面板复用）+ 导出入口 |
| `src/ui/exportGame.ts` | **导出这一局**（纯函数）：结算分享 / 整体流程分享两份 Markdown，**不含真相** |
| `src/ui/TestSandbox.tsx` | **测试沙盒**（三套脚本化模组，含地图/道具表/敌对者） |
| `src/ui/Preparation.tsx` | 准备页：角色 / 模组（含篇幅、道具表）/ 世界书 / 同行者 |
| `tests/*.ts` | 10 个测试文件，**221 条**（`prompt.test.ts` 是"用户踩过的坑"断言，`export.test.ts` 钉住"导出不剧透"） |

---

## 6. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-16 | 台账 v1.9 | **更新问题根治（v0.2.1）**：用户实测"更新问题还没修好"。诊断先排除服务端（线上 version.json / sw.js / 根路径都是新包），定位到客户端加载层——① `index.html` 在 precache 里（CacheFirst）导致旧 SW 永远用旧 HTML 响应导航 → `globPatterns` 去掉 html + 导航改 `NetworkFirst`；② **不设 `navigateFallback`**（设了会让 workbox 注册一条去 precache 找 index.html 的 NavigationRoute，而它已不在那儿，路由必然失败）；③ 探测到新版本后**自动更新一次**（10 分钟冷却，不死循环）+ 设置里常驻「强制重载」。`version.json` 走 `NetworkOnly`。测试 269 不变（本次是缓存配置 + UI 兜底，无新增纯函数）。已上线，线上核对一致。中途建的 `src/core/names.ts` 脚手架按用户"只收尾"指示**已删除** |
| 2026-09-16 | 台账 v1.8 | **按桌面 `积压需求实现清单.md`（第 2 版）第一优先四项全做完 → v0.2.0**：① **负重系统**（`src/core/encumbrance.ts`，物品 `weight` + 规则包 `carryCapacity` + 超重 −10/−20（d100）与 −1/−2（d20），概况/背包/检定面板三处可见，`SAVE_VERSION` 3→4 迁移补 1）；② **点击人物看档案**（旁挂 `npcNotes` + `NpcCard.tsx` 三处查表 + 宽松名字匹配，不剧透动机与秘密）；③ **濒死当轮冻结扣血**（`VitalDef.isLife` + `DYING_FREEZE_REASON` 拒绝 delta + 一次性 `DYING_NOTE` 在叙境内给生机）；④ **结档「完整留档 · 含真相」**（`buildArchiveMarkdown`，真相放末尾 `<details>`，分享版仍不含）。测试 232→**269**（12 文件）。**云端沟通取消**（协作方打不开），回到桌面文件 + 代码包。已上线 |
| 2026-09-16 | 台账 v1.7 | **协作方第 5 版回复 → 热修四连 + 版本号体系**：① **P0 更新弹窗死循环**（`applyUpdate` 改硬重置：注销 SW + 清缓存 + 带戳 `location.replace`，删掉作为死循环一半的 `reload()`）；② **武器 `remove` 判据改「失去理由白名单」**（空 reason 一律拒，「炸膛」不再误伤）+ 被拒时给玩家 toast；③ **R32 描述加权改「可行性为主 + 题材极性」**（`rationalityBias` 落到 `Genre`，克苏鲁=punish / 奇幻异能日常=reward）；④ **锚点收敛**（位置只在首次到访记、同 label 折叠）；⑤ **版本号统一成语义化**（新增 `src/version.ts`，当前 **v0.1.0**；`version.json` 带 `version`，更新检测只认它）。测试 221→**232**。已上线。协作方其余结论（3.1/3.8/3.9/3.10/3.12 等）已落进 3.7，不再是悬案 |
| 2026-09-16 | 台账 v1.6 | **用户弹窗拍板 → 实现两条**：① **角色卡拆成三个页签**（概况 / 技能 / 背包，同一份数据换呈现）；② **终幕导出**（新增 `exportGame.ts`，Markdown 分「结算分享」与「整体流程分享」，**两份都不含真相**）。用户另定：负重走**完整版**（待做）、**「需求 2」确认不存在、已全部删除**。测试 212→**221**（10 文件）。已上线 |
| 2026-09-16 | 台账 v1.5 | **用户实测 17 条 → 修掉 12 条**：武器误扣（引擎拒绝 dec + 有文字陷阱的 remove）、无武器仍可检定（按钮禁用）、状态不可见（动态渲染 flags）、换模组文案不清（同步条件扩到 goal/stakes/urgency）、敌对生物无命名（提示词 + 测试模组补 monsters）、属性齐平（确定性散列）、技能排序与低值折叠；**新增结局收束机制**（契约 `ending`）；提示词新增 `SPACE_RULES`（空间连贯 + 不编造玩家）、`STATUS_RULES` 加「状态与伤口」。测试 193→**212**。已上线。**协作方复活，恢复双 AI 协作** |
| 2026-09-15 | 台账 v1.4 | **体检轮（无代码改动）**：新增 `初期报告.md`（性能体检 / 立意核对 / 需求盘点 / 规划）；台账新增 3.4 性能小节、重排 3.2 待办为批 1-7。门禁复跑：tsc 通过 · 193 测试全过 · build 通过。基线记为 `65e7ba5` |
| 2026-09-15 | 台账 v1.3 | 修：结档只有一个出口（测试沙盒死亡不结算）；新：更新检测双路（`version.json` 版本号探测 + 自动/手动检查更新、设置里显示当前版本号）。测试 189→193，已上线 |
| 2026-09-15 | 台账 v1.2 | R42 帮助常驻入口（`HelpGuide.tsx`，内容重写补全）；R34 音频全槽位（7 个槽位 + 程序化兜底音色 + `stopSfx` + 顶栏音频控制坞 + 占用表与超限提醒）。测试 189 不变（UI 为主），已上线 |
| 2026-09-15 | 台账 v1.1 | 修 6 个 bug/问题：背包使用无效、使用无效果描述、移除玩家难度选项、描述加权打分重写并改为调目标值；新增内置更新日志（设置入口 + 新版本自动弹 + 未读打点）；新增敌对者表（单独生成 + 提示词注入 + 一键投入战斗）。测试 190→189（描述加权用例重写），已上线 |
| 2026-09-15 | 台账 v1 | 建立本台账（协作方退出，改由实现方独立推进）。本轮落地：测试沙盒、篇幅/时间尺度与长篇生成、道具表单独生成、描述加权（每一次检定）。测试 181→190，已上线 |
| 2026-09-15 | 清单 v6 | 批三第 1–3 节落地（历史记录见 `协作清单.md`） |
