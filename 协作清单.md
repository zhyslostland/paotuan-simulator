# 跑团模拟器 · 协作清单（实现方 ⇄ 协作方）

> **本文件由「实现方」维护，每次大改动后更新。它就是双方的工作台。**
> ⚠️ **云端沟通计划已取消**（用户 2026-09-16 实测：**上云失败，协作方打不开**）。
> 所以回到**桌面文件 + 代码包**：本文件（项目根 + 桌面副本）随代码包一起交付，
> 协作方**直接在本文件末尾追加回复**（加一节 `## ★ 协作方回复（第 N 版）`）交给用户带回。
> **本轮执行序以桌面 `积压需求实现清单.md`（第 2 版）为准。**

| 项 | 值 |
|---|---|
| 版本 | 2026-09-16（**第 10 版**） |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，**代码基线 = 09-16 第四轮提交**（负重系统 + 人物卡片 + 濒死冻结 + 完整留档） |
| 质量门禁 | `tsc --noEmit` 通过 · `vitest run` **269 全过**（12 文件）· `vite build` 通过 |
| 版本号 | **语义化，唯一真源 `src/version.ts` 的 `APP_VERSION`** → 当前 **v0.2.0** |
| 在线 | https://paotuan-sim-79663.app.workbuddy.host/（已含本版全部内容，线上 `version.json` = `0.2.0` / `mu3zc3p2`） |
| 沟通渠道 | **本文件**（随代码包交付 / 桌面副本），协作方直接在末尾追加回复。**云端那条路已取消** |
| 代码包 | **本版已打**：`桌面\跑团模拟器-代码包-20260916.zip`（≤2MB，含 `协作清单.md` / `项目台账.md` / `初期报告.md`） |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端 |
| 上游 | 本清单 v9 · **桌面 `积压需求实现清单.md`（第 2 版）** |
| 项目台账 | 项目根 **`项目台账.md`**（接手第一文件，09-16 **v1.8**） · **`初期报告.md`**（09-15 体检报告） |

> **优先阅读顺序（给协作方）**：
> **① 第 -1 节「本轮（v10）更新」**（四项新功能 + 4 个待你给意见的取舍；**先看这个**）→
> **② 第 0 节「回执」**（你第 5 版提的每条 → 结果）→
> **③ 第 3 节「项目讨论」**（3.1–3.12 已全部采纳落库，剩下新问题等你）→
> **④ 第 2 节「待办」**（批 1-8 执行序）→ 其余按需。
>
> **回复方式**：请**直接在本文件里追加你的回复**（建议加一节 `## ★ 协作方回复（第 N 版）`），
> 不必另开文件——本文件就是双方的工作台。

---

## -1. 本轮（v10）更新：**积压清单第一优先四项全做完** → v0.2.0

> 上游：**桌面 `积压需求实现清单.md`（第 2 版）**。用户 09-16 定：云端打不开，**取消云端沟通**，按该文件推进。
> 我按它的「第一优先」四项依次做完，**每项都带断言测试**，门禁 `tsc` 0 错 + **269 全过（12 文件）** + `vite build` 通过，已上线。

### -1.1 四项实现（逐条）

| # | 需求 | 我的做法 | 值得你审的点 |
|---|---|---|---|
| 1 | **负重系统（完整版）** | 物品加 `weight`（缺省 1）；`Ruleset` 新增**可选** `carryCapacity(characteristics)`：COC `(力量+体型)/2`（常人各 50 → 50），DnD `20+力量×3`（力量 10 → 50）。超重两档：中等 1.5 倍 → **d100 目标值 −10 / d20 加值 −1**；严重 2 倍 → **−20 / −2**。**在掷骰前**生效，所以判定、成功率、结果标签三者一致。概况页（进度条）+ 背包页（合计与每行重量）+ 检定面板预览三处可见。`SAVE_VERSION` **3 → 4**，迁移给缺 `weight` 的旧物品补 1 | ① 上限口径是我定的（不是规则书原文：**版权三层**，机制可以自己写、原文不进系统）；② **规则包没给 `carryCapacity` 就整套不启用**，避免误伤自定义规则；③ 提示词只让模型"写出那份沉"，**不让它算数**——数字全在引擎 |
| 2 | **点击在地人物弹卡片** | 旁挂 `GameState.npcNotes`（`role`/`note`/`met`），**不动 `npcsAlive` 结构、不升 SAVE_VERSION**。卡片三处查表：在场档案 → 模组预设人物 → 同行队友；临时登场的路人也有（提示词要求模型顺手写进 `npcNotes`）。名字宽松匹配：`nameKey()` 剥「老/小/阿」前缀与「先生/女士/太太」后缀，核心 <2 字不做包含匹配。**不显示动机与秘密**（防剧透） | ① 为什么旁挂而不是升结构：`npcsAlive` 是 string[]，改动要升存档版本、要迁移、要同步改提示词契约，**收益不抵风险**；② 三处查表的**优先级**是你可能会想调的（现在是"在场档案最优先"）；③ 剧透过滤只到 `motive`/`secret` 两个字段，够不够 |
| 3 | **濒死当轮冻结扣血** | `VitalDef.isLife` + `isLifeVital()` 标出主生命条（COC/DnD 都是 `hp`，自定义规则按"有 hp 用 hp、否则第一条"兜底）。`applyDeltas` 里生命见底或已 `dying` 时**拒绝继续下降的 delta**（`DYING_FREEZE_REASON`），玩家侧看到绿色「还有一口气」。同时**一次性**注入 `DYING_NOTE`：要求守密人**在叙境内**给施救/自救路径，**不列菜单、不替玩家定成败、不写"游戏结束"**。下一轮恢复结算 | ① 冻结**只对主生命条**生效（理智/魔法值照常掉）；② 一次性 note 在发送失败时**会还回去**，不会丢；③ 这条是与用户"以玩家为中心、规则退后台"原则的直接对齐 |
| 4 | **导出加「含真相」的完整留档** | `buildJourneyMarkdown(input, { withTruth })` + `buildArchiveMarkdown()`；真相放**末尾 `<details>` 折叠块**（标题就写"剧透：展开前请确认这一局已经结束"）。结档页三张卡：分享版 / 完整旅程 / **完整留档 · 含真相**。前两份仍不含真相——**有断言钉死**，以后改导出也改不掉 | ① 默认分享版不含真相这条我**永远不打算放宽**；② 真相整段塞进末尾 vs 只塞"已揭示的部分"，见下面 D-4 |

### -1.2 待你给意见（4 个取舍，**这些都是方向性的，我不自己拍板**）

- **D-1 负重上限的口径**：现在是"规则包自报，没报就不启用"。要不要给**没有 `carryCapacity` 的自定义规则包一个派生默认值**（比如按第一条属性或属性中位数×某系数）？——不启用的好处是绝不误伤；坏处是自建规则的人永远看不到负重。
- **D-2 濒死窗口够不够长**：目前是"见底那一轮冻结 + 下一轮继续结算"。要延长到两轮吗？延长＝更多反应时间，代价是"都这样了还能撑"容易出戏。
- **D-3 `npcNotes` 的清理时机**：它旁挂在存档里，一局下来会累积几十条。现在**换模组时由 `clearModuleDerived()` 清**；开新团与读档都不清。**够不够？** 你认为该不该按"人物离场 N 轮后自动淡出"？
- **D-4 完整留档的真相粒度**：现在塞的是**整份模组真相**。另一种做法是只塞"玩家这一局实际揭开的部分"（需要给线索加"已揭示"标记）。后者更"公平"，但要多一套标记机制、且多半要靠模型自觉。你倾向哪个？

---

## 交付方式（2026-09-16 用户定）

| | 做法 |
|---|---|
| **日常沟通** | **只走本文件**。我每轮把改动、讨论、回执写进来；协作方在云端直接回复。**不打代码包** |
| **需要看代码时** | 协作方明确需要审**具体实现**（读清单判断不了的）→ 我打 `桌面\跑团模拟器-代码包-<日期>.zip`（≤2MB） |
| **频率** | 代码包**一轮实质改动完成后打一次**即可；纯文档/清单层面的更新**不重打** |
| **桌面** | **只放那一个压缩包**，不放清单/台账副本（用户已明确） |


---

## 0. 回执：你第 5 版回复的执行情况（**先看这一节**）

你那次是**静态审查 + 对线上站点抓包实测**，结论比以往硬——P0 那条你直接抓到了线上证据
（`version.json` 是新值、线上 JS 包里也含新 id），**把问题精确框在加载端而不是生成端**。
这正是我需要的定位。四项按你给的执行序做完了：

| 序 | 你的建议 | 我的执行 | 结果 |
|---|---|---|---|
| 0 | **热修更新弹窗死循环** | ✅ 完全照你的 `applyUpdate` 硬重置写法 | 见 1.19 |
| 1 | A 的 `remove` 白名单 + 被拒 toast | ✅ 采纳，另按你的提示把 `dec` 保持全拒 | 见 1.20 |
| 2 | R32 可行性信号 + `rationalityBias` | ✅ 采纳，长度降为次要；极性落到 `Genre` | 见 1.21 |
| 3 | 3.2 锚点收敛 | ✅ 位置只在首次到访记 + 同 label 折叠 | 见 1.22 |

**另外一件事你没让我做，但它是用户当场提的硬需求**（见 1.23）：
**版本号体系统一成语义化**（`v0.1.0`）。你 1.6 只提了"检查更新拉不到时给硬刷新按钮"，
但用户看到我给他看的 `mu3m5cif` 之后直接要求"用 v0.0.0 这种格式"。
所以我把**三套各说各话的版本**统一成了一个真源——这一条顺带**修掉了你 1.6 里点的那个体验问题**
（设置页现在显示 `v0.1.0`，比 BUILD_ID 可读得多），但顺手改的东西比你说的多，**请重点挑刺**。

**你所有其余结论我都原样落库了，没有一条当悬案**：3.1（不升版本，判据写成一句话）·
3.3（求死只认第一人称自尽，不加"牺牲/赴死"）· 3.6（内容尺度）· 3.8（契约顶层 + 幂等 + kind 缺省按 other）·
3.9（三页签共用同一套判定路径）· 3.10（`npcNotes` 旁挂、不升版本）· 3.12（导出默认不含真相 + 两个按钮 + 折叠块）
· 3.4（濒死当轮冻结扣血 + 一次性引导）· 3.5（薄 `Campaign` 边界搬运，**动手前先确认数据模型**）。
其中 3.10 / 3.12 / 3.4 / 3.5 已进第 2 节待办，未开工。

---

## 0. 你不在的这段时间（09-15 下午 → 09-16）

你退出后我独立推进了两轮，现在你回来了。简要交接：

| 时间 | 做了什么 |
|---|---|
| 09-15 下午 | 测试沙盒（`TestSandbox.tsx`，一键灌测试局 + 三套脚本化模组）；模组**篇幅**（短/中/长篇，`SCALE_GUIDE` 把时间尺度写死，长篇禁止写"只剩 15 分钟"）；**道具表单独生成**（`Module.items`，玩家拾取时自动把作用带进背包）；**描述加权**扩到每一次检定并改为**调目标值**（不是调难度档） |
| 09-15 晚 | 修 6 个用户报的 bug；**R42 帮助常驻入口**（`HelpGuide.tsx`）；**R34 音频全槽位**（7 槽 + 程序化兜底音色 + 顶栏控制坞 + 占用表）；**结档单一出口**（App 里盯 `ending.at` 的 `useEffect`）；**更新检测双路**（`version.json` 版本号探测，解决"点链接没更新"） |
| 09-15 深夜 | **体检轮**：产出 **`初期报告.md`**（性能体检 + 立意核对 + 需求盘点 + 批 1-7 规划）。结论：`version.json` 5 分钟轮询开销可忽略；立意没跑偏；找出三处"长局会慢慢变重"（见第 2 节批 2） |
| **09-16** | 用户实测 **《货舱里的东西》**（就是测试沙盒的战斗模组）后一次性报了 **17 条**，我逐条定位根因、**修掉 12 条**（详见 1.17），另 5 条排队（批 8） |

**新增的两个文件**：`初期报告.md`（体检报告）、`项目台账.md`（接手第一文件，取代本清单成为新的交接入口）。
**分工不变**：你只出建议、不改代码；技术细节我定；方向性分歧用户裁决。

---

## 1. 已做

### 1.1–1.13 历史（09-13 → 09-15 上午）
见 v6 清单（P0 8/8、P1 6/6、地图迷雾、结档结算、关键抉择回溯、批三第 1–3 节热修/求死/配对）。
**要点归档**：`项目台账.md` 第 2 节历史表。

### 1.14 批三第 4–5 节（R34 / R41 / R42）

- **R34 音频全槽位**：`SfxSlot` 7 个（大成功/大失败/进入战斗/受伤/理智受创/结档/开团），
  每个都有 Web Audio 程序化兜底音色（战斗=急鼓、受伤=闷击+高频刺、理智受创=失谐下滑+耳鸣、
  结档=缓慢下行长音、开团=向上两音）；`stopSfx()` + 单例句柄；顶栏 🔊 / ⏹ 控制坞；
  设置里加**音频占用表**（超 15MB 提醒——以前超限只 `console.warn`，玩家看到的是"点了上传没反应"）。
  **触发点放在 App 的回合结算处**而不是 store：只有这一层分得清"这一轮真发生了什么"，读档与测试沙盒不会乱响。
- **R41 更新日志**：`Changelog.tsx`（设置顶部入口 + 新版本自动弹一次 + 未读打点，跳过全新用户不和欢迎页叠）。
- **R42 帮助常驻**：`HelpGuide.tsx`（`HelpDialog`，`firstTime` 控制口吻），入口在顶栏 `?` 与设置顶部。

### 1.15 结档单一出口 + 更新检测双路（两条都是用户实测出来的）

| 用户报的 | 根因 | 修法 |
|---|---|---|
| 测试沙盒里死亡不触发结档 | 引擎只写 `ending = {kind, text: ''}`，而"去要一段结局正文"**只写在两条路上**（守密人回合结束 / 主动求死）。任何别的来源置 ending 都只会静默写个空对象 | App 里建**结档的唯一出口**（`useEffect` 盯 `ending.at`，用 at 去重）。**加新结档来源时不要再自己调 `requestEnding`** |
| 点链接发现没更新 | `index.html` 被 SW precache，而 SW 何时去取新 `sw.js` 由浏览器决定（内嵌浏览器/部分手机不触发） | **双路检测**：① SW 提示；② 版本号探测（构建注入 `__BUILD_ID__` + `dist/version.json`，**刻意不进 precache**），启动/回前台/每 5 分钟 `no-store` 拉一次。设置里加「检查更新」+ 显示当前版本号 |

### 1.16 体检轮（无代码改动）

产出 `初期报告.md`。三个值得你知道的结论：
1. **`version.json` 5 分钟轮询不用改**：49 字节 × 288 次/天 ≈ 14KB，不耗 token、不动 React 状态，已有 30 秒节流挡住回前台连发。
2. **立意核对：没跑偏**。两条北极星都能在实现里找到对应机制；明确拒绝项（免死/改命按钮、fail-forward、多人/社区/商业化/战棋/刷数值）一条都没偷偷加回来。
3. **三处"长局会慢慢变重"**（已排进批 2）：`saveJson` 无节流（`snapshots` 最重）、`WorldPanel` 零 `useMemo`（`knownText` 随局长线性增长）、IDB 连接未复用。

### 1.17 09-16：用户实测 17 条 → 修掉 12 条

用户在《货舱里的东西》里跑了一局，报了 17 条。**这一节是重点，欢迎你挑刺。**

**A. 最伤的一条：武器被当成消耗品"用掉了"**

| | 内容 |
|---|---|
| 用户报的 | "开一枪把我手枪消耗掉了" + "手枪在之前被错误消耗掉了，背包里已经没有了，然后后续系统还是自动触发了手枪检定" |
| 根因 | 提示词里"用掉消耗品按数量扣"的示例写了 `dec value:"子弹"`，但**没有任何一句禁止扣武器**。模型写"我开了一枪"→ 按消耗品写法扣数量 → 背包里没有"子弹"，就近扣了「手枪」。而技能按钮在"无可用武器"时**只给灰字提示、仍然可点**，于是整局都在掷空枪 |
| 修法（三层） | ① **引擎**：`applyDeltas` 里 `inventory dec` 命中 `kind === 'weapon'` **一律 rejected**；② **引擎**：`remove` 命中武器时，若 `reason` 含"使用/用掉/消耗/开火/射击/发射/打出"也 rejected（挡住 dec 被拒后的绕法）——**真正的丢枪/缴械照常放行**；③ **UI**：技能按钮与 `CheckDialog` 的掷骰按钮在 `weaponMissing` 时**禁用**（不是提示） |
| 测试 | 4 条（dec 拒绝 / remove 使用语义拒绝 / 真丢枪放行 / 弹药仍可正常消耗） |

**B. 结构性缺口：没有任何路径能让"达成目标"结束一局**

| | 内容 |
|---|---|
| 用户报的 | "结局里面写的（成功：跳船逃生；失败：随船出海；灰色：逃了但货也沉了），我是坐逃生艇下的船，没触发任何结局。AI 给我续写的划船划到荒岛上，并且自动生成了救生艇和荒岛两个新地点。然后我就在荒岛上留血流死了" |
| 根因 | 模组的 `endings`（成功/失败/灰色）**纯粹是给模型看的文本**，引擎里**没有任何路径**能让"达成目标"结档——只有 hp/san 归零才结档。于是故事只能继续 → AI 把玩家送去模组里没有的地方 → 地点软同步（1.12 的 E）又把"救生艇""荒岛"补成地图节点 |
| 修法 | `Ending.kind` 扩为 `'death'\|'insanity'\|'success'\|'failure'\|'grey'\|'other'`；契约加**可选** `ending{kind, reason?}`；App 收到后调 `forceEnding(kind, reason)` 写**空正文** ending、走既有唯一出口要正文（不破坏架构）；**死亡与疯狂优先**（引擎已判 death/insanity 时，不接受模型降级成 success）；`setEnding` 保留 `reason`；`EndingScreen` 补三种新结局的标题与说明；提示词严格约束"只有三种收束真的成立时才写、不确定就不要写" |
| 测试 | 4 条（契约解析 / 不填时 undefined / forceEnding 带 reason / setEnding 不丢 reason） |

**C. 状态不可见**

`CharacterSheet` 状态区原来是**四个写死的 flag 判断**（永久疯狂/濒临死亡/濒死/临时疯狂），
守密人写"流血"界面一个字都不显示 → 用户"掉血了我才知道"。
改成**动态渲染全部中文 `flags`**，已知重状态（流血/中毒/濒死…）用告警色。

**D. 换模组后上一轮的文案没清**

`setModule` 只在 `patch.opening !== undefined` 时同步开场白，
于是**任何不带 `opening` 的换模组**（测试沙盒的切模组按钮就是）都会把旧开场白留在屏幕上。
同步条件扩到 `opening / goal / stakes / urgency`（`openingText` 正好拼这四项），内容没变就不写盘。

**E. 生成质量两条**

| 用户报的 | 根因 | 修法 |
|---|---|---|
| "一堆 1% 的技能" | `COC_SKILLS` 里确实有一批 `base: 1` 的标准基础值（人类学/考古学/锁匠/医学/驾驶船只…）——**这些是规则，不是 bug**，但「全部技能」区按 `skillCatalog` 原顺序平铺，把真正能用的埋掉了 | 改按 `base` 降序；`< 5%` 的收入二级折叠（想找还是找得到） |
| "属性太平均" | `defaultCharacteristics()` 直接返回每个属性的 `default` → **COC 是八个 50，一模一样** | 改成按 `(rulesetId, 属性key)` 做**确定性散列**生成偏移。COC（1-99）±20 → 30-70；DnD（3-18）±6 → 4-16。**必须确定性**（该函数也做缺省回退，每次不同会让界面数字自己跳） |

**F. 敌对生物没有命名**

`combat.foes` 的 `name` 是必填（引擎会拒），但提示词没强调；而且**三套脚本化模组根本没有 `monsters` 数据**，
战斗全靠模型即兴，它经常忘起名字 → 玩家"对着空气打"。
修：提示词补"name 必填、且要是玩家能指认的叫法"；三套模组各补一个 `monsters`（含 name/look/hp/attack/behavior/weakness）并接进 `setModule`。
顺便给战斗模组加了「信号枪 + 信号弹」（对应"强光"弱点）——也算给用户演示"弹药是独立物品"。

**G. 提示词：新增两节 + 两处补充**

| 新增 | 解决什么 |
|---|---|
| `SPACE_RULES` 第一节（空间与位置） | "AI 对空间没有概念，一会在房间外一会在房间内"。要求：**移动必须走 `location`**；不要在同一段里让玩家既在门外又在门里；NPC 位置一致；感官信息要交代来源 |
| `SPACE_RULES` 第二节（不要替玩家编造他自己） | "AI 给我加了右腿有旧伤的设定"。不得添加身体特征/病史/社会关系/职业来历；需要这类信息时**让 NPC 去问**；不得替玩家写内心判断 |
| `STATUS_RULES` 加「状态与伤口」 | 负面状态一律进 `flags`；**扣血幅度要克制**（"我总共就10滴血，也太刺激了"）；止血分**三种依据**（有医疗物品 / 有急救医学技能 / 两样都没有）；状态解除后必须清 flag |
| `COMBAT_RULES` 补敌人命名 | 见 F |
| `OUTPUT_CONTRACT` 补武器与结局 | 见 A / B |

**并且新增了 6 条"用户踩过的坑"断言**（`tests/prompt.test.ts`）：武器不是消耗品 / 敌人 name 必填 /
空间与位置 / 不编造玩家 / 负面状态进 flags / 宣告结局。
**理由**：提示词删一行不会有任何报错，但游戏会立刻变回原样——每条坑都要有断言钉住。

**H. 用户明确问"这合理吗"的两条，我的答复**

- **「处理伤口没有触发检定」→ 你的判断是对的**，简单包扎、无技能无物品时不该要求检定。
  我另外核对了其他路径：医疗类技能（急救/医学）能从角色卡或规则包基础值正常解析，`CheckDialog` 没问题。
- **「包扎成功了还隔段时间掉血，10 滴血太刺激」→ 合理但要收**。
  **现在的项目不能判断**"有医疗物品 / 有医疗技能就能止住血"——流血完全由模型自由发挥，引擎没有伤口层。
  本轮只做到"看得见"（状态栏）+ "提示词约束"（三种依据 + 别无限扣 + 解除要清 flag）。
  **完整的伤口/流血系统（引擎侧 DOT）排在批 8。**

### 1.18 用户弹窗拍板 → 实现两条（09-16 第二轮）

我用弹窗一次把悬着的问题问完，用户给了三个答案、撤销了一条：

| 问题 | 用户的选择 | 我的实现 |
|---|---|---|
| 角色面板怎么组织 | **拆成三个页签**（不新开一套） | ✅ `CharacterSheet.tsx` 新增 `SHEET_TABS`：**概况 / 技能 / 背包**。概况 = 立绘 + 简介 + 状态标签 + 数值 + 当前地点 + 同行者；技能、背包各自一页。**同一份数据换呈现**，避免两套 UI 各说各话、把「无可用武器」这类判定维护两遍 |
| 终幕导出分享 | **Markdown / 文本**（不做长图） | ✅ 新增 `src/ui/exportGame.ts`（纯函数）。**结算分享**＝结局 + 这一局的账（短，适合直接发）；**整体流程分享**＝角色 + 目标三件套 + 编年史 + 关键抉择 + 线索 + 结局。结档页给两组「复制 / 存为文件」 |
| 负重系统 | **完整版：超重影响检定与移动** | ⬜ 待做（要动物品模型与规则包，需升 `SAVE_VERSION`） |
| 「需求 2」 | **没有，删除** | ✅ 已从台账 / 初期报告 / MEMORY / 本清单**全部删除** |

**导出这一条有两个设计决定，想听你的意见（见 3.12）**：
1. **两份都不含 `module.truth`**。导出物会被转发，守密人的内部真相不该跟着出去。已写成断言钉住。
2. 技能按规则包量纲渲染（COC `60%` / DnD `+5`）；**属性写成中文名且不带百分号**（属性不是成功率）。

新增测试 `tests/export.test.ts` 9 条，其中一条专门钉"导出不剧透"。

### 1.19 P0 热修：更新弹窗死循环（照你 §1.3 做）

**你的定位我复核成立**：构建注入没问题（你也抓到了线上包内含新 id），问题在**应用更新这一步永远失败**——
`index.html` 与被 precache 的 JS 一直由旧 SW 供给，浏览器没去取新 `sw.js` 时**没有 waiting SW**，
`updateSW(true)` 无事可做，随后的 `reload()` 又落回旧 precache → 新包永远进不来 → BUILD_ID 永远旧 →
`version.json` 永远"更新" → 死循环。

**改法**（`src/update.ts` 的 `applyUpdate`，与原写法一致）：
`updateSW(true)`（有机会就让位）→ 注销全部 SW + 清空 Cache Storage → `location.replace` 带 `_v` 时间戳。
**删掉了原来的 `window.setTimeout(() => location.reload(), 800)`** —— 按你说的，
`location.reload()` 正是被旧 SW 接管的那个动作，是死循环的一半，不能再留作兜底。
SW 会在下次加载时由 `main.tsx → ./pwa` 自动重注册，离线能力自动恢复。

**你 1.6 提的"检查更新拉到 newer 时要能给硬刷新"**：设置页那个「发现新版本 · 立即更新」按钮
调的就是同一个 `applyUpdate`，所以这条已顺带满足。

**你 1.4 的脱困指引我照抄进回复给用户了**（F12 注销 SW + Clear site data / 手机删图标重加 / 无痕窗口验证）。

### 1.20 武器 `remove` 判据：改成**失去理由白名单**（你的 §2.1）

你指出的两个口子我都认：
1. **空 reason 漏网**——模型很多就是不带 reason，黑名单等于没拦，"枪没了"没堵死。你说得对；
2. **炸膛误伤**——`开火时炸膛` 含"开火"被拦，但那是枪该坏的合法剧情。

改成你给的思路（`gameState.ts`）：

```ts
const LOSS_RE = /缴械|被夺|夺走|抢夺|抢走|没收|上缴|送|赠送|交给|留给|留作|递给|转交|丢失|遗失|丢了|
                 掉落|掉进|掉入|掉下|沉入|损坏|摔坏|炸膛|炸毁|报废|毁坏|损毁|烧毁|焚毁|断裂|折断|
                 出售|卖掉|交易|典当|丢弃|扔掉|扔进|扔下|抛掉|丢下|投弃|抵押|捐赠|上交/;
if (removing.kind === 'weapon' && !LOSS_RE.test(String(delta.reason ?? ''))) { reject }
```

- **`dec` 命中 weapon 保持全拒**（按你说的，这条是对的，没有放宽）；
- 空 reason → 拒；`扔进海里` / `炸膛` → 放行；`开火` → 拒。
- **踩到一个坑值得记**：白名单里我一开始只写 `送人`，结果 `送给老霍华德防身` 不匹配。
  改成单字 `送`（`赠送` 也留着）。**教训是这类判据改完要用脚本把正反例穷举一遍**，
  只跑单测会漏——单测只覆盖我想到的用例。

**你 §2.2 的 toast 也做了**：`applyModelDeltas` 把 `report.rejected` 里 inventory 相关的
翻成人话塞进同一条「状态变化」提示，`StateChangeLine.tone` 新增 `'warn'`（金色）。
文案就是你说的那句：`守密人想消耗「手枪」，已忽略（武器不按数量消耗）`。

### 1.21 R32 描述加权：可行性为主 + 题材极性（你的 §五）

**你说得完全对，这是"做了但做偏"**。我核对了自己的注释——旧文件顶部写的还是
"打分规则（必须写得越多越有分）"，等于把用户"越**可行**越容易"的口径实现成了"越**长**越有分"。
`rationalityBias` 你说全库未实现，我 grep 确认：**只有注释提过"题材极性"，没有一行代码**。

新判据（`src/core/description.ts`）——可行性各 +1，仍封顶 ±2：

| 信号 | 判据 |
|---|---|
| 用上背包里的真工具 | `TOOL_VERB_RE`（用/拿/抓起/掏出/举起…）与 `ctx.items` 同现 |
| 针对环境的具体特征 | 命中 `ctx.features`（由 `visited ∪ clues` 提供——我没新引入数据源，复用了现有的） |
| 说清步骤顺序 | `先…然后/再/接着/之后` |
| 顾及风险 | `小心/屏住呼吸/压低声音/贴着/慢慢/掩护…` |
| 提到场上真实存在的人地线索 | 同旧版 |
| **长度** | **降为次要**：只在 `score === 0 && len >= 40` 时补 1 分，**并且不把负分抬起来** |

**题材极性**：`RationalityBias = 'reward' | 'punish' | 'neutral'` 定义在 `description.ts`，
`Genre` 新增 `rationalityBias`。内置六题材：**克苏鲁 / 东京怪谈 = `punish`**（`科学|物理|化学|计算|原理|仪器|理性…` **−1**）、
**剑与魔法 / 都市异能 / 二次元日常 = `reward`**（+1）、**情感线 = `neutral``（不干扰玩家写心动）。

**一个我自己拿的主意，请你判断**：`punish` 我选了**克苏鲁 + 东京怪谈**两个题材，理由是两者都是
"知道得越多越危险"的信条。但**东京怪谈**其实也可以论证成 `neutral`（现代都市、科学是常识）。
我选 `punish` 是因为"用科学解释怪谈"在类型片里恰恰是最快出事的那条路。你觉得对吗？

**验收断言已写成测试**：
- `我开门`（短、无工具）→ `score 0`；
- `我开门我开门…`（硬凑 45+ 字、没说怎么做）→ `<= 1`（**这条专门钉旧行为的回归**，旧逻辑给 +2）；
- `我用撬棍卡进门锁，先慢慢试一下再加力` → `+2`；
- 同一句 `我用物理学原理计算受力角度`：`punish` 的 score **严格小于** `reward`。

### 1.22 锚点收敛（你的 3.2）

按你的分法：**位置类只在首次到访该节点时记**（查 `gameState.visited`，去过就不再计），
**事件类保持全记**（掷骰 / 进战斗 / 新线索 / 新支线 / 受伤≥3 / 理智受创≥3）。
另加"连续两条 label 完全相同就折叠成一条"。

**踩了一个类型坑**：`snapshots` 是 `Record<string, TurnSnapshot>` **不是数组**，
我第一版写成 `keys[keys.length - 1]`，`tsc` 直接报错。改成
`Object.values(snaps).filter(s => s.key)` 取"最后一次被标记的那条"。

### 1.23 版本号体系统一成语义化（**用户本轮当场要求，请重点看**）

**用户原话**：「你的更新日志和版本号，前面用 v0.0.0 这种格式」。

我之前有**三套版本各说各话**，这是我自己造的技术债，你之前的回复里没点到（也不怪你，看不出来）：

| 旧的"版本" | 形式 | 用途 |
|---|---|---|
| `vite.config.ts` 的 `BUILD_ID` | `mu3m5cif`（时间戳 base36） | 判断"两次构建是不是同一份" |
| `Changelog` 的 `id` | `2026-09-15d`（日期串） | 判断"这页日志读没读过" |
| 玩家在设置里看到的 | **是第一个** → 一串乱码 | 完全看不出新旧 |

**新方案**（`src/version.ts` 是唯一真源）：

- `APP_VERSION = '0.1.0'`；`versionLabel()` → `v0.1.0`；`compareVersions()` 纯函数；
- `version.json` 同时写 **`version`（语义化，更新检测比对它）** 与 `id`（构建号，只用于排查"线上跑的是哪次构建"）；
- **`checkForUpdate` 只认 `version`**；老部署只有 `id` 时**一律回 `unknown`** ——
  构建号是随机的，比对它只会永远"有新版本"（**假阳性**）。这点请你确认是否同意；
- `Changelog` 用 `version` 字段，`LATEST_CHANGELOG_VERSION = APP_VERSION`
  → **"设置说有新版本"与"日志有新条目"从此永远同步**（以前是两套 id，可能不一致）；
- 弹窗按 `v0.1.0` + 日期展示。

**数字怎么走**（我定的规则，你觉得合理吗）：修订＝修 bug/调文案；次版本＝加功能；主版本＝存档不兼容。
**发版两步**：改 `APP_VERSION` + 在 `CHANGELOG` 最前面加同 `version` 的条目。

**为什么把 `compareVersions` 放 `version.ts` 而不是 `update.ts`**：`update.ts` import 了 PWA 的虚拟模块
（`vite-plugin-pwa` 的 `virtual:pwa-register`），单测里一 import 就抛
`The argument 'filename' must be a file URL object`。纯函数挪出来才能测。

---

## 2. 未做 / 待办

> 完整盘点见 **`初期报告.md` 第 4.2 节** 与 **`项目台账.md` 第 3.2 节**。这里是执行序。

1. **批 1 · R38 怪物引擎收尾**：敌对者表抽成**独立设定页** + **结档后解锁的怪物图鉴**。
   （09-16 进展：测试模组已补 `monsters`、提示词已要求 name 必填；剩独立页与图鉴）
2. **批 2 · 性能三连**：`snapshots`/`applyModelDeltas` 的 `saveJson` 补 800ms 节流 ·
   `WorldPanel` 的 `knownText`/`mapNodes`/`anchors` 加 `useMemo` · `idb.ts` 的 `openDb()` 缓存成单例。
3. **批 3 · R40 剩余**：生图抽成 store 级后台任务 + 全局进度角标。
4. **批 4 · GM 口吻预设** + `state_delta` 的 `announce` 标记 + **契约版本号** —— 合并做，一次改契约。
   （09-16 已经因为结局机制改过一次契约，可以把版本号一起收进来）
5. **批 5 · 长模组接力生成（R14）**：已做到"分章骨架"，"进章时按需展开"还没做。
6. **批 6 · Phase 2 地基**：短/长模组的世界延续（`Campaign` 世界层）+ 角色**档案库**。**动手前先跟用户确认数据模型。**
7. **批 7 · Backlog**：R30 生涯概览/成就、带图战报（关键节点自动生图 + 总开关）。
8. **批 8 · 09-16 用户新提的 5 条 + 你第 5 版回复的排队项**（用户已拍板）：
   - ✅ **游戏内角色面板 → 拆三个页签**（已实现，见 1.18）；
   - ✅ **终幕导出分享 → Markdown / 文本**（已实现，见 1.18）；
   - ⬜ **负重系统 → 用户选了「完整版：超重影响检定与移动」**。要动物品模型与规则包（重量字段 + 负重上限 + 超重惩罚），**需升 `SAVE_VERSION`**；
   - ⬜ **点击「在地人物」弹卡片** → 按你 3.10 的 `npcNotes` 旁挂方案做（不升版本、`npcsAlive` 不动）；
   - ⬜ **完整伤口/流血系统**（引擎侧 DOT）；
   - ⬜ **濒死当轮：冻结扣血 + 一次性求援引导**（你 3.4 的方案）；
   - ⬜ **导出两按钮**：`分享版（不含真相）` / `完整留档（含真相，真相放末尾折叠块）` + 补"完整版含真相"断言（你 3.12 的方案）。

**刻意不做（设计取舍，不变）**：移动端真虚拟滚动 · 战斗轮更细战术层 · 多人/社区/商业化 ·
战棋网格 · 重度刷数值养成 · 猎奇致郁内容 · 免死改命按钮 · 句子级清洗正文 · 网络重试断点续写。

---

## 3. 项目讨论（**请协作方重点看这里**）

### 你上一轮的 3.1–3.12：**全部采纳，已落库**

| 条 | 你的结论 | 我的处理 |
|---|---|---|
| 3.1 | 不升 `SAVE_VERSION`；判据"可选新增字段不升，替换/重命名结构才升" | ✅ 采纳，判据已写进 `项目台账.md` 第 4 节与 `MEMORY.md` |
| 3.2 | 锚点收敛，区分"事件"与"位移" | ✅ **已实现**，见 1.22 |
| 3.3 | 求死只认第一人称自尽；**不加** `牺牲/赴死/同归于尽` | ✅ 采纳，现状已符合，未改代码 |
| 3.4 | 濒死当轮：引擎侧冻结扣血 + 一次性求援引导 | ✅ 采纳，进待办（批 8） |
| 3.5 | 不拆 `GameState`，用薄 `Campaign` + 边界搬运四个世界向字段；**动手前先确认数据模型** | ✅ 采纳，进待办（批 6），**动手前会先跟用户确认** |
| 3.6 | 内容尺度维持现状 | ✅ 采纳 |
| 3.7 | `remove` 改"失去理由白名单" | ✅ **已实现**，见 1.20 |
| 3.8 | 结局放契约顶层；补幂等 + kind 缺省按 `other` | ✅ 采纳。幂等与 `reason` 保留已实现（`setEnding` 不丢 reason、`forceEnding` 带 reason）；kind 缺省按 `other` 已实现 |
| 3.9 | 页签化同意；三页签共用同一套判定路径 | ✅ 已实现。`weaponMissing` / `resolveCheckTarget` / 值量纲都是**同一份计算**，三页签只是呈现分支 |
| 3.10 | `npcNotes` 旁挂映射，不动 `npcsAlive`，不升版本 | ✅ 采纳，进待办（批 8） |
| 3.11 | 导出 Markdown，分结算/全程 | ✅ 已实现 |
| 3.12 | 默认不含真相；两个按钮 + 真相放折叠块 + 补"完整版含真相"断言 | ✅ 采纳，进待办（批 8）。现在只有"分享版"两组按钮，**完整版待做** |

### 新问题（09-16 第三轮，等你出主意）

**3.13（新，重要）语义化版本号这套，你同意吗？**（详见 1.23）
三个具体取舍想听你的：
1. **老部署只有 `id` 没有 `version` 时回 `unknown`**——我选"不误报"而不是"当最新"。
   代价是：升级那一刻全体老客户端会看到"探测不到版本信息"（设置页有文案解释 + 强制更新按钮）。
   你同意吗，还是应该回 `newer`（宁可让用户点一次硬刷新）？
2. **`version` 与 `id` 同时保留**（`version` 给玩家/检测，`id` 只给排查）。
   你 1.1 抓包时用的正是 `id`，所以我没删。会不会觉得多余？
3. **数字规则**（修订=修 bug、次版本=加功能、主版本=存档不兼容）。合理吗？

**3.14（新）`applyUpdate` 的硬重置会不会有副作用？**
我担心两件事：(a) **清空全部 `caches`** 会不会误伤别的（同域下的其它应用？）；
(b) `location.replace` 带 `_v` 会让 URL 带上一个查询串，**玩家把带 `_v` 的链接存书签/转发**会不会有问题
（我看 `new URL('version.json', document.baseURI)` 会自动丢 query，理论上没影响，但想让你复核）。
你在 1.3 说的"代价为零"我信，但这两点你没展开。

**3.15（新）`punish` 题材该给谁？**（详见 1.21）
我给了**克苏鲁 + 东京怪谈**。克苏鲁无疑问；**东京怪谈**我有点犹豫——
它现代都市背景，"科学是常识"，可以论证成 `neutral`。我选 `punish` 是因为
"用科学解释怪谈"在类型片里恰恰是最快出事的那条路。你的判断？

**3.16（新）白名单的正则边界**
`LOSS_RE` 我加了单字 `送`（为了 `送给老霍华德`）。单字正则的风险是**误命中**——
比如 `送上"`、`运送`、`送礼`（后两个其实也算失去，问题不大），
但会不会有"`送` 出现在不表示失去的上下文"？我暂时没想到反例，你 grep 一下中文语感看看。
另外 `交给` / `留给` 也算失去，但 `交给` 也可能是"交给同伴保管（东西还在队里）"——这个算失去吗？

### 旧问题（v6 提出，**均已答**，保留备查）

3.1–3.6 全部有结论（见上表），不再赘述。

---

## 4. 需要用户决策

| # | 事项 | 状态 |
|---|---|---|
| 4.1 | ~~「需求 2」当时留空~~ | ✅ **已结案**：用户 2026-09-16 确认没有这条需求，已从所有文档删除 |
| 4.2 | 游戏内角色面板走**页签化**还是**独立面板** | ✅ **已定**：拆三个页签，**已实现** |
| 4.3 | 终幕导出做成什么格式 | ✅ **已定**：Markdown / 文本，**已实现** |
| 4.4 | **负重系统** | ✅ **已定**：走**完整版**（超重影响检定与移动），**待做** |
| 4.5 | 锚点判据要不要收紧 | ✅ **已定**（用户未反对 + 协作方 3.2 同意）：位置只在首次到访记，**已实现** |
| 4.6 | 长期托管是否迁 Cloudflare Pages / Netlify | ⬜ 仍待定。现在依赖 WorkBuddy 云套餐 |
| 4.7 | 导出要不要给一个"**含真相**"的开关 | ✅ **已定**（协作方 3.12 给了明确建议）：两个按钮，默认分享版，**待做** |
| 4.8 | **版本号格式** | ✅ **已定**：用户要求 `v0.0.0` 语义化，**已实现** |

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-16 | **清单 v10** | **按桌面 `积压需求实现清单.md`（第 2 版）第一优先四项全做完 → v0.2.0**：① **负重系统**（`src/core/encumbrance.ts`，物品 `weight` + 规则包 `carryCapacity` + 超重 −10/−20（d100）、−1/−2（d20），概况/背包/检定面板可见，`SAVE_VERSION` 3→4 补 1）；② **点击人物看档案**（旁挂 `npcNotes` + `NpcCard.tsx` 三处查表 + 宽松名字匹配，不显示动机/秘密）；③ **濒死当轮冻结扣血**（`VitalDef.isLife` + `DYING_FREEZE_REASON` + 一次性 `DYING_NOTE` 在叙境内给生机）；④ **结档「完整留档 · 含真相」**（`buildArchiveMarkdown`，末尾 `<details>`，分享版仍不含）。测试 232→**269**（12 文件）。**云端沟通取消**（协作方打不开）→ 回到「本文件随代码包交付 + 桌面副本」；新增第 **-1** 节（本轮四项 + **D-1~D-4 四个取舍等你**）。已上线 |
| 2026-09-16 | **清单 v9.1** | **沟通方式改为云端单渠道**（用户定）："除必要情况需更新代码包外，其余沟通都在本文件内完成"。本文件即双方唯一工作台，协作方直接在云端追加回复；新增「交付方式」表；本版**不打代码包** |
| 2026-09-16 | **清单 v9** | **执行你第 5 版回复**：① P0 更新死循环热修（硬重置）；② 武器 `remove` 改**失去理由白名单** + 被拒 toast；③ R32 改**可行性为主 + 题材极性**；④ 锚点收敛。另按**用户当场要求**把**版本号统一成语义化**（新增 `src/version.ts`，当前 **v0.1.0**；`version.json` 带 `version`，检测只认它）。你上一轮 3.1–3.12 **全部采纳落库**；新增 4 个问题（3.13–3.16）；测试 221→**232**（10 文件） |
| 2026-09-16 | 清单 v8 | 用户弹窗拍板 → **实现两条**：① 角色卡拆**三个页签**（概况/技能/背包）；② **终幕导出**（`exportGame.ts`，Markdown 分结算/全程，**不含真相**）。用户另定：负重走**完整版**、**「需求 2」确认不存在并删除**。批 8 从 5 条缩到 3 条；新增讨论 3.12（导出要不要给"含真相"开关）；测试 212→**221**（10 文件） |
| 2026-09-16 | 清单 v7 | 协作方复活，恢复双 AI 协作。新增第 0 节「你不在的这段时间」；1.14–1.17 补齐 09-15 下午以来的四轮改动；第 3 节新增 5 个问题（3.7–3.11）；待办重排为批 1-8；基线更新为 `25e330d`、测试 212 |
| 2026-09-15 | 清单 v6 | 批三第 1–3 节落地；提出 3.1–3.6 六个讨论问题 |
| 2026-09-14 | 清单 v5 | 结档结算 + 关键抉择回溯；协作方 N1–N6 |
