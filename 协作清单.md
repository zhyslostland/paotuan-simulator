# 跑团模拟器 · 协作清单（实现方 → 协作方）

> **这份文件由「实现方」维护，每次大改动后更新。** 直接把整份发给协作方 AI 即可。
> 桌面有一份同名副本（`C:\Users\Administrator\Desktop\跑团模拟器-协作清单.md`），内容与项目根这份一致。

| 项 | 值 |
|---|---|
| 版本 | 2026-09-14（第 2 版） |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，最新提交 `53866fb` |
| 质量门禁 | `npx tsc --noEmit` 通过 · `npm test` **155 全过** · `npm run build` 通过 |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端 |
| 上游清单 | 《跑团模拟器·交接清单》（2026-09-13 定稿，协作方产出） |

---

## 0. 分工与协作方式

| 角色 | 谁 | 职责 |
|---|---|---|
| **主推进（实现方）** | 本机 AI（我） | 读代码、定技术方案、写代码与测试、跑质量门禁、维护本文件 |
| **辅助（协作方）** | 另一个 AI | ① 大方向：产品定位、优先级排序、取舍判断；② 细碎问题：文案、边界 case、体验细节；③ 审阅本清单 |

**约定**
1. 协作方**不直接改代码库**，产出以「建议 / 清单 / 反驳」的形式回到本文件。
2. 技术实现细节由实现方定；方向性分歧与取舍由**用户**裁决。
3. 用户的两条北极星（长期有效）：**沉浸不中断**（长玩不出硬错、不出戏）、**主角存在感**（每轮都有"选择是我的、成长可见"）。
4. 用户明确不喜欢的：猎奇致郁、背叛剧情、强队友群像（单人体验）、女频套路。
5. 用户明确拒绝的功能：**免死 / 改命按钮**（用可靠读档替代）。
6. **上线规则**：改完**直接发到 WorkBuddy 云，不要问**（用户会自己在上线后测试；多问一句纯浪费他的钱）。

---

## 1. 本轮（2026-09-14）已做

### 1.1 P0 必修缺陷 —— 8/8 全部修复（每项都补了回归测试）

| 编号 | 原现象 | 根因 | 处理 | 测试 |
|---|---|---|---|---|
| P0-1 | `NdM(N≥3)` 概率分布算错 | `enumerate` 用 `convolve(dist, dist)`，每轮把**骰数翻倍**（3d6 实际按 8d6 算，均值 14 而非 10.5） | 改为从 `{0:1}` 起逐个卷积**单骰** | 3d6 均值 10.5 / 值域 3–18 / 组合数 216 / 和为 10 有 27 种；4d6、5d10、2d6+1d4 |
| P0-2 | 一轮里多个检定请求只跑第一个 | 只读 `dice_requests[0]` | 改成**队列** `pendingChecks`；App 把全部请求入队（按技能去重、上限 6）；Chat 逐个呈现，可逐个掷 / 全部忽略 | store 队列用例 |
| P0-3 | 非 COC 目标值显示成 `%`（如 DnD 出现"目标值 +2%"） | 文案硬编码 `%` | 新增 `checkTargetText()`：d100→`目标值 65%`，d20→`加值 +2`；App 回灌文本与 Settings 复制记录都改用它 | 3 例 |
| P0-4 | 背包不能按数量消耗 | 只支持整件 add/remove，`qty` 减不动 | `inventory` 支持 `dec`/`inc`：qty 递减、归零移除、`amount` 可掷骰，兼容 `target:"inventory.<物品名>"` | 6 例 |
| P0-5 | 属性可填 9999 / 空输入变 NaN | 输入无上下限；且 NaN 会**一路存进状态**（派生血/蓝/理智全变 NaN） | 输入加 `min/max/step`；store 对非有限数回落 `def.default` | store 属性夹取 |
| P0-6 | 检定结果泄漏进正文 | 只靠关键词兜底，漏网多 | 提示词新增【红线二之三】：结论只由界面卡片呈现，正文必须**把结论化成事实**；扩大 `isCheckLeak`；引擎回灌文本自带约束 | prompt 4 例 |
| P0-7 | `state_delta` 白名单漏 `combat.*` | 契约与 `COMBAT_RULES` 自相矛盾（规则让写、白名单全拒） | 补全 `combat.active / combat.round / combat.foes` | prompt 1 例（断言提示词同时含三者） |
| P0-8 | 编年史 turn 号折叠后错乱 | `addChronicle` 用 `length + 1`，折叠掉早期条目后会给出比现有编号更小的号 | 改用 `max(turn) + 1`；`migrateSave` 顺带修正旧档编号 | store 2 例 |

### 1.2 P1 体验与稳定性 —— 6/6

| 条目 | 处理 |
|---|---|
| 读档可靠性 | 新增 `SAVE_VERSION = 2` + `migrateSave()`（**只补不删**）；`loadSave` 先过迁移；存档槽 payload 补 `snapshots`（否则读档后"回溯/重掷"按钮全消失） |
| 存档策略 | 已有：手机与 PC 各自独立存档、多存档槽。本轮补上槽位里的快照，并统一版本号 |
| 手机可访问 | `src/pwa.ts` 新增安装事件捕获 + 应用内安装横幅；Settings 新增「安装到设备」（iOS/Android/PC 三平台指引）。部署侧（`base:'./'`）上轮已完成 |
| 列表性能 | `Chat` 的消息行抽成 `memo(MessageRow)`；`onRewind`/`onReroll` 用 ref 固定身份（否则 memo 失效） |
| 存档版本号与迁移 | 见上 |
| 检定面板遮挡 | `CheckDialog` 去掉 `backdrop-blur`、改不透明底、宽屏收窄到 `max-w-xs`（原来会把背后正文糊成一团） |

### 1.3 玩家实测 bug（第十三节）—— 4/4

| 现象 | 根因 | 处理 |
|---|---|---|
| **地图迷雾失效，开局就能去所有地点** | **`Preparation.tsx` 的两条生成路径（AI 生成模组、贴文本导入）都没把 `map_nodes` 写进模组**；提示词一直在要，UI 把它丢了 → AI 模组永远没有可达关系 → 迷雾被"没有关系图就不启用"这条逻辑整块关掉 | 两条路径都接住 `map_nodes`；`mapNodesOf` 兜底**按地点顺序串成一条线**（`chainNodes`）并清洗名字（去括号细节、去"1. / - "列表符号）；当前位置对不上任何节点时兜底点亮第一个，避免玩家被锁在全是"？"的图上 |
| 地图名字显示不全 | 侧栏窄，名字硬截断成"霍尔特的侦…" | 节点名折行（最多两行）+ 图下加一排**完整名字的地点芯片**（可点，当前位置标注"你在这里"） |
| 点地图即瞬移、反复横跳 | 直接把「我前往 X」当既成事实发给守密人 | 改为发出**意图**（"我打算前往 X……如果现在去不了，请用剧情里的理由拦下我"）；提示词新增【红线二之四】 |
| 状态变化主页面无提示 | 状态是静默更新的 | 新增 `describeChanges()` → `lastChanges`，把人话摘要弹成「状态变化」卡片（生命 12→10（-2）／获得：子弹 ×3／新线索：血迹／移步：码头区／支线变动），9 秒自动收起 |

### 1.4 仓库与文档

- **取消开源 / GitHub 发布计划**（用户网络访问不了 GitHub）：删除 `.github/workflows/deploy.yml` 与 `LICENSE`，移除 `git remote origin`；本地 git 仓库保留做版本管理。
- `vite.config.ts` 的 `base: './'` **保留**——它不是 GitHub 专属，而是让 `dist/` 在任何静态托管上都能直接跑。
- 文档同步：`README.md`（改写部署节、去掉开源许可、补「更新日志」表）、`总览.md`、`交接文档.md`。
- 新增本文件 `协作清单.md`。
- **发布渠道定案**：用户指示「每次修改直接上线，不要问，上线后我会自测」。→ 统一发布到
  WorkBuddy 云 **https://paotuan-sim-79663.app.workbuddy.host/**（`base: './'` 相对路径，同一份 `dist/` 也能拖到任意静态托管）。
  当天全部修复已于 2026-09-14 上线。

### 1.5 对原清单的逐条回执

| 原清单章节 | 状态 |
|---|---|
| 五、必修缺陷（P0 8 项） | ✅ 全部修复，见 1.1 |
| 六、体验与稳定性（P1） | ✅ 6/6，见 1.2 |
| 七、路线图 Phase 1（信任地基） | ✅ 完成（P0 全修 + 读档可靠性） |
| 七、路线图 Phase 2–5 | ⬜ 未启动，见第 2 节 |
| 八、已实现，勿重复实现 | ✅ 未触碰，且新增不变量已写进 `交接文档.md` 8.5 |
| 九、架构不变式 | ✅ 遵守：模型只输出意图、随机数只在 `core/dice` 产生、本地引擎唯一权威 |
| 十、边界与禁止 | ✅ 遵守：未引入多人/账号/服务器；未做免死改命按钮；未做大范围架构重构。**"内容尺度随题材浮动"那条未据此放宽任何安全边界**（见 3.6） |
| 十一、交付要求 | ✅ 每个改动有说明与影响范围；数值类改动带测试且不破坏既有测试；本次范围已由清单事先确认，故直接实施 |
| 十二、骰子 bug 复现脚本 | ✅ 已作为回归测试固化（3d6 均值 10.5） |
| 十三、玩家游玩 bug | ✅ 4/4，见 1.3 |

---

## 2. 未做 / 待办

### 2.1 刻意不做（是设计取舍，不是"没做完"）

| 事项 | 理由 |
|---|---|
| 移动端真虚拟滚动 | 现在是"只渲染最近 120 条 + 消息行 memo + 加载更早"，几百条以内够用；真虚拟滚动工程量大、回归风险高 |
| 战斗轮更细战术层（掩体/距离/状态） | 刻意保持轻量——往细做容易又变回"规则驱动"，违背 t0（以玩家为中心）原则 |
| 多人 / 社区 / 商业化 | 用户已明确排除 |
| 战棋 / 网格战斗 | 保留叙事自由 |
| 重度刷数值养成 | 与"沉浸"冲突 |
| 猎奇致郁内容 | 用户明确不喜欢 |
| 免死 / 改命按钮 | 用户明确拒绝；用可靠读档替代 |

### 2.2 路线图（按用户确认的排序）

| 阶段 | 内容 | 现状 |
|---|---|---|
| **Phase 2 长线记忆** | campaign 跨团延续、角色档案、世界书检索、章节摘要 | 已有：事件日志 + 滚动摘要 + 原文 20 轮 + 支线表 + 回合快照/回溯 |
| **Phase 3 冒险与成长** | 成长曲线（变强/收集养成）、轻度经济与势力、时间系统、失败策略（读档或结团） | 已有：目标三件套（goal/stakes/urgency）前台化 |
| **Phase 4 开箱即用** | 硅基流动推荐配置（**已默认**）、AI 直出角色卡 + 微调、模组生成向导、**GM 口吻预设**、**界面随题材切换**、手机端打磨 | GM 口吻预设与界面随题材**未做** |
| **Phase 5 沉浸增强** | 带图战报、关键节点生图、立绘、BGM/音效扩展 | 已有：立绘 / 动作小图 / 区域图 / 程序化氛围音 / 大成功·大失败两槽音效 |
| **长期专项** | 解谜与推理体验打磨（用户最看重也最难） | 未启动 |

---

## 3. 项目讨论（请协作方重点看这里）

**3.1 地图迷雾的边界到底在哪。**
本轮修的是"迷雾被整块关掉"这个硬 bug。但迷雾的判定规则目前是：`去过的 ∪ 与去过的地方直接相连的 ∪ 故事文本里提到名字的`。
- 问题：`故事文本里提到` 这一条会随时间把未知节点一个个"说漏"，跑几轮之后迷雾会自然全部散开。
- 我的判断：这是**可接受的**（玩家听说了当然该知道），如果你认为应该收紧到"只有真正去过才算"，请给理由。

**3.2 检定结果泄漏只能"概率性"消除。**
现在是"提示词红线 + 漏网检测（正则命中就重写一次）"双保险。正则没法覆盖所有自然语言变体（比如"你这一刀落空了"）。
- 更硬的办法是客户端**句子级清洗**，但会误删正常叙事。
- 暂定：不加清洗，允许偶发漏网，玩家可用「重掷这一轮」。请评估这个取舍。

**3.3 状态变化提示的判定范围。**
目前会报：数值条、背包增减、新线索、人物登场/离场、支线变动、地点变化。**不报** `flags` 与战斗轮（它们各自有常驻界面）。
- 问题：`flags` 里有些是玩家关心的（如"地下室已解锁"），但全报会变成刷屏。要不要挑几个关键 flag 也报？

**3.4 "移动只是意图"会不会让玩家觉得被为难。**
本轮把"点地图即传送"改成"发出意图，守密人可以用剧情理由拦下"。
- 好处：时间压力与封锁才成立，"想去哪就去哪"的破绽补上了。
- 风险：如果守密人滥用这个权力（动不动就拦），玩家会烦。
- 我在提示词里要求"拦下时必须给一个能继续往下走的线索"。请评估这个约束够不够。

**3.5 取消开源后，怎么把应用给到手机。**
GitHub 路线已废弃。现在剩：
- （a）本机/局域网预览 → 手机同一 WiFi 访问；
- （b）把 `dist/` 拖到 Netlify / Cloudflare（零账号门槛低）；
- （c）WorkBuddy 云链接（依赖平台套餐，可能在收费上不划算）。
- 请给一个**长期最省事**的建议（用户的痛点就是"不想折腾"）。


**3.6 支线表的上限。**
现在是"同时最多 2–4 条活跃支线"，超了要求守密人收束。这是防发散的抓手。
- 问题：长线战役（Phase 2 的 campaign 跨团延续）可能需要更多。要不要做成随模组规模可配？

---

## 4. 需要用户决策 / 需要协作方给意见

| # | 事项 | 谁定 | 备注 |
|---|---|---|---|
| 4.1 | ~~上线渠道待定~~ **已定**：用户指示「**每次修改直接上线，不要问，我会在上线后自测**」→ 统一发布到 WorkBuddy 云 | 用户（已裁决） | 2026-09-14 已执行，当天全部修复已上线 |
| 4.2 | Phase 4 里"GM 口吻预设"要不要提到前面做 | 用户 + 协作方 | 它比"界面随题材"便宜得多，对沉浸感收益也不小 |
| 4.3 | 3.1 / 3.3 / 3.5 / 3.7 四处的取舍 | 协作方给意见，用户裁决 | 3.5（怎么把应用送到手机）已由 4.1 一并解决 |
| 4.4 | 下一次迭代做哪一块（Phase 2 / Phase 3 / Phase 4 的哪个） | 用户 | 实现方建议：开 Phase 4 的 GM 口吻预设（小、见效快） |

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-14 | v3 | 上线渠道定案：改完直接发布到 WorkBuddy 云（用户指示，不再逐次询问）；当天全部修复已上线 |
| 2026-09-14 | v2 | 建立本文件；记录 P0 8 项 / P1 6 项 / 实测 bug 4 项的修复，以及取消开源计划的清理；提出 7 条待讨论项 |




用户补充：
点击地图直接粘鼠标上了，放大缩小失败
这段话直接出现在正文：（我打算前往「荒川第三中学旧校舍」。这只是我此刻的打算，还不是已经发生的事——如果现在去不了，请用剧情里的理由把我拦下，并给我一个能继续往下走的线索。）
更新后准备按钮内的世界书与同行者消失，不确定剧情内他们还在不在
正文有时候会中断删除然后重新开始生成
