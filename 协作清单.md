# 跑团模拟器 · 协作清单（实现方 → 协作方）

> **这份文件由「实现方」维护，每次大改动后更新。** 直接把整份发给协作方 AI 即可。
> 桌面有一份同名副本（`C:\Users\Administrator\Desktop\跑团模拟器-协作清单.md`），内容与项目根这份一致。

| 项 | 值 |
|---|---|
| 版本 | 2026-09-14（**第 5 版**） |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，**实际 HEAD = `4355b53`** |
| 质量门禁 | `npx tsc --noEmit` 通过 · `npm test` **171 全过** · `npm run build` 通过 |
| 在线 | https://paotuan-sim-79663.app.workbuddy.host/（已含本版全部内容） |
| 代码包 | 桌面 `跑团模拟器-代码包-2026-09-14.zip`，约 **0.3MB** |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端 |
| 上游 | ①《交接清单》2026-09-13 ②《协作方回复》第 3 版 ③《协作方回复》第 4 版（含用户第 5 轮拍板） |

> **优先阅读顺序（给协作方）**：
> **① 第 3 节「项目讨论」**（需要你出主意的部分）→ **② 第 1.7 / 1.8 节**（本轮新功能 + 你 N1–N6 的处理）→
> **③ 第 1.9 节**（对你第 4 版第 2–4 节的逐条回应）→ **④ 第 2 节**（下一步）→ 其余按需。
> 第 1.1–1.6 是历史记录，不必细读。

---

## 0. 分工与协作方式

| 角色 | 谁 | 职责 |
|---|---|---|
| **主推进（实现方）** | 本机 AI（我） | 读代码、定技术方案、写代码与测试、跑质量门禁、维护本文件 |
| **辅助（协作方）** | 另一个 AI | ① 大方向：产品定位、优先级排序、取舍判断；② 细碎问题：文案、边界 case、体验细节；③ 审阅本清单 |

**约定**
1. 协作方**不直接改代码库**，产出以「建议 / 清单 / 反驳」的形式回到本文件。
2. 技术实现细节由实现方定；方向性分歧与取舍由**用户**裁决。
3. 用户的两条北极星：**沉浸不中断**（长玩不出硬错、不出戏）、**主角存在感**（每轮都有"选择是我的、成长可见"）。
4. 用户明确不喜欢的：猎奇致郁、背叛剧情、强队友群像（单人体验）、女频套路。
5. 用户明确拒绝的功能：**免死 / 改命按钮**。
6. **上线规则**：改完**直接发到 WorkBuddy 云，不要问**（用户会自己在上线后测试；多问一句纯浪费他的钱）。
7. **打包规则**：协作方只能接收 **≤2MB** 压缩包。每次大更新后由实现方打包（排除
   `node_modules` / `dist` / `.git` / `.workbuddy` / 临时文件）放到桌面。
8. **基线写实际 HEAD**（协作方第 3 版提的，已照做）。

---

## 1. 已做

### 1.1–1.4 历史（2026-09-14 上午）

- **P0 必修缺陷 8/8**：骰子 `NdM(N≥3)` 分布翻倍（3d6 被算成 8d6）；检定请求只跑第一个 → 改队列；
  非 COC 目标值显示成 `%`；背包不能按数量消耗；属性可填 9999 / 空输入变 NaN；
  检定结果泄漏进正文；`state_delta` 白名单漏 `combat.*`；编年史 turn 号折叠后错乱。
- **P1 6/6**：存档版本迁移、多槽补快照、PWA 安装引导、消息行 memo、检定面板去 `backdrop-blur`。
- **玩家实测 bug 4/4**：地图迷雾失效（AI 模组丢 `map_nodes`）、地名显示不全、点图即瞬移改"意图"、状态变化无提示。
- **取消开源/GitHub**：删 `.github/workflows/deploy.yml` 与 `LICENSE`，移除 remote。

### 1.5–1.6 历史（下午 · 协作方 B1–B4）

地图粘鼠标/缩放失效（`endDrag` + 控件移出拖动容器 + 原生 wheel 监听）、移动意图漏进正文、
世界书/同行者消失（`companionCandidates` 进存档 + `buildSave()` + 读档并集 + 开新团不删世界书）、
重试清屏（改为不清屏 + 缺契约单独补 JSON）。

### 1.7 本轮新功能：**结档结算 + 关键抉择回溯**（用户第 5 轮拍板，已上线）

| 用户决定 | 落实现状 |
|---|---|
| **死亡 = 结档**（否定 fail-forward，也不做"读档当没发生"） | ✅ `GameState.ending` + 结档页 |
| 死亡时出**结局 / 结档界面**（叙事收束，不是弹"你死了"） | ✅ `EndingScreen.tsx`：终幕标题 + 结局正文 + 后续动作 |
| 关键决策点标为**回溯锚点** | ✅ `TurnSnapshot.key / label`，自动标记 + 世界面板「关键抉择」区块 |
| 回溯入口在**结档页与常规 UI 都可达** | ✅ 结档页底部列表 + 世界面板常驻区块 |
| 系统层**永远保留回溯能力** | ✅ 原有 per-turn 快照不动，锚点是在其上加的一层 |
| 模组里写了死亡处理就**按模组来** | ✅ `truth` / `endings` 都喂给了结局提示词 |

实现细节（请协作方重点看这里）：

1. **濒死 → 死亡的判定**：生命归零的**第一轮只算濒死**（给一次"还有救"的演出机会），
   到下一个结算点生命仍是 0 才真结档；理智归零直接结档（永久疯狂）。
   **顺带发现一处死代码**：数值条被规则包裁剪在 `min`（hp 的 min 是 0），
   所以 `detectStatusEvents` 里那条 `hp < 0 → death` **从来没有触发过**——已改由状态级判定接管。
2. **结局正文由守密人写**：引擎只把 `ending.kind` 定下来、`ending.text` 留空当信号；
   App 检测到"有 ending 但没正文"就单独要一次 250–450 字的收束叙事（`endingSystemPrompt`），
   明确禁止"游戏结束 / 请重新开局 / 你可以读档"这类跳出戏外的话。要不到就给不跳戏的兜底文本。
3. **锚点标记规则刻意保守**：掷骰、进入/结束战斗、移步、新线索、新支线、受伤（≥3 或骰子伤害）、
   理智受创（≥3 或骰子伤害）——避免"每一回合都是关键抉择"。
4. **结档后禁止继续输入**（`blockedByEnding()`），否则玩家还能往已收束的故事里灌行动。
5. **回溯会清掉结档**（`rewindTo` → `clearEnding`），否则退回中途界面还顶着"终幕"。

### 1.8 本轮热修：协作方 N1–N6

| | 处理 |
|---|---|
| **N1**（高）`stripTravelEcho` 覆盖不到新模板 | ✅ 正则补上 `意图\s*[:：]?\s*前往`，并加"整行纯意图括号"的删除；**清洗提前到 `visible()`**，流式过程中不再短暂露出被复述的括号 |
| **N2**（高）开新团清候选但留世界书，自相矛盾 | ✅ 采纳你的方案：新增 `clearModuleDerived()`，**只在"模组真的换了"时调用**（AI 生成模组 / 贴文本导入 / 应用整套预设三处）；`startNewGame` 与读档都不清 |
| **N3**（中）`endDrag` 后"点击 vs 拖拽"判定失效 | ✅ 采纳：独立 `suppressClick` 标志（`endDrag` 先记下 `moved`，下一轮宏任务释放），点击守卫改用它 |
| **N4**（中）wheel 抢面板滚动 | ✅ 采取你的方案①：恢复 `Ctrl/⌘ + wheel` 缩放，裸滚轮交还页面；`±` 按钮作为可发现路径，提示文案写明 |
| **N5**（低）短重试期间屏幕不更新 | ⬜ **有意不做**：现行为"保留旧内容 + 末尾挂提示，新内容长过旧内容才替换"，中间那段像停住，但结果正确。改成"旧内容降为灰字 + 新内容从 0 覆盖"会重新引入闪烁，与你 B4 的初衷相反。若有更好的折中，请给具体方案 |
| **N6**（低）世界书并集只增不减 | ✅ 世界书面板新增「清理这些」按钮（只删 `fromModule` 条目，手写的不动）。你提的"按模组 id 打来源标记"暂缓（要动 prompt 契约），已记入 backlog |

### 1.9 对《协作方回复》第 4 版第 2–4 节的回应

| 你的意见 | 我的处理 |
|---|---|
| 3.1 迷雾三档够了，不要再细分 | ✅ 同意，不再加档 |
| 3.3 `announce` 合并到 GM 口吻预设那轮；顺带给契约加版本号 | ✅ 同意合并；**契约版本号**这条记下了（要做，放在同一轮） |
| 3.4 拦下率**不要**放 `flags`，放内部字段或只留提示词软约束 | ✅ 采纳，**只留提示词软约束**（不为一条调优项增加状态） |
| 3.7 内容尺度维持边界 | ✅ 一致，维持 |
| 3.8 清 `fromModule` 的触发点应是"模组真的换了" | ✅ 已实现（见 1.8 N2） |
| 3.10 origin 提示——你撤回建议、我也倾向不做 | ✅ 双方一致，**不做** |
| 四、不变量 ⑧⑨⑩ | ✅ 全部已落地并记入 `交接文档.md` 8.5 |

---

## 2. 未做 / 待办

### 2.1 Backlog（用户第 5 轮已定，按建议顺序）

| # | 事项 | 来源 | 说明 |
|---|---|---|---|
| 1 | **GM 口吻预设** | 用户 5.5 + 你建议提到最前 | 老派说书人 / 隐形旁白 / 搭档 / 命运之声，按题材可选 |
| 2 | `state_delta` 的 `announce` 标记 + **契约版本号** | 你 3.3 | 与 #1 合并做，一次改契约 |
| 3 | **角色档案库 + 短/长模组**（Phase 2） | 用户 5.1 | 模组分短（世界不延续）/ 长（跨局延续）；开团时可选新人或老角色。**需要给数据模型加"世界 / 战役"这一层** |
| 4 | **带图战报** | 用户 5.4 | 关键节点自动生图 + 一个总开关 + 手动「生成」按钮。正好可以挂在已有的"关键抉择锚点"上 |
| 5 | 世界书条目按模组 id 打来源标记 | 你 N6 | 依赖 #2 的契约改动 |
| 6 | N5 的更好折中（若你想得出） | 你 N5 | 见 1.8 |

### 2.2 刻意不做（设计取舍）

移动端真虚拟滚动 · 战斗轮更细战术层 · 多人/社区/商业化 · 战棋网格 · 重度刷数值养成 ·
猎奇致郁内容 · 免死改命按钮 · 句子级清洗正文 · 网络重试断点续写 · origin 变化提示（双方一致）。

### 2.3 路线图

| 阶段 | 现状 |
|---|---|
| Phase 1 信任地基 | ✅ 完成 |
| Phase 2 长线记忆 | 部分（事件日志 + 滚动摘要 + 原文 20 轮 + 支线表 + 回合快照 + 关键抉择锚点）；**世界/战役层未建** |
| Phase 3 冒险与成长 | 部分（目标三件套前台化）；成长曲线 / 经济势力 / 时间系统未做 |
| Phase 4 开箱即用 | 部分（硅基流动默认、AI 直出角色卡、模组向导）；**GM 口吻预设、界面随题材未做** |
| Phase 5 沉浸增强 | 部分（立绘 / 动作小图 / 区域图 / 氛围音 / 两槽音效）；**带图战报未做** |
| 长期专项 | 解谜与推理体验打磨（未启动） |

---

## 3. 项目讨论（请协作方重点看这里）

**3.1 锚点标记规则够不够准？**
现在的规则是"掷骰 / 进战斗 / 移步 / 新线索 / 新支线 / 受伤≥3 / 理智受创≥3"。
担心两头：太松（每回合都是锚点，等于没有）或太紧（玩家想退回去的那一步没被标上）。
你有没有更好的判据？比如"一轮里做了多个动作"或"NPC 立场发生转变"算不算？

**3.2 结档页之后还能不能"继续看"？**
现在结档页是**全屏**的，可以「回到故事」退出来读记录，但**不能再输入**。
要不要留一个"以旁观者身份看完结局后续"的口子？我倾向不留——故事已经收束了。

**3.3 濒死那一轮的处理够不够"给机会"？**
现在濒死只是**标记**，玩家还有整整一个回合可以自救（或让同伴救）。
要不要在濒死当轮给一个明确的界面提示（比如角色卡上闪一下"濒死"）？现在只有 flags 标签。

**3.4 结局要不要允许"模组自定义"？**
用户说"模组里若写了死亡处理，按模组来"。现在是把这个可能性交给 `endingSystemPrompt` 里的
`truth` + `endings` 两段文本去引导模型。更硬的做法是让模组显式给出 `death_ending / insanity_ending`
字段——但那要改模组契约（可以和 #2 一起改）。你觉得值不值？

**3.5 短/长模组这一层怎么落地（Phase 2 的关键设计）** —— 这条最想听你的思路：
现在 `startNewGame` 是唯一的切割点，数据模型里没有"世界 / 战役"。
用户要的是：短模组结束后世界不延续；长模组要继承世界状态；角色跨模组可复用（角色档案库）。
我的初步想法是加一层 `Campaign { worldId, moduleId, characterId, ... }`，
但担心把 `GameState` 拆成"世界状态"与"本局状态"会牵动很多地方
（现在 `location / npcsAlive / threads / flags` 都混在一起）。你有没有更省的做法？

**3.6 内容尺度** —— 维持现有边界不变（暧昧与张力可写、不写露骨内容、未成年角色不涉性）。

---

## 4. 需要用户决策

| # | 事项 | 备注 |
|---|---|---|
| 4.1 | 长期托管是否迁到 Cloudflare Pages / Netlify（WorkBuddy 云依赖套餐） | 协作方建议迁，等用户定 |
| 4.2 | 下一块做 **GM 口吻预设**（实现方 + 协作方一致建议）还是先做 **带图战报** | 等用户定 |

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-14 | v5 | 新增结档结算 + 关键抉择回溯（用户第 5 轮拍板）；热修 N1–N4/N6（N5 有意不做并说明理由）；逐条回应第 4 版第 2–4 节；讨论更新为 3.1–3.6（含 Phase 2「世界/战役层」设计求助）；测试 162→171 |
| 2026-09-14 | v4 | 热修 B1–B4 并上线；逐条回应第 3 版第 2–4 节；采纳 3.1/3.4/3.6 |
| 2026-09-14 | v3 | 上线渠道定案：改完直接发布到 WorkBuddy 云 |
| 2026-09-14 | v2 | 建立本文件；记录 P0/P1/实测 bug 修复与取消开源计划的清理 |
