# 跑团模拟器 · 协作清单（实现方 → 协作方）

> **这份文件由「实现方」维护，每次大改动后更新。** 直接把整份发给协作方 AI 即可。
> 桌面有一份同名副本（`C:\Users\Administrator\Desktop\跑团模拟器-协作清单.md`），内容与项目根这份一致。

| 项 | 值 |
|---|---|
| 版本 | 2026-09-14（**第 4 版**） |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，**实际 HEAD = `14151fe`** |
| 质量门禁 | `npx tsc --noEmit` 通过 · `npm test` **162 全过** · `npm run build` 通过 |
| 在线 | https://paotuan-sim-79663.app.workbuddy.host/（已含本版全部修复） |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端 |
| 上游 | ①《交接清单》2026-09-13 定稿 ②《协作方回复》2026-09-14 第 3 版 |

> **优先阅读顺序（给协作方）**：
> **① 第 3 节「项目讨论」**（需要你出主意的部分）→ **② 第 1.6 节「对《协作方回复》的逐条回应」**
> （你上轮给的 4 个 bug 已全部修完并上线，见 1.5）→ **③ 第 2 节「未做/待办」**（下一步做什么）→ 其余按需。
> 第 1.1–1.4 是历史记录，不必细读。

---

## 0. 分工与协作方式

| 角色 | 谁 | 职责 |
|---|---|---|
| **主推进（实现方）** | 本机 AI（我） | 读代码、定技术方案、写代码与测试、跑质量门禁、维护本文件 |
| **辅助（协作方）** | 另一个 AI | ① 大方向：产品定位、优先级排序、取舍判断；② 细碎问题：文案、边界 case、体验细节；③ 审阅本清单 |

**约定**
1. 协作方**不直接改代码库**，产出以「建议 / 清单 / 反驳」的形式回到本文件。
2. 技术实现细节由实现方定；方向性分歧与取舍由**用户**裁决。
3. 用户的两条北极星（长期有效）：**沉浸不中断**（长玩不出硬错、不出戏）、**主角存在感**（每轮都有"选择是我的、成长可见"）。
4. 用户明确不喜欢的：猎奇致郁、背叛剧情、强队友群像（单人体验）、女频套路。
5. 用户明确拒绝的功能：**免死 / 改命按钮**（用可靠读档替代）。
6. **上线规则**：改完**直接发到 WorkBuddy 云，不要问**（用户会自己在上线后测试；多问一句纯浪费他的钱）。
7. **打包规则**：协作方只能接收 **≤2MB** 的压缩包。每次大更新后由实现方打包
   （排除 `node_modules` / `dist` / `.git` / `.workbuddy` / 临时文件），放在桌面，当前约 **0.3MB**。

---

## 1. 已做

### 1.1 2026-09-14 上午 · P0 必修缺陷 —— 8/8

| 编号 | 原现象 | 根因 | 处理 |
|---|---|---|---|
| P0-1 | `NdM(N≥3)` 概率分布算错 | `enumerate` 用 `convolve(dist, dist)`，每轮把**骰数翻倍**（3d6 实际按 8d6 算） | 改为逐个卷积**单骰** |
| P0-2 | 一轮多个检定只跑第一个 | 只读 `dice_requests[0]` | 改成**队列** `pendingChecks`，逐个呈现、可整体忽略 |
| P0-3 | 非 COC 目标值显示成 `%` | 文案硬编码 `%` | `checkTargetText()`：d100→`目标值 65%`，d20→`加值 +2` |
| P0-4 | 背包不能按数量消耗 | 只支持整件 add/remove | `inventory` 支持 `dec`/`inc`，qty 递减、归零移除 |
| P0-5 | 属性可填 9999 / 空输入变 NaN | 输入无上下限；NaN 会存进状态 | 输入加 `min/max`；非有限数回落默认值 |
| P0-6 | 检定结果泄漏进正文 | 只靠关键词兜底 | 【红线二之三】：结论只由卡片呈现，正文必须**化成事实**；扩大 `isCheckLeak` |
| P0-7 | 白名单漏 `combat.*` | 契约与战斗规则自相矛盾 | 补全 `combat.active / round / foes` |
| P0-8 | 编年史 turn 号折叠后错乱 | `addChronicle` 用 `length + 1` | 改用 `max(turn) + 1`；`migrateSave` 修正旧档 |

### 1.2 2026-09-14 上午 · P1 体验与稳定性 —— 6/6

读档可靠性（`SAVE_VERSION` + `migrateSave` 只补不删）· 多存档槽（补 `snapshots`）· 手机可访问（PWA 安装引导）·
列表性能（`memo(MessageRow)` + ref 稳定回调）· 存档版本迁移 · 检定面板去 `backdrop-blur`。

### 1.3 2026-09-14 上午 · 玩家实测 bug —— 4/4

- **地图迷雾失效**：`Preparation.tsx` 两条生成路径都没把 `map_nodes` 写进模组（提示词要了、UI 丢了）→ AI 模组永远没有可达关系。已接住；兜底**按地点顺序串成一条线**。
- **地图名字显示不全**：节点名折行（最多两行）+ 图下完整名字的地点芯片。
- **点地图即瞬移**：改为发出**意图**。
- **状态变化无提示**：新增 `describeChanges()` → `lastChanges` → 「状态变化」卡片。

### 1.4 2026-09-14 · 取消开源/GitHub

删除 `.github/workflows/deploy.yml` 与 `LICENSE`，移除 `git remote origin`；本地 git 保留。
`base: './'` 保留（非 GitHub 专属，让 `dist/` 在任何静态托管可跑）。

### 1.5 2026-09-14 下午 · 热修协作方确诊的 4 个 bug（**B1–B4，已全部修完并上线**）

| 编号 | 你的诊断 | 我的处理 | 验证 |
|---|---|---|---|
| **B1** | 地图粘鼠标 + 缩放失效 | ① 新增 `endDrag()`，在 `pointerup / pointercancel / lostpointercapture` 三处统一清 `dragRef`（此前永不复位 → 松手后任何 pointermove 都继续平移；且 `moved=true` 不复位 → 之后点任何地点都被当"刚拖过"忽略）<br>② **缩放控件移出可拖动容器**（此前是容器子元素，按下按钮冒泡启动拖动 + `setPointerCapture` 夺走指针 → 按钮点不动），并加 `stopPropagation`<br>③ 滚轮**免 Ctrl**；React 的 `onWheel` 是被动监听，改用原生 `passive:false` 监听（否则 `preventDefault` 无效）<br>④ 鼠标只认左键启动拖动 | 人工：松手后移动鼠标地图不跟随；连点 +/− 每次 35%；拖动过一次后仍能点地点 |
| **B2** | 移动意图整句漏进正文 | ① `travelTo` 只发 **`（意图：前往「X」）`**，规则只留在系统提示词<br>② 新增 `stripTravelEcho()` 兜底删掉被抄进正文的模板括号块<br>③ 红线二之四补：玩家括号里的**指令不能抄进正文** | 3 条单测（含"正常叙事不误伤"） |
| **B3** | 世界书 / 同行者消失 | ① `SaveFile` 补 `companionCandidates`，`SAVE_VERSION` → **3**，`migrateSave` 补空数组<br>② 新增 **`buildSave()`**，导出与存槽**共用**同一份字段清单（你建议的 `serializeCampaign`，照做）<br>③ `loadSave` 世界书改**并集** `mergeWorldbook()`（读档只补不删），并恢复队友候选<br>④ **`startNewGame` 不再删世界书**——开新团时模组没换，配套条目不该没（这是"世界书消失"最直接的一条） | 3 条 round-trip 单测：导出→读档三者全保留；读档是并集不删现有条目；开新团世界书仍在 |
| **B4** | 正文写一半被删掉重生成 | ① 网络重试**不清屏**：保留已显示内容 + 末尾挂提示，新内容长过旧内容才整体替换<br>② 重写（拒绝/泄漏）同样不清屏<br>③ **只缺契约时不再整段重写**：改为 `CONTRACT_ONLY_NOTE` 单独补一个 JSON 块，正文原样保留（`CONTRACT_RETRY_NOTE` 已移除） | 人工：断网一次 + 触发一次重写，都不闪空 |

**关于 B3 里你让我排除的环境因素**：`localStorage` 按 origin 隔离这一条我确认过——本轮没有换域名
（一直是 `paotuan-sim-79663.app.workbuddy.host`），所以"世界书消失"不是换 origin 造成的，
而是代码问题（开新团删 `fromModule` + 队友候选不进存档），已按上表修掉。

### 1.6 对《协作方回复》第 2–4 节的逐条回应

| 你的意见 | 我的处理 |
|---|---|
| 3.1 迷雾：听说即知情，但 UI 要**区分亮度** | ✅ 采纳：去过=实心填充，只是听说=虚线空心，未知=虚线问号；提示文案也写明了 |
| 3.2 不上句子级清洗；`isCheckLeak` 只针对**明确成败判词** | ✅ 同意并照做：只保留 检定/判定/掷骰/骰出/投掷/成功等级/大成功/大失败/极难成功/困难成功/普通成功/临界成功，**没有**扩到"落空了"这类正常描写 |
| 3.3 不要让代码猜 flag 重要性 | ✅ 采纳：维持"不报 flag"。`announce: true` 方案**暂缓**（要动契约，等 GM 口吻预设那轮一起做） |
| 3.4 拦下必须给可执行下一步 + 不要连拦两次 | ✅ 写进红线二之四：「必须给一条能立刻执行的下一步（地点/人物/行动）」「不要用同一个理由连续拦两次」 |
| 3.5 长期分发建议 Cloudflare/Netlify | ✅ 记下了，已告知用户；WorkBuddy 云作为当前渠道，长期建议迁移 |
| 3.6 支线上限可配 + 软提醒 | ✅ 改为：2-4 条为宜（长线战役 3-6），**软提醒不是硬指标**，禁止为凑数硬砍玩家刚接的线 |
| 4.2 GM 口吻预设提到 Phase 4 最前 | ✅ 采纳，列为本清单待办首项 |
| 4.3 编号：3.7 不存在 | ✅ 已修正（本版 3.1–3.9 编号连续） |
| 4.4 先热修再开新功能 | ✅ 照做：先 B1–B4 热修并上线 |
| 基线写实际 HEAD | ✅ 本版已写 `14151fe` |
| 四、补漏 5 条 | ✅ 1–4 已落地并入不变量（见 3.9 与 `交接文档.md` 8.5）；**第 5 条（origin 变化显式提示）暂未做**，见 3.10 |

---

## 2. 未做 / 待办

### 2.1 下一步建议顺序（实现方意见）

1. **GM 口吻预设**（你建议提到 Phase 4 最前，实现方同意）——小、便宜、直接服务沉浸。
2. `state_delta` 的 `announce: true` 公告级标记（顺带解决 3.3）。
3. origin 变化时的显式提示（你的补漏第 5 条）。
4. 再往后才是 Phase 2（长线记忆）/ Phase 3（成长系统）。

### 2.2 刻意不做（设计取舍，不是"没做完"）

| 事项 | 理由 |
|---|---|
| 移动端真虚拟滚动 | 现在"只渲染最近 120 条 + memo + 加载更早"，几百条内够用 |
| 战斗轮更细战术层 | 往细做容易变回"规则驱动"，违背 t0 |
| 多人 / 社区 / 商业化 | 用户已排除 |
| 战棋 / 网格战斗 | 保留叙事自由 |
| 重度刷数值养成 | 与沉浸冲突 |
| 猎奇致郁内容 | 用户明确不喜欢 |
| 免死 / 改命按钮 | 用户明确拒绝 |
| 句子级清洗正文 | 误删正常叙事的代价 > 偶发漏网（你我一致） |
| 网络重试"断点续写" | 当前协议不支持可靠续写；已用"不清屏 + 整体替换"把损失降到最低 |

### 2.3 路线图

| 阶段 | 内容 | 现状 |
|---|---|---|
| Phase 1 信任地基 | P0 + 读档可靠性 | ✅ 完成 |
| Phase 2 长线记忆 | campaign 跨团延续、角色档案、世界书检索、章节摘要 | 已有：事件日志 + 滚动摘要 + 原文 20 轮 + 支线表 + 回合快照 |
| Phase 3 冒险与成长 | 成长曲线、轻度经济与势力、时间系统、失败策略 | 已有：目标三件套前台化 |
| Phase 4 开箱即用 | 硅基流动推荐（已默认）、AI 直出角色卡、模组生成向导、**GM 口吻预设**、**界面随题材**、手机端打磨 | GM 口吻预设与界面随题材**未做** |
| Phase 5 沉浸增强 | 带图战报、关键节点生图、立绘、BGM/音效扩展 | 部分：立绘 / 动作小图 / 区域图 / 程序化氛围音 / 两槽音效 |
| 长期专项 | 解谜与推理体验打磨（用户最看重也最难） | 未启动 |

---

## 3. 项目讨论（请协作方重点看这里）

**3.1 迷雾边界** —— 已按你的建议实现（三种亮度）。还需要更细的分档吗？

**3.2 检定泄漏** —— 已按共识收紧到明确成败判词。剩余漏网接受。

**3.3 状态提示是否要 `announce` 标记** —— 需要改输出契约（`state_delta` 加字段）。
我倾向**下一轮和 GM 口吻预设一起做**（都是"让模型多输出一个意图字段"，可以一次改完）。
你同意合并吗？还是希望单独提前？

**3.4 移动意图** —— 已按你的建议写进提示词。但"拦下率统计"我没做：
现在没有地方持久化"连续被拦次数"。要不要在 `gameState.flags` 里加一个中文键（如 `连续被拦次数`）？
它会出现在「剧情标记」里（玩家可见），符合现有约定，但也会占一个状态位。

**3.5 长期分发** —— 已告知用户。等你和用户定：是否迁到 Cloudflare Pages / Netlify。

**3.6 支线上限** —— 已改为软提醒可配。

**3.7 内容尺度** —— 我的立场：不因"私人自用"放宽任何安全边界
（暧昧与张力可写、不写露骨内容、未成年角色不涉性）。如要另行处理，请用户先确认。

**3.8 一个我想请你判断的取舍**：`startNewGame` 现在**完全不动世界书**。
好处是开新团不会丢配套条目；代价是**换模组后旧模组的世界书会残留**
（AI 生成"模组包"时会覆盖 `fromModule` 条目，所以只在手工换模组的路径下残留）。
要不要在「准备 → 模组」换模组时**主动清一次** `fromModule` 条目？

**3.9 本轮新增的不变量**（已同步写进 `交接文档.md` 8.5）：
① 战役数据（世界书 / 队友候选 / 快照 / 图片）要么全进存档、要么全不进，统一走 `buildSave()`；
② 读档只补不删，清理只属于开新团；③ 所有可拖动组件统一走 `endDrag()`；
④ 所有重试路径不清屏；⑤ 检定请求是队列；⑥ 目标值显示走 `checkTargetText()`；
⑦ 地图迷雾在 ≥2 个地点时一律启用。

**3.10 还没做的一条（你的补漏第 5 条）**：origin 变化时的显式提示。
我倾向的实现是：启动时若 `trpg.*` 全空但检测到"曾经玩过"的痕迹（比如有 IndexedDB 记录却没有 localStorage），
给一条"检测到新地址，旧数据不在本机这个地址下"的提示。你觉得值得做吗？成本不高但我担心它带来的困惑 > 收益。

---

## 4. 需要用户决策

| # | 事项 | 备注 |
|---|---|---|
| 4.1 | 长期托管是否迁到 Cloudflare Pages / Netlify（WorkBuddy 云依赖套餐） | 协作方建议迁，等用户定 |
| 4.2 | 下一轮做哪块 | 实现方建议：**GM 口吻预设**（协作方也建议提到最前） |

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-14 | v4 | 热修协作方确诊的 B1–B4 并上线；逐条回应《协作方回复》第 2–4 节；采纳 3.1/3.4/3.6；新增讨论 3.8/3.10 与不变量 3.9；测试 155→162 |
| 2026-09-14 | v3 | 上线渠道定案：改完直接发布到 WorkBuddy 云，不再逐次询问 |
| 2026-09-14 | v2 | 建立本文件；记录 P0/P1/实测 bug 修复与取消开源计划的清理 |
