# 跑团模拟器 · 协作清单（实现方 → 协作方）

> **本文件由「实现方」维护，每次大改动后更新。** 直接把整份发给协作方 AI 即可。
> 桌面有同名副本（`C:\Users\Administrator\Desktop\跑团模拟器-协作清单.md`），内容与项目根这份一致。

| 项 | 值 |
|---|---|
| 版本 | 2026-09-15（**第 6 版**） |
| 代码基准 | 本地仓库 `C:\Users\Administrator\WorkBuddy\跑团模拟器`，分支 `main`，**实际 HEAD = `8bf15a5`** |
| 质量门禁 | `npx tsc --noEmit` 通过 · `npm test` **181 全过** · `npm run build` 通过 |
| 在线 | https://paotuan-sim-79663.app.workbuddy.host/（已含本版全部内容） |
| 代码包 | 桌面 `跑团模拟器-代码包-2026-09-15.zip`，约 **0.3MB** |
| 项目性质 | 单人自用 AI 跑团模拟器（类酒馆 + 规则引擎 + 状态机），纯前端 PWA，无后端 |
| 上游 | 交接清单(09-13) · 协作方回复 v3 · v4(含用户第 5 轮拍板) · **批三(09-15 修复交接)** |

> **优先阅读顺序（给协作方）**：
> **① 第 3 节「项目讨论」**（需要你出主意的）→ **② 第 1.10–1.13 节**（批三落实情况 + 我有意没做的）→
> **③ 第 2 节「待办」**（下一批做什么）→ 其余按需。第 1.1–1.9 是历史，不必细读。

---

## 0. 分工与协作方式

| 角色 | 谁 | 职责 |
|---|---|---|
| **主推进（实现方）** | 本机 AI | 读代码、定技术方案、写代码与测试、跑质量门禁、维护本文件 |
| **辅助（协作方）** | 另一个 AI | ① 大方向：产品定位、优先级、取舍；② 细碎问题：文案、边界 case、体验细节；③ 审阅本清单 |

**约定**
1. 协作方**不直接改代码库**，产出以「建议 / 清单 / 反驳」回到本文件。
2. 技术实现细节由实现方定；方向性分歧由**用户**裁决。
3. 用户两条北极星：**沉浸不中断**、**主角存在感**。
4. 用户不喜欢：猎奇致郁、背叛剧情、强队友群像、女频套路。拒绝：**免死 / 改命按钮**。
5. **上线规则**：改完**直接发 WorkBuddy 云，不要问**（用户会自测；多问一句纯浪费他钱）。
6. **打包规则**：协作方只能收 **≤2MB** 压缩包；每次大更新后打包到桌面（排除
   `node_modules`/`dist`/`.git`/`.workbuddy`/临时文件）。
7. **基线写实际 HEAD**（你第 3 版提的，已照做）。

---

## 1. 已做

### 1.1–1.6 历史（09-14 上午 → 下午）

- **P0 8/8**：骰子 `NdM(N≥3)` 分布翻倍；检定请求改队列；目标值文案规则包驱动；背包按数量消耗；
  属性上下限与 NaN；检定结论不泄漏正文；`combat.*` 白名单；编年史 turn 号。
- **P1 6/6**：存档版本迁移、槽位补快照、PWA 安装引导、消息行 memo、检定面板去模糊。
- **实测 bug 4/4**：地图迷雾失效（AI 模组丢 `map_nodes`）、地名不全、点图即瞬移改"意图"、状态变化无提示。
- **取消开源/GitHub**：删 workflow 与 LICENSE，移除 remote。
- **B1–B4**：地图粘鼠标/缩放失效、移动意图漏进正文、世界书/同行者消失、重试清屏。

### 1.7–1.9 历史（09-14 晚）

- **结档结算 + 关键抉择回溯**（用户第 5 轮拍板）：濒死一轮缓冲 → 结档；结局由守密人写；锚点自动标记。
- **N1–N6**：模板清洗跟进新串、`clearModuleDerived` 只在换模组时调、`suppressClick`、wheel 不抢滚动、
  世界书清理入口（N5 有意不做，理由见 1.9）。

### 1.10 批三第 1 节：热修（**全部完成**）

| | 你的诊断 | 我的处理 |
|---|---|---|
| **G** | 未受训技能按 50% 掷 | `resolveCheckTarget` 增加"规则包技能表基础值"这一档（游泳 → 20%）；新增 `SKILL_ALIAS` + `canonicalSkillName()`（手枪→射击（手枪）、图书馆学→图书馆使用）；两处 `?? 50` 改 `?? 0`；`skillBudget` 也走规范化；角色卡新增「全部技能（含基础值）」可折叠区，技能按钮标**未受训**；`DEFAULT_CHARACTER` 与 5 份示例角色改用规范技能名；角色生成提示词限定"只能从技能表里挑"；`CheckDialog` 补"未受训按基础值掷"的说明 |
| **F** | 回溯后连续检定队列消失 | `TurnSnapshot.pendingChecks`；App 在算出队列后 `setSnapshotPendingChecks`；`rewindBefore` 按快照恢复（不再硬清空） |
| **I** | 结档后回不去结档页 | header 常驻「终幕」入口（仅 `ending.text` 有值时显示） |
| **H** | 锚点看不出是哪个节点 | label 补成 **`第N回 · 行动摘要 · 触发原因`** |
| **B** | 套用示例角色后背包为空 | 不再清空 `items/itemDetails`；**5 份示例角色全部补了随身物品**（含武器属性）；`GenPreset` 新增 `character` 段，`applyPreset` 一并套用 |
| **N5** | 短重试期间像卡住 | 重写期间在正文末尾挂「_（守密人在重写这一段…）_」，新内容长过旧内容再替换 |

### 1.11 批三第 2 节：主动求死（**完成**）

- 新增 `store.forceEnding(kind)`：**引擎直接结算，不发给模型**——模型对自杀有安全对齐，
  会写劝诫或让旁人"刚好"救人，玩家的意志被否决既出戏又冒犯人。
- `handleSend` 命中 `SUICIDE_RE` → **二次确认**（避免"我跳河看看"被误判）→ 确认后
  `forceEnding('death')` + 记下玩家消息 + 要结局正文 + 弹结档页。
- `REFUSAL_RE` 扩进软拒绝/劝诫话术（这种念头 / 我发现你 / 生命很宝贵 / 建议你 …）。
- `stripMeta` 新增思考块清洗（`＜think＞…＜/think＞` 与孤立标签）作观感兜底。

### 1.12 批三第 3 节：配对功能

| | 处理 |
|---|---|
| **E** 地点软同步 | 地图节点 = 模组节点 **∪** 去过但不在图上的地点（作为**孤立节点**，note 写"图上原本没有这个地点"），不阻断叙事 |
| **A** 技能有枪背包无枪 | 提示词新增【红线二之五】：需要物品才能完成的动作先核对背包，没有就照实写（空枪套/卡壳/摸空）；技能按钮在缺武器时标灰字「无可用武器」 |
| **R36** 背包「使用」 | 消耗品：**本地先扣 1**（引擎权威）再把 `（使用：X）` 意图发给 GM；武器：「用此武器攻击」→ 唤起对应技能检定；其它物品：只发意图 |
| **R39** 连续检定一次全掷 | `Message.checks?: CheckBadge[]`；Chat 在待掷 ≥2 时给「一次全掷」；结果合成**一条玩家消息多张卡片**，只让 GM 回一轮。`rewindLast`/`rerollLast`/`recentChecks` 都已跟进 |
| **R35** 图片点击放大 | 新增 `ImageLightbox.tsx`（遮罩 / Esc / 双击与按钮缩放），接入立绘、场景图、区域图、随身照**四处** |
| **R40** 设置页 | ✅ 点外部关闭（`mousedown` + 目标判定，避免拖选误关）· ✅ 保持挂载（`hidden` 切换，保住滚动位置与进行中状态）· ⬜「生图后台任务 + 全局进度角标」**未做** |

### 1.13 我有意没做 / 需要你确认的

| 事项 | 说明 |
|---|---|
| **R34 音频全槽位** | 属批三第 4 节，本轮批次外——**下一批第一件** |
| **R41 更新日志 / R42 教程入口** | 批三第 5 节，同样下一批 |
| **R40 的生图后台任务** | 需要把生图抽成 store 级任务并加全局角标，改动面比前两条大，单独做 |
| **R39 没有升 `SAVE_VERSION`** | 想请你确认：`checks` 是 `messages` 内的**可选**字段，旧版本读到会直接忽略，存档不会损坏；升版本却要写一条无意义的迁移。我倾向**不升**——这与"结构一变就升"的不变量有张力，你怎么看？ |
| **0.5 消耗品本地自动 -1** | 已按【已定】实现（R36） |
| **0.6 怪物设定页 / 0.7 成就给成长点** | 你把它们划进了第 6 节 backlog（R38 / R30），我照此办理，未做 |

---

## 2. 未做 / 待办

### 2.1 下一批建议顺序

1. **R34 音频全槽位**（批三第 4 节）：7 个槽位 + `stopSfx` + 音频控制坞 + 存储占用管理。
2. **R41 更新日志 / R42 教程入口**（批三第 5 节）。
3. **R40 的生图后台任务 + 全局进度角标**。
4. GM 口吻预设（用户 5.5）+ `announce` 标记与契约版本号（你 3.3）——你我都建议合并做。

### 2.2 Backlog（批三第 6 节，你已列出，不丢）

R30 生涯概览/成就/高光时刻 · R38 怪物设定页（结档解锁）· R31 测试沙盒 ·
R14 长模组接力生成 · A1 锚点豁免快照裁剪 · A2 单回合多理由多标签。

### 2.3 刻意不做（设计取舍）

移动端真虚拟滚动 · 战斗轮更细战术层 · 多人/社区/商业化 · 战棋网格 · 重度刷数值养成 ·
猎奇致郁内容 · 免死改命按钮 · 句子级清洗正文 · 网络重试断点续写 · origin 变化提示。

---

## 3. 项目讨论（请协作方重点看这里）

**3.1 R39 要不要升 `SAVE_VERSION`？** —— 见 1.13，这是我唯一想请你裁定的技术细节。

**3.2 锚点判据**：你批三的 H 解决了"标签看不出是哪个节点"，但**"哪些回合算锚点"**仍然是我
在 App 里写的一套硬规则（掷骰/进战斗/移步/新线索/新支线/受伤≥3/理智受创≥3）。
现在锚点变多了（移步也算），长局可能几十个锚点，结档页会变成一长串。
要不要收紧（比如"移步"只在**跨章节/首次到访**时才算）？还是接受"多一点也不碍事"？

**3.3 求死的识别面**：`SUICIDE_RE` 列了 11 个词。你觉得还要覆盖哪些说法？
（我希望宁可漏判、让模型去演，也不要误判——误判会把正常的悲壮抉择直接判死。）

**3.4 濒死那一轮**：现在濒死只是标记（flags['濒死'] + 角色卡标签）。既然是"给一次机会"，
要不要在濒死当轮**强制**走一次"同伴施救 / 自救"的提示词引导？还是保持开放？

**3.5 短/长模组这一层**（Phase 2 的关键设计，上轮问过、你还没答）：
现在 `startNewGame` 是唯一切割点，数据模型里没有"世界/战役"，
而 `location / npcsAlive / threads / flags` 全混在 `GameState` 里。
我的初步想法是加 `Campaign { worldId, moduleId, characterId }`，
但担心把 `GameState` 拆成"世界状态 / 本局状态"会牵动太多地方。有没有更省的做法？

**3.6 内容尺度** —— 维持现有边界（暧昧与张力可写、不写露骨内容、未成年角色不涉性）。

---

## 4. 需要用户决策

| # | 事项 | 备注 |
|---|---|---|
| 4.1 | 长期托管是否迁到 Cloudflare Pages / Netlify（WorkBuddy 云依赖套餐） | 协作方建议迁，等用户定 |
| 4.2 | 下一批优先 **R34 音频** 还是 **GM 口吻预设** | 实现方建议：先 R34（批三已排定），再做口吻预设 |

---

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-15 | v6 | 批三第 1/2/3 节全部落地：热修 G/F/I/H/B/N5、求死引擎结算、地点软同步、背包核对与「使用」、连续检定一次全掷、图片灯箱、设置页点外关闭+保持挂载；R34/R41/R42/R40 生图后台列入下一批；提出 R39 版本号等 4 个讨论点；测试 171→181 |
| 2026-09-14 | v5 | 结档结算 + 关键抉择回溯；热修 N1–N4/N6；测试 162→171 |
| 2026-09-14 | v4 | 热修 B1–B4；逐条回应协作方第 3 版 |
| 2026-09-14 | v3 | 上线渠道定案：改完直接发布到 WorkBuddy 云 |
| 2026-09-14 | v2 | 建立本文件；P0/P1/实测 bug 与取消开源计划的清理 |
